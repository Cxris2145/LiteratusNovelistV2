# NIGHT_AUDIT_LOG — Literatus Novelist

## 2026-09-03T04:35:09Z — corrida horaria #1

- **Entorno**: repo OK (`git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Rama de trabajo `agent/nightly-optimization-chris` no existía ni local ni en origin -> bootstrap desde `origin/Chris`.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` ("feat(catalog, reader, standardization): update library standardization, audio/assisted reading service, API optimizations, and documentation"), confirmado que viene de `origin/Chris`.
- **Push verificado (sección 2.9)**: en una corrida anterior (misma sesión) el `git push --dry-run` había fallado con 403 (GitHub App sin acceso instalado en la organización). Tras habilitarse el acceso por el dueño del repo, se reintentó: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> **OK**. Se hizo el bootstrap real: `git push -u origin agent/nightly-optimization-chris`, confirmado con `git ls-remote` (hash remoto == HEAD local, `3786d288...`).
- **Smoke check (Fase A)**: **PRUEBAS NO DISPONIBLES**. El entorno de esta rutina trae Python 3.11.15; `requirements.txt` pinea `Django==6.0.4`, que exige Python >=3.12. No hay servidor PostgreSQL corriendo (`pg_isready` sin respuesta) ni archivo `.env` (solo `.env.example`, sin `SECRET_KEY`). No se instaló Django ni el resto del stack pesado (`ctranslate2`, `onnxruntime`, `faster-whisper`, etc. — fuera del alcance de "mínimo para correr las pruebas"). Por lo tanto no se pudo ejecutar `manage.py check`, `makemigrations --check --dry-run` ni `manage.py test`. Registrado como MR-0001.
- **Fase y lote trabajados**: Fase A (errores críticos), lote acotado a hallazgos verificables sin BD/Django.
- **Cambios aplicados**:
  - `backend/requirements.txt` — el archivo estaba codificado en **UTF-16LE con CRLF** (mojibake tipo "espacio entre cada carácter" al abrirlo como texto normal), lo que rompe `pip install -r requirements.txt` en cualquier entorno estándar. Se reconvirtió a **UTF-8 con LF**, preservando el contenido y las 90 dependencias pineadas sin alterar ninguna versión. Verificado: `file` ahora reporta `ASCII text`; las 90 líneas se validaron como especificadores de paquete bien formados.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F requiere BD para correlacionar `Book.cover_image` / `Author.photo` / etc. con archivos; bloqueada por falta de BD, ver MR-0001).
- **NEEDS MANUAL REVIEW nuevos**:
  - **MR-0001** (Fase A): entorno de esta rutina sin Python 3.12+ y sin PostgreSQL disponible; bloquea `manage.py check`/`test` y por extensión las Fases B–H (todo lo que depende de Django/BD). Se necesita: (a) Python 3.12+ en el entorno donde corre la rutina, y (b) una base de datos PostgreSQL accesible con `DATABASE_URL` (o credenciales) para que la rutina pueda leer/corregir datos reales de libros, autores, avatares, portadas, etc.
- **Pruebas**: ninguna corrida (ver "Smoke check" arriba). Cambio aplicado (encoding de `requirements.txt`) verificado de forma manual: `file` + parseo de cada línea como especificador de paquete válido (90/90 OK), sin depender de Django/BD.
- **Bundle initial**: no aplica (sin cambios de frontend esta corrida).
- **Commit**: pendiente de crear en este mismo lote (ver commit siguiente en el historial de git).
- **Push confirmado en origin**: sí, para el bootstrap de la rama (`3786d288...`). El commit de este lote se sube a continuación.

## 2026-09-03T04:46:00Z — corrida horaria #2

- **Entorno**: repo OK (`git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Guarda de solapamiento: `run_in_progress` era `false` -> se procedió. Rama `agent/nightly-optimization-chris` existía en origin -> `git checkout -B agent/nightly-optimization-chris origin/agent/nightly-optimization-chris`.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado igual al `base_commit` del STATE y a `origin/Chris` actual (no hubo cambios nuevos en `Chris` desde la corrida anterior).
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> **OK**. Push verificado antes de trabajar.
- **Smoke check (Fase A)**: se detectó que el entorno SÍ trae `python3.12` (`/usr/bin/python3.12`, Python 3.12.3) además de `python3` (3.11.15, el que se probó en la corrida #1). Se creó un venv aislado con `python3.12 -m venv` y se instaló `backend/requirements.txt` completo (90 paquetes) sin errores (`pip install -r requirements.txt` -> exit 0). Se generó un `.env` **local, no versionado** (gitignored) apuntando a una BD `sqlite` temporal solo para poder cargar `settings.py` (que exige `SECRET_KEY`/`DATABASE_URL`); no se usó ninguna credencial real ni se tocó `.env.example`. Con eso:
  - `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`.
  - `python -X utf8 manage.py makemigrations --check --dry-run` -> `No changes detected` (exit 0).
  - `python -X utf8 manage.py test` -> `Ran 67 tests in 36.942s` -> `OK`.
  Esto resuelve la causa raíz de **MR-0001** (falta de Python 3.12): el entorno sí lo tiene, solo no se había probado. Sigue sin haber un dump real de la BD de producción (la sqlite usada está vacía, generada solo desde migraciones) — ver **MR-0002**.
- **Fase y lote trabajados**: Fase A (validación de entorno/código, sin datos) + un ítem puntual de Fase I (dependencia duplicada en frontend), dado que Fase B en adelante sigue bloqueada por falta de datos reales (MR-0002).
- **Cambios aplicados**:
  - `respaldos-software/LiteratusNovelist-main/Producto/frontend/package.json` — eliminada la dependencia `lucide-angular` (^1.0.0), duplicada con `@lucide/angular` (^1.16.0). Verificado con `grep` que **ningún** archivo de `src/` importa desde `'lucide-angular'` (0 coincidencias); solo `@lucide/angular` está en uso (1 import, en `src/app/app.module.ts`). Fase I la señala explícitamente como caso a unificar.
  - `respaldos-software/LiteratusNovelist-main/Producto/frontend/package-lock.json` — regenerado con `npm install` tras el cambio anterior (no editado a mano).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F requiere BD real con filas de `Book`/`Author`/etc.; sigue bloqueada, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**:
  - **MR-0002** (reemplaza el motivo de MR-0001, que ya no aplica en la parte de Python): el entorno de la rutina no tiene un dump/backup restaurable de la base de datos de producción ni acceso a `respaldos-software/books//images/`. Aunque `manage.py check`/`makemigrations`/`test` ya corren OK con Python 3.12, las Fases B a H (libros, capítulos, personajes IA, autores, sinopsis/portadas, duplicados de imágenes, lector/audiolibro, backend/API con datos reales) necesitan datos reales para auditar algo — no hay nada que corregir sobre una BD vacía. Se necesita un dump de Postgres restaurable (o `DATABASE_URL` de un entorno de staging con datos reales) y acceso de lectura a las carpetas de imágenes de libros/autores.
- **Pruebas**: `manage.py check` OK, `makemigrations --check --dry-run` OK (`No changes detected`), `manage.py test` -> 67/67 OK (~37s), todo contra sqlite local temporal (no versionada). Frontend: `npm install` OK (1105 paquetes, sin nuevos paquetes agregados — uno removido), `npm run build` -> exit 0, sin errores nuevos (solo warnings preexistentes de CommonJS en `canvg`/`html2canvas`/`lottie-web`, no relacionados con el cambio).
- **Bundle initial**: no se pudo comparar "antes vs después" de forma estricta porque `node_modules` no estaba instalado al inicio de esta corrida (0 paquetes) — se instaló por primera vez ya con `lucide-angular` removido. `main-*.js` resultante: ~1.03 MB (sin comprimir). El cambio en sí (remover una dependencia no importada por ningún archivo) no puede aumentar el bundle y como mucho lo reduce levemente; efecto esperado neutro-a-positivo, no verificado con comparación numérica exacta.
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `package.json`, `package-lock.json`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` inmediatamente después del push, registrada en el informe final de esta corrida.

## 2026-09-03T05:15:00Z — corrida horaria #3

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2` (Cxris2145/LiteratusNovelistV2). `git status` limpio al iniciar. Guarda de solapamiento: `run_in_progress` era `false` -> se procedió. Rama `agent/nightly-optimization-chris` existía en origin -> `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris`.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado igual al `base_commit` del STATE y a `origin/Chris` actual (sin cambios nuevos en `Chris` desde la corrida #2).
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` (sin error de autenticación/permiso). Push verificado antes de trabajar.
- **Smoke check (Fase A)**: venv nuevo con `python3.12` (3.12.3), `pip install -r requirements.txt` (90 paquetes) OK sin errores. `.env` local temporal (no versionado) apuntando a sqlite temporal solo para poder cargar `settings.py`. `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. `python -X utf8 manage.py makemigrations --check --dry-run` -> `No changes detected`. No se corrió `manage.py test` completo (no hubo cambios de código backend esta corrida; ya validado 67/67 OK en la corrida #2 sobre el mismo commit base). `.env`, sqlite temporal y el venv se borraron al terminar.
- **Fase y lote trabajados**: Fase B (`libros_capitulos`) sigue **bloqueada** por MR-0002 (sin dump/BD real de producción). Se avanzó **Fase J (rendimiento)** en su lugar, ya que no requiere BD y sí es accionable: revisión de imports estáticos de librerías pesadas en el frontend (Angular) listadas explícitamente en la sección J del prompt (`jspdf`, `jspdf-autotable`, `lottie-web`, `@huggingface/transformers`).
- **Cambios aplicados**:
  - `respaldos-software/LiteratusNovelist-main/Producto/frontend/src/app/dashboard/overview/overview.component.ts` — los imports estáticos `import jsPDF from 'jspdf'` e `import autoTable from 'jspdf-autotable'` se movieron a un `import()` dinámico dentro del método `exportPdf()` (el único lugar donde se usan, invocado solo por clic del usuario en "Exportar PDF"). Sin cambios de plantilla, estilos ni comportamiento visible; misma salida de PDF.
- **Verificación de rendimiento (`npm run build` / `ng build --configuration production`)**:
  - **ANTES** (import estático, HEAD sin el cambio vía `git stash`): chunk lazy `dashboard-module` = **574.83 kB** raw / **144.14 kB** transferencia (jspdf + jspdf-autotable + html2canvas embebidos en el chunk de la ruta del dashboard).
  - **DESPUÉS** (con el cambio): chunk lazy `dashboard-module` = **142.16 kB** raw / **25.91 kB** transferencia; `jspdf` (401.47 kB / 110.19 kB), `jspdf-plugin-autotable` (30.33 kB / 8.79 kB) y `html2canvas` (198.36 kB / 37.61 kB) pasan a chunks lazy independientes, descargados solo si el usuario exporta el PDF.
  - Bundle **Initial** (app shell): sin cambio, 1.63 MB / 338 kB en ambos casos (esperado: el dashboard ya era una ruta lazy-loaded; el ahorro es en el chunk de esa ruta, no en el shell inicial).
  - Build compiló sin errores en ambos casos (solo los mismos warnings preexistentes de CommonJS en `canvg`/`html2canvas`/`lottie-web`, no relacionados con el cambio).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno nuevo. Se actualizó la redacción de **MR-0002** para reflejar que el árbol de imágenes/EPUBs reales SÍ está disponible en el repo (`respaldos-software/books/`, 1103 libros) y que existe un script 100% local (`scripts/db_setup/bulk_db_injection.py`) que podría poblar una BD de prueba real a partir de esos EPUBs — pero no se ejecutó esta corrida por acotarse el lote a ~25 min y porque requiere confirmación explícita del dueño sobre si usar esa vía en lugar de esperar un dump real de producción.
- **Pruebas**: backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (sin cambios de código backend, no se corrió la suite completa esta vez). Frontend — `npm install` OK (1105 paquetes, igual que la corrida #2), `ng build --configuration production` OK (dos builds: antes/después del cambio, ambos exitosos). `npm test` (Angular unit tests): **OMITIDA** — no hay binario de Chrome/Chromium headless en este entorno (`CHROME_BIN` vacío, sin `chromium`/`google-chrome` en PATH).
- **Bundle**: ver detalle arriba (Initial sin cambio; `dashboard-module` -432.67 kB raw / -118.23 kB transferencia).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `overview.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T05:53:32Z — corrida horaria #4

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`. `git status` limpio al iniciar. Guarda de solapamiento: `run_in_progress` era `false` (último heartbeat `2026-09-03T05:15:00Z`, 38 min antes) -> se procedió. Rama `agent/nightly-optimization-chris` existía en origin -> `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris` + `git pull --ff-only` (ya estaba al día).
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado igual a `origin/Chris` actual (sin cambios nuevos en `Chris` desde la corrida #3) y al `base_commit` del STATE.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` (sin error de autenticación/permiso). Push verificado antes de trabajar.
- **Smoke check (Fase A)**: venv nuevo con `python3.12` (3.12.3), `pip install -r requirements.txt` (90 paquetes) OK. `.env` local temporal (no versionado, gitignored) apuntando a sqlite temporal solo para cargar `settings.py`. `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. `python -X utf8 manage.py makemigrations --check --dry-run` -> `No changes detected`. No se corrió `manage.py test` completo (sin cambios de código backend esta corrida; ya validado 67/67 OK en la corrida #2 sobre el mismo base_commit).
- **Fase y lote trabajados**: Fase B (`libros_capitulos`) sigue **bloqueada** por MR-0002. Se continuó **Fase J (rendimiento)** siguiendo el cursor de la corrida #3 ("revisar lottie-web (3 componentes) y GenreViewSet sin paginación").
- **Cambios aplicados**:
  - `frontend/src/app/home/home.component.ts` — `import lottie from 'lottie-web'` (estático) movido a `import('lottie-web').then(({ default: lottie }) => ...)` dentro de los dos setters `@ViewChild` (`readingContainer`, `saludoContainer`) que lo usan. Mismo comportamiento (misma animación Lottie), solo carga asíncrona.
  - `frontend/src/app/library/reader/reader.component.ts` — mismo cambio en el setter `@ViewChild('loadingLottie')`.
  - `frontend/src/app/library/library-list/library-list.component.ts` — mismo cambio en el setter `@ViewChild('libritoContainer')`.
  - `GenreViewSet` (`catalog/views.py`) — revisado, **sin cambio**: no era un hallazgo real. `REST_FRAMEWORK.DEFAULT_PAGINATION_CLASS` ya está fijado globalmente en `config/settings.py` a `core.pagination.StandardResultsSetPagination` (`PAGE_SIZE=12`, `max_page_size=50`) y `GenreViewSet` no lo sobrescribe con `pagination_class = None`, así que ya hereda paginación estándar.
- **Verificación de rendimiento (`ng build --configuration production`, comparando antes/después con `git stash` sobre el mismo commit)**:
  - **ANTES**: Initial total = **1.63 MB** raw / **338.24 kB** transferencia (`main-*.js` 1.00 MB, `lottie-web` embebido en el chunk de `reader.component.ts` dentro del bundle inicial porque los 3 componentes se referencian de forma estática, no lazy, en `app-routing.module.ts`).
  - **DESPUÉS**: Initial total = **1.34 MB** raw / **274.68 kB** transferencia (`main-*.js` 724.41 kB), con `lottie-web` ahora en un chunk lazy independiente (`chunk-NYKGWHZM.js`, nombre `lottie`, 300.82 kB raw / 64.00 kB transferencia) que solo se descarga cuando se monta alguno de los 3 componentes.
  - **Ahorro**: **-290 kB raw / -63.56 kB transferencia** en el bundle inicial (app shell), sin cambio de DOM/estilos/comportamiento visible.
  - Build compiló sin errores en ambos casos (mismos warnings preexistentes de CommonJS en `canvg`/`html2canvas`/`lottie-web`, no relacionados con el cambio).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno nuevo. MR-0002 sigue abierta y sin cambios (sigue faltando dump real de BD de producción o autorización para `bulk_db_injection.py` sobre datos de prueba locales).
- **Pruebas**: backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (1105 paquetes). `ng build --configuration production` OK (dos builds: antes/después). **Angular unit tests** (`ng test --watch=false --browsers=ChromeHeadless`): por primera vez se pudieron correr — el entorno sí trae un Chromium headless (`/opt/pw-browsers/chromium`, de Playwright), pero fallaba con `Running as root without --no-sandbox`; no hay `karma.conf.js` en el repo con un `customLauncher`. Se usó `CHROME_BIN` apuntando a un wrapper temporal **fuera del repo** (un script de una línea que añade `--no-sandbox --disable-gpu --disable-dev-shm-usage`; no se commiteó nada). Resultado: **8 de 8 tests OK**.
- **Bundle**: ver detalle arriba (Initial: 1.63 MB -> 1.34 MB raw; 338.24 kB -> 274.68 kB transferencia).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `home.component.ts`, `reader.component.ts`, `library-list.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T07:05:00Z — corrida horaria #5

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`. `git status` limpio al iniciar. Guarda de solapamiento: `run_in_progress` era `false` (último heartbeat `2026-09-03T06:02:02Z`, 52 min antes) -> se procedió. Rama `agent/nightly-optimization-chris` existía en origin -> `git checkout agent/nightly-optimization-chris` + `git pull --ff-only` (ya estaba al día, `bc3da858...`).
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado igual a `origin/Chris` actual (`git merge-base agent/nightly-optimization-chris origin/Chris` == HEAD de `Chris`; sin cambios nuevos en `Chris` desde la corrida #1) y al `base_commit` del STATE.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` (sin error de autenticación/permiso). Push verificado antes de trabajar.
- **Smoke check (Fase A)**: venv nuevo con `python3.12` (3.12.3), `pip install -r requirements.txt` (90 paquetes) OK. `.env` local temporal (no versionado, gitignored) apuntando a sqlite temporal solo para cargar `settings.py`. `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. `python -X utf8 manage.py makemigrations --check --dry-run` -> `No changes detected`. No se corrió `manage.py test` completo (sin cambios de código backend esta corrida; ya validado 67/67 OK en la corrida #2 sobre el mismo base_commit).
- **Fase y lote trabajados**: Fase B (`libros_capitulos`) sigue **bloqueada** por MR-0002. Se continuó **Fase J (rendimiento)** siguiendo el cursor de la corrida #4: "revisar `@huggingface/transformers` en `sherpa-tts.worker.ts`" y "añadir preconnect/dns-prefetch a Supabase en `index.html`".
- **Cambios aplicados**:
  - `frontend/src/index.html` — se agregaron `<link rel="preconnect" href="https://srbmswjsbkpftjabcurg.supabase.co" crossorigin>` y `<link rel="dns-prefetch" href="https://srbmswjsbkpftjabcurg.supabase.co">` (dominio tomado de `environment.ts`/`environment.prod.ts`), antes de los `preconnect` existentes de Google Fonts. Sin cambio de DOM visible/estilos; solo adelanta la resolución DNS/TLS hacia Supabase (portadas/storage). Verificado que `dist/frontend/browser/index.html` contiene ambas etiquetas tras `ng build --configuration production`.
  - `sherpa-tts.worker.ts` — **sin cambio de código**, revisado a fondo y descartado como falso hallazgo: el worker ya se compila como build target propio de esbuild (`dist/frontend/browser/worker-*.js`, ~512 KB), completamente separado del bundle inicial (`main-*.js` 724.41 kB) y de los chunks lazy de rutas; solo se descarga cuando `WasmTtsService.initWorker()` ejecuta `new Worker(new URL('../workers/sherpa-tts.worker', import.meta.url))`, y eso solo ocurre al instanciarse `reader.component.ts` (abrir la vista del lector), que es justamente la condición pedida en la sección J ("solo al abrir su vista"). Convertir el `import` estático interno del worker a `import()` dinámico no reduciría el bundle inicial (ya está aislado) y, como `initWorker()` llama `postMessage({type:'INIT'})` inmediatamente tras crear el worker, la descarga real de `@huggingface/transformers` ocurriría casi al mismo tiempo de todas formas. Retrasarla más (p. ej. hasta el primer clic en "reproducir audio") cambiaría la latencia/comportamiento percibido del lector, lo que excede el alcance de un cambio puramente de bundling y es ambiguo -> se descarta sin tocar código, análogo a `GenreViewSet` en la corrida #4.
- **Verificación (`ng build --configuration production`)**: build OK, 0 errores, mismos warnings preexistentes de CommonJS (`canvg`/`html2canvas`/`lottie-web`), no relacionados con el cambio. Bundle **Initial** sin cambio respecto a la corrida #4 (esperado: `index.html` no afecta el tamaño de los bundles JS): `main-IVHP7WYD.js` 724.41 kB / 128.60 kB transferencia + chunks compartidos (455.80 kB, 47.22 kB, 8.35 kB, 2.73 kB) + `styles` 96.90 kB / 9.95 kB + `polyfills` 33.71 kB / 11.02 kB. Worker `sherpa-tts` confirmado como archivo aparte (`worker-54QLZBKY.js`, 512 KB), fuera de las listas de "Initial chunk files" / "Lazy chunk files".
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno nuevo. MR-0002 sigue abierta y sin cambios.
- **Pruebas**: backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (1105 paquetes). `ng build --configuration production` OK. `ng test --watch=false --browsers=ChromeHeadless` (mismo wrapper temporal `CHROME_BIN` fuera del repo que en la corrida #4, con `--no-sandbox --disable-gpu --disable-dev-shm-usage`; nada commiteado) -> **8 de 8 tests OK**.
- **Bundle**: sin cambio en el Initial (ver detalle arriba); el cambio de esta corrida es de latencia de red (preconnect), no de tamaño de bundle.
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `index.html`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T08:02:22Z — corrida horaria #6

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`. `git status` limpio al iniciar (rama `claude/admiring-noether-xb5fkc` de la sesión base, ajena a esta rutina). Guarda de solapamiento: `run_in_progress` era `false` (último heartbeat `2026-09-03T07:05:00Z`, ~49 min antes) -> se procedió. Rama `agent/nightly-optimization-chris` existía en origin -> `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris` (tras `git fetch origin --prune`, que trajo `Chris`, `Felipe`, `eithan`, `agent/nightly-optimization` y `agent/nightly-optimization-chris`, ausentes del clon inicial). Ya estaba al día con origin (`498b1d70...`, sin cambios pendientes de reintegrar).
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado igual a `origin/Chris` actual vía `git merge-base origin/Chris origin/agent/nightly-optimization-chris` (sin cambios nuevos en `Chris` desde la corrida #1) y al `base_commit` del STATE.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` (sin error de autenticación/permiso). Push verificado antes de trabajar.
- **Smoke check (Fase A)**: el entorno traía `python3` por defecto en 3.11.15 (distinto a las corridas #2/#5, que ya arrancaban en 3.12.3); se creó el venv explícitamente con `python3.12` (3.12.x disponible en `/usr/bin/python3.12`). `pip install -r requirements.txt` OK. `.env` local temporal (no versionado, gitignored) apuntando a sqlite temporal solo para cargar `settings.py`. `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. `python -X utf8 manage.py makemigrations --check --dry-run` -> `No changes detected`. No se corrió `manage.py test` completo (sin cambios de código backend esta corrida).
- **Fase y lote trabajados**: Fase B (`libros_capitulos`) sigue **bloqueada** por MR-0002. Se continuó **Fase J (rendimiento)** siguiendo el cursor de la corrida #5: "revisar deduplicación de llamadas HTTP (shareReplay/cache en memoria) en servicios de catálogo, y presupuestos de bundle en angular.json".
- **Cambios aplicados**:
  - `frontend/angular.json` — budget `initial` del build de producción bajado de `maximumWarning: 2mb` / `maximumError: 5mb` a `1.6mb` / `2.5mb`. El bundle inicial real es 1.34 MB (idéntico a las corridas #4 y #5, sin regresión), por lo que el umbral anterior dejaba ~660 KB de margen antes de avisar; con el nuevo umbral el build avisa con solo ~260 KB de crecimiento, detectando antes una futura regresión de tamaño. Cambio puramente de configuración de build, sin efecto en tiempo de ejecución ni en el DOM.
  - `api.service.ts` / `ngsw-config.json` — **sin cambio de código**, solo investigación: se detectó que `ApiService.get<T>()` agrega un parámetro `_t=Date.now()` a **todas** las peticiones GET de la app (comentario existente: "evitar caché agresivo del navegador"). Esto invalida la caché HTTP del navegador en general y, más relevante, hace inefectivo el `dataGroup` `api-inventory` ya configurado en `ngsw-config.json` (`**/api/v1/library/inventory/**`, estrategia `performance`, 30 días) si esas llamadas pasan por `ApiService.get`, porque el Angular Service Worker matchea por URL completa (incluye query string) y cada `_t` distinto es un cache-miss nuevo. No se modificó por ser un cambio de comportamiento amplio (afecta toda la app) y potencialmente enmascarar un bug real de datos obsoletos en algún endpoint de escritura -> registrado como **MR-0003** para decisión del dueño.
- **Verificación (`ng build --configuration production`)**: build OK, 0 errores, **sin warnings de budget** con el nuevo umbral (bundle real 1.34 MB < 1.6 MB de warning), mismos warnings preexistentes de CommonJS (`canvg`/`html2canvas`/`lottie-web`). Bundle **Initial** sin cambio respecto a corridas #4/#5: `main-IVHP7WYD.js` 724.41 kB + chunks compartidos 455.80/47.22/8.35/2.73 kB + `styles` 96.90 kB + `polyfills` 33.71 kB = 1.34 MB raw / 274.68 kB transferencia.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: **MR-0003** (`_t` de cache-busting en `ApiService.get` invalida caché HTTP/Service Worker en toda la app; ver detalle en `NIGHT_AUDIT_STATE.json`). MR-0002 sigue abierta y sin cambios.
- **Pruebas**: backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (1105 paquetes, 0 previamente instalados). `ng build --configuration production` OK. `ng test --watch=false` con `karma.conf.js` temporal fuera del repo (`CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, launcher custom `--no-sandbox --disable-gpu --disable-dev-shm-usage`, nada commiteado) -> **8 de 8 tests OK**.
- **Bundle**: Initial sin cambio de tamaño (1.34 MB); el cambio de esta corrida es solo el umbral de alerta del budget, no el bundle en sí. Antes: warning 2mb/error 5mb. Después: warning 1.6mb/error 2.5mb.
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `angular.json`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T08:54:12Z — corrida horaria #7

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`. `git status` limpio al iniciar. Guarda de solapamiento: `run_in_progress` era `false` (último heartbeat `2026-09-03T08:02:22Z`, 52 min antes) -> se procedió. Rama `agent/nightly-optimization-chris` existía en origin -> `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris` (tras `git fetch origin Chris agent/nightly-optimization-chris main`). Ya estaba al día con origin (`030e9090...`).
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado igual a `origin/Chris` actual vía `git merge-base origin/Chris HEAD` (sin cambios nuevos en `Chris` desde la corrida #1) y al `base_commit` del STATE.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` (sin error de autenticación/permiso). Push verificado antes de trabajar.
- **Smoke check (Fase A)**: venv nuevo con `python3.12` (entorno trae `python3` 3.11.15 como default). `pip install -r requirements.txt` OK. `.env` local temporal (no versionado, gitignored) apuntando a sqlite temporal solo para cargar `settings.py`. `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. `python -X utf8 manage.py makemigrations --check --dry-run` -> `No changes detected`. No se corrió `manage.py test` completo (sin cambios de código backend esta corrida).
- **Fase y lote trabajados**: Fase B (`libros_capitulos`) sigue **bloqueada** por MR-0002. Se trabajó **Fase I (frontend — OnPush/trackBy)**, siguiendo la sugerencia dejada en el cursor de la corrida #6 ("evaluar OnPush/trackBy en los componentes de listados grandes de biblioteca/catálogo").
- **Cambios aplicados** (7 componentes, solo se agregó `trackBy` a `*ngFor` existentes + su método `trackBy*`; sin cambios de DOM/estilos/comportamiento visible):
  - `catalog/author-list/author-list.component.ts` + `.html` — nuevo método `trackByAuthor` (usa `author.slug`), aplicado al `*ngFor` de `filteredAuthors`.
  - `library/library-list/library-list.component.ts` + `.html` — nuevo método `trackByItem` (usa `item.id`), aplicado al `*ngFor` de `inventoryItems`.
  - `discover/discover.component.ts` + `.html` — nuevos métodos `trackByBook` (usa `book.slug`/`book.id`) y `trackByMatchPair`, aplicados a `quoteBooks`, `trendingBooks`, `iaBooks`, `quickBooks` y `matchPairs`.
  - `characters/character-hub/character-hub.component.ts` + `.html` — nuevo método `trackByChar` (usa `char.id`), aplicado a `filteredCharacters`, `recentCharacters`, `featuredCharacters` y `allCharacters`.
  - `dashboard/books/books.component.ts` + `.html` — nuevo método `trackByBook` (usa `book.id`), aplicado a `filteredBooks`.
  - `dashboard/avatars/avatars.component.ts` + `.html` — nuevo método `trackByAvatar` (usa `avatar.id`), aplicado a `filteredAvatars`.
  - `dashboard/authors/authors.component.ts` + `.html` — nuevo método `trackByAuthor` (usa `author.id`), aplicado a `filteredAuthors`.
  - `catalog/book-list/book-list.component.ts` — revisado, **sin cambio**: ya tenía `trackByBook`/`trackByCategory` correctamente aplicados desde antes.
  - **No** se aplicó `ChangeDetectionStrategy.OnPush` a ningún componente esta corrida — queda para una corrida futura, evaluando con cuidado los `@ViewChild` setters, `setTimeout` de debounce y suscripciones manuales que tienen varios de estos componentes (podrían necesitar `markForCheck()` explícito antes de poder marcarlos OnPush con seguridad).
- **Verificación (`ng build --configuration production`)**: build OK, 0 errores, mismos warnings preexistentes de CommonJS (`canvg`/`html2canvas`/`lottie-web`), sin warnings de budget. Bundle **Initial**: `main-CA55TLKQ.js` 724.77 kB + chunks compartidos 455.80/47.22/8.35/2.73 kB + `styles` 96.90 kB + `polyfills` 33.71 kB = **1.34 MB raw / 274.82 kB transferencia** (antes, corrida #6: 274.68 kB; +0.14 kB por los nuevos métodos `trackBy*`, sin regresión real — muy por debajo del budget 1.6mb/2.5mb).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (1105 paquetes; primer intento falló por `ECONNRESET` en el postinstall de `onnxruntime-node`, reintento exitoso sin cambiar dependencias). `ng build --configuration production` OK. `ng test --watch=false` con `karma.conf.js` temporal fuera del repo (`CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, launcher custom `--no-sandbox --disable-gpu --disable-dev-shm-usage`, nada commiteado) -> **8 de 8 tests OK**.
- **Bundle**: Initial 1.34 MB (sin cambio relevante respecto a corridas #4-#6); ver detalle arriba.
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y los 7 componentes listados arriba).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T09:53:44Z — corrida horaria #8

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`. `git status` limpio al iniciar (rama `claude/admiring-noether-k3ionj` de la sesión base, ajena a esta rutina). Guarda de solapamiento: `run_in_progress` era `false` (último heartbeat `2026-09-03T09:01:07Z`, ~52 min antes) -> se procedió. Rama `agent/nightly-optimization-chris` existía en origin -> `git fetch origin --prune` (trajo `Chris`, `Felipe`, `eithan`, `agent/nightly-optimization`, `agent/nightly-optimization-chris`, ausentes del clon inicial de esta sesión) + `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris`. Ya estaba al día con origin (`f9e0335e...`, sin cambios pendientes de reintegrar).
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado igual a `origin/Chris` actual vía `git merge-base origin/Chris HEAD` (sin cambios nuevos en `Chris` desde la corrida #1) y al `base_commit` del STATE.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` (sin error de autenticación/permiso). Push verificado antes de trabajar.
- **Smoke check (Fase A)**: venv nuevo con `python3.12` (disponible en `/usr/bin/python3.12`; requirements.txt exige Django==6.0.4 que requiere >=3.12). `pip install -r requirements.txt` OK. `.env` local temporal (no versionado, gitignored) apuntando a sqlite temporal solo para cargar `settings.py`. `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. `python -X utf8 manage.py makemigrations --check --dry-run` -> `No changes detected`. No se corrió `manage.py test` completo (sin cambios de código backend esta corrida). `.env` temporal eliminado inmediatamente después del smoke check.
- **Fase y lote trabajados**: Fase B (`libros_capitulos`) sigue **bloqueada** por MR-0002. Se continuó **Fase I (frontend — trackBy)**, siguiendo la lista de candidatos de menor prioridad dejada en el cursor de la corrida #7.
- **Cambios aplicados** (6 componentes, solo se agregó `trackBy` a `*ngFor` existentes + su método `trackBy*`; sin cambios de DOM/estilos/comportamiento visible):
  - `dashboard/users/users.component.ts` + `.html` — nuevo método `trackByUser` (`user.id ?? user.username`), aplicado al `*ngFor` de `filteredUsers`.
  - `dashboard/book-editor/book-editor.component.ts` + `.html` — nuevos métodos `trackByAuthorId`, `trackByGenreId`, `trackByAvatarId`, aplicados a los `*ngFor`/`<option>` de `authorsList`, `genresList` y `avatars`. **NO** se tocó el `*ngFor` de `chapters`: tiene un `<input [(ngModel)]="chap.title">` de edición en vivo por fila, y los capítulos recién parseados de un EPUB nuevo no tienen `id` (se asigna recién al guardar en backend) ni un `order` garantizado como llave estable — un trackBy con llaves inestables/duplicadas (`undefined`) arriesgaba que Angular reutilizara el nodo DOM equivocado entre filas mientras el usuario escribe, causando pérdida de foco/cursor. Se dejó sin cambio, documentado en el STATE.
  - `dashboard/overview/overview.component.ts` + `.html` — nuevos métodos `trackByDate` (`day.date`) y `trackByBookTitle` (`book['edition__book__title']`), aplicados a `stats.sales_chart` y `stats.top_books`.
  - `catalog/author-detail-page/author-detail-page.component.ts` + `.html` — nuevos métodos `trackByBookSlug` y `trackByGenreName`, aplicados a `author.books` y `book.genres`.
  - `library/ai-chat/ai-chat.component.ts` + `.html` — nuevos métodos `trackByCharId` (`recentCharacters`) y `trackByTag` (`avatar.tags`). **NO** se tocó el `*ngFor` de `messages`: el servicio (`chat.service.ts`) hace `pop()` del mensaje temporal `isTyping` y `push()` de un objeto **nuevo** en su lugar (mismo índice, distinta referencia) para el efecto "escribiendo...". Sin trackBy, Angular compara por referencia y destruye/recrea ese nodo DOM (podría disparar una animación CSS de aparición de burbuja). Un trackBy por índice cambiaría eso a "reusar el nodo", alterando si esa animación se re-dispara — efecto no verificable sin correr la app, fuera de alcance de un cambio "sin efecto visual verificado". Se dejó sin cambio, documentado en el STATE.
  - `characters/demo-chat-page/demo-chat-page.component.ts` + `.html` — nuevos métodos `trackByTag` (`avatar.tags`) y `trackByMsgIndex` (`messages`, por índice). Este componente **sí** se pudo aplicar a `messages` porque su servicio local solo hace `push()` (sin `pop()`/reemplazo del último mensaje), por lo que un trackBy por índice se comporta igual que la comparación por referencia por defecto de Angular — sin riesgo de reutilización indebida de nodos.
- **Verificación (`ng build --configuration production`)**: build OK, 0 errores, mismos warnings preexistentes de CommonJS (`canvg`/`html2canvas`/`lottie-web`), sin warnings de budget. Bundle **Initial**: **1.34 MB raw / 274.87 kB transferencia** (antes, corrida #7: 274.82 kB; +0.05 kB por los nuevos métodos `trackBy*`, sin regresión real, muy por debajo del budget 1.6mb/2.5mb).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (1105 paquetes, sin errores esta vez). `ng build --configuration production` OK. `ng test --watch=false --browsers=ChromeHeadlessCustom` con `karma.conf.js` temporal fuera del repo (`CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, launcher custom `--no-sandbox --disable-gpu --disable-dev-shm-usage`, nada commiteado) -> **8 de 8 tests OK**.
- **Bundle**: Initial 1.34 MB (+0.05 kB de transferencia vs. corrida #7); ver detalle arriba.
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y los 6 componentes listados arriba).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T11:01:00Z — corrida horaria #9

- **Entorno**: repo OK (`git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin y local, creada a partir de `origin/Chris` (confirmado: `git merge-base --is-ancestor origin/Chris agent/nightly-optimization-chris` -> sí). Commit base de Chris: `3786d288` (sin cambios desde la corrida #8). Guarda de solapamiento: `run_in_progress` era `false` -> se continuó sin conflicto.
- **Push verificado**: OK (`git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> "Everything up-to-date", exit 0).
- **Smoke check**: backend — venv Python 3.12 (no versionado) + `.env` temporal (SQLite local, no versionado, borrado al terminar). `manage.py check` -> 0 issues. `manage.py makemigrations --check --dry-run` -> "No changes detected". Sin cambios de código backend esta corrida.
- **Fase y lote trabajados**: `I_frontend`, cursor -> se tomó el candidato dejado por la corrida #8 (`library/reader.component`), revisando primero en el `.ts` cómo se mutan los arrays antes de tocar el `.html`, para evitar el mismo riesgo ya documentado en `library/ai-chat` (mensaje "escribiendo..." reemplazado en el mismo índice).
- **Cambios aplicados** (2 archivos, 4 líneas de sustancia):
  1. `reader.component.ts` — se agregaron los métodos `trackByAvatarId(index, avatar)` (retorna `avatar?.id ?? index`) y `trackByChatMsgIndex(index)` (retorna `index`), junto a los `trackBy*` ya existentes (`trackByBlock`, `trackByToken`, `trackByChapter`).
  2. `reader.component.html` línea ~158 — `*ngFor="let avatar of characterAvatars"` -> se agregó `trackBy: trackByAvatarId`. Verificado en el `.ts`: `characterAvatars` es un getter (`this.avatars.filter(a => !a.is_author)`) que siempre reutiliza los mismos objetos `avatar` de `this.avatars`, así que es seguro trackear por `id`.
  3. `reader.component.html` línea ~294 — `*ngFor="let msg of chatMessages"` -> se agregó `trackBy: trackByChatMsgIndex`. Verificado en el `.ts` (`grep -n "chatMessages"` sobre todo el archivo): solo hay 4 puntos de mutación — asignación completa al cargar historial, `.push()` de mensaje optimista del usuario, `.push()` de la respuesta del asistente, y `.pop()` del último mensaje si la petición falla. No existe ningún patrón de "reemplazar el mensaje en el mismo índice por otro objeto" (a diferencia de `library/ai-chat`, donde sí existe ese patrón con el mensaje `isTyping` y por eso se dejó sin `trackBy` en la corrida #8) — por lo tanto `trackBy` por índice es seguro aquí, no se pierde foco ni se altera ninguna animación de aparición existente.
  - NO se tocó ningún DOM/clase/estilo/texto visible; solo se agregaron atributos `trackBy` a `*ngFor` ya existentes y sus métodos correspondientes.
  - NO se aplicó `trackBy` a `block.sentences` / `sentence.tokens` (ya tenían `trackByBlock`/`trackByToken` donde correspondía) ni a los `*ngFor` de `meaning`/`def` del modal de diccionario (listas pequeñas de bajo impacto; queda como candidato para un lote futuro).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (1105 paquetes). `ng build --configuration production` OK, 0 errores, mismos warnings preexistentes de CommonJS (canvg/html2canvas/jspdf/lottie-web), sin warnings de budget. `ng test --watch=false` con `karma.conf.local.js` temporal DENTRO de `frontend/` (necesario para que Node resuelva `karma-jasmine`/`karma-chrome-launcher` vía `node_modules`; `CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, launcher custom `--no-sandbox --disable-gpu --disable-dev-shm-usage`) -> **8 de 8 tests OK**. El archivo `karma.conf.local.js` se borró antes del commit; no quedó nada nuevo sin versionar.
- **Bundle**: Initial 1.34 MB raw / 274.79 kB transferencia (antes, corrida #8: 274.87 kB; leve baja, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `reader.component.ts` y `reader.component.html`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T11:59:53Z — corrida horaria #10

- **Entorno**: repo OK (`git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin y local. Commit base de Chris: `3786d288` (sin cambios desde la corrida #9; confirmado con `git merge-base agent/nightly-optimization-chris origin/Chris` == `git rev-parse origin/Chris`). Guarda de solapamiento: `heartbeat_utc` de la corrida #9 tenía ~54 min de antigüedad y `run_in_progress` ya era `false` -> se continuó sin conflicto.
- **Push verificado**: OK (`git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> "Everything up-to-date", exit 0).
- **Smoke check**: backend — venv Python 3.12 temporal (no versionado) + `.env` temporal (SQLite local, no versionado, borrado al terminar). `manage.py check` -> 0 issues. Sin cambios de código backend esta corrida.
- **Fase y lote trabajados**: `I_frontend`, cursor -> se tomó el candidato dejado por la corrida #9 (modal de diccionario integrado en `library/reader.component.html`, líneas 739 y 742).
- **Cambios aplicados** (2 archivos):
  1. `reader.component.ts` — se agregó el método `trackByDictIndex(index)` (retorna `index`), junto a los `trackBy*` ya existentes.
  2. `reader.component.html` líneas 739 y 742 — `*ngFor="let meaning of dictionaryResult.meanings"` y `*ngFor="let def of $any(meaning).definitions | slice:0:2"` -> se agregó `trackBy: trackByDictIndex` a ambos. Verificado en el `.ts`: `dictionaryResult` se pone en `null` al abrir el modal y luego se reemplaza por completo con `data[0]` (respuesta de la API) o con un objeto `{error}` nuevo — nunca se muta in-place — por lo que `trackBy` por índice es seguro, consistente con el patrón ya usado en `trackByChatMsgIndex`.
  - NO se tocó ningún DOM/clase/estilo/texto visible; solo se agregó 1 método `trackBy` y su uso en 2 `*ngFor` ya existentes.
  - NO se tocó `block.sentences` (sigue sin `trackBy` propio) ni se evaluó `ChangeDetectionStrategy.OnPush` esta corrida; quedan como candidatos para la próxima.
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (1105 paquetes). `ng build --configuration production` OK, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web), sin warnings de budget. `ng test --watch=false` con `karma.conf.local.js` temporal DENTRO de `frontend/` (`CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, launcher custom `--no-sandbox --disable-gpu --disable-dev-shm-usage`) -> **8 de 8 tests OK**. El archivo `karma.conf.local.js` se borró antes del commit; no quedó nada nuevo sin versionar.
- **Bundle**: Initial 1.34 MB raw / 274.95 kB transferencia (antes, corrida #9: 274.79 kB; variación de +0.16 kB por el código agregado, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `reader.component.ts` y `reader.component.html`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T13:02:36Z — corrida horaria #11
- **Entorno**: repo OK (`git rev-parse --show-toplevel` -> `.../LiteratusNovelistV2`, `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Rama `agent/nightly-optimization-chris` creada localmente desde `origin/agent/nightly-optimization-chris` (rama ya existente en remoto, con historial real de 10 corridas previas). Commit base confirmado desde **Chris**: `origin/Chris` = `3786d288` (igual al `base_commit` ya registrado en el STATE; sin commits nuevos en Chris desde la corrida #10). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #10 tenía ~63 min de antigüedad -> se continuó sin conflicto.
- **Push verificado**: OK (`git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> "Everything up-to-date", exit 0).
- **Smoke check**: backend — venv Python 3.12 temporal (no versionado, en el scratchpad de la sesión) + `.env` temporal (copiado de `.env.example`, `DATABASE_URL` apuntando a una SQLite temporal en el scratchpad, no versionada). `pip install -r requirements.txt` OK. `manage.py migrate --run-syncdb` OK. `manage.py check` -> **0 issues (0 silenced)**. `.env` y la SQLite temporal se borraron al terminar. Sin cambios de código backend esta corrida (no se corrió `makemigrations --check --dry-run` por no haber cambios de modelos).
- **Fase y lote trabajados**: `I_frontend` (frontend, Angular 17) — cursor: se tomó el candidato dejado por la corrida #10 ("retomar la evaluación de OnPush en los componentes que ya tienen trackBy") y se evaluó `LibraryListComponent` (el más pequeño y simple de la lista de candidatos).
- **Cambios aplicados** (1 archivo, 1 componente):
  1. `frontend/src/app/library/library-list/library-list.component.ts` — se agregó `changeDetection: ChangeDetectionStrategy.OnPush` al `@Component`, se inyectó `ChangeDetectorRef` (`private cdr = inject(ChangeDetectorRef)`) y se agregó `this.cdr.markForCheck()` al final de los callbacks `next` y `error` de `fetchInventory()`.
  - **Motivo del `markForCheck()`**: se leyó `library-list.component.html` completo y se confirmó que no usa `async` pipe ni eventos de template que disparen CD (`inventoryItems`/`isLoading`/`errorMsg` solo se leen en `*ngIf`/`*ngFor`/interpolación). Como esos campos se asignan dentro de un `.subscribe()` manual (no `async` pipe), con `OnPush` puro Angular no vuelve a chequear el componente cuando llega la respuesta del API — el `markForCheck()` reproduce exactamente el comportamiento visual anterior (sin él, el bug habría sido invisible en desarrollo por el tick global de zona.js, pero real en producción tras el primer render).
  - NO se tocó ningún DOM/clase/estilo/texto visible; NO se modificó `library-list.component.html`.
  - NO se tocaron `discover.component.ts` (328 líneas), `character-hub.component.ts` (140 líneas) ni `reader.component.ts` (2291 líneas) esta corrida — quedan como candidatos, requieren el mismo análisis línea por línea de callbacks async antes de aplicar OnPush con seguridad.
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (1105 paquetes). `ng build --configuration production` OK, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web). `ng test --watch=false` con `karma.conf.local.js` temporal DENTRO de `frontend/` (`CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, launcher custom `--no-sandbox --disable-gpu --disable-dev-shm-usage`) -> **8 de 8 tests OK** (no hay spec dedicado a `LibraryListComponent`, pero la suite completa sigue en verde). `karma.conf.local.js` se borró antes del commit; no quedó nada nuevo sin versionar.
- **Bundle**: Initial 1.34 MB raw / 274.79 kB transferencia (corrida #10: 274.95 kB; variación de -0.16 kB, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `library-list.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T13:59:13Z — corrida horaria #12
- **Entorno**: repo OK (`git rev-parse --show-toplevel` -> `.../LiteratusNovelistV2`, `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Rama `agent/nightly-optimization-chris` creada localmente desde `origin/agent/nightly-optimization-chris` (rama ya existente en remoto, con historial real de 11 corridas previas). Commit base confirmado desde **Chris**: `origin/Chris` = `3786d288` (igual al `base_commit` ya registrado en el STATE y a `git merge-base origin/Chris origin/agent/nightly-optimization-chris`; sin commits nuevos en Chris desde la corrida #11). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #11 tenía ~57 min de antigüedad -> se continuó sin conflicto.
- **Push verificado**: OK (`git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> "Everything up-to-date", exit 0).
- **Smoke check**: backend — venv Python 3.12 temporal (no versionado, en el scratchpad de la sesión) + `.env` temporal (SQLite local temporal, no versionada). `pip install -r requirements.txt` OK. `manage.py check` -> **0 issues (0 silenced)**. `.env` y la SQLite temporal se borraron al terminar. Sin cambios de código backend esta corrida (no se corrió `makemigrations --check --dry-run` por no haber cambios de modelos).
- **Fase y lote trabajados**: `I_frontend` (frontend, Angular 17) — cursor: se tomó el candidato dejado por la corrida #11 (`character-hub.component.ts`, 140 líneas, el más chico de los que quedaban para evaluar `OnPush`).
- **Cambios aplicados** (1 archivo, 1 componente):
  1. `frontend/src/app/characters/character-hub/character-hub.component.ts` — se agregó `changeDetection: ChangeDetectionStrategy.OnPush` al `@Component`, se inyectó `ChangeDetectorRef` (`private cdr = inject(ChangeDetectorRef)`) y se agregó `this.cdr.markForCheck()` en los 4 sitios donde el componente reasigna campos leídos en el template desde un callback asíncrono que NO pasa por un evento de template ni por `async` pipe: `loadAllData()` (`next`/`error` de `getGlobalAvatars` y `next` de `getRecentAvatars`), `onSearchChange()` (`next`/`error` de `getGlobalAvatars`) y, el más crítico, dentro del `subscribe()` de `interval(5000)` en `startHeroCarousel()` — sin `markForCheck()` ahí, el carrusel hero se habría congelado visualmente en producción tras el primer render, ya que reasigna `currentHeroIndex` cada 5s sin ningún evento de template involucrado.
  - Se leyó `character-hub.component.html` completo antes de tocar el `.ts`, confirmando que `isLoading`, `heroCharacters`, `currentHeroIndex`, `searchQuery`, `filteredCharacters`, `isSearching`, `recentCharacters`, `featuredCharacters` y `allCharacters` se leen en el template (`*ngIf`/`*ngFor`/interpolación) y todos son reasignaciones completas (nunca mutación in-place).
  - `setHeroIndex(idx)`, disparado por `(click)` en el propio template, dispara CD automático de `OnPush` (evento de la propia vista) y no necesitó `markForCheck()` adicional — se dejó intacto. `openChat()` solo hace `router.navigate` en sus callbacks, no reasigna nada leído en el template, tampoco necesitó `markForCheck()`.
  - NO se tocó ningún DOM/clase/estilo/texto visible; NO se modificó `character-hub.component.html`.
  - NO se tocaron `discover.component.ts` (328 líneas) ni `reader.component.ts` (2291 líneas) esta corrida — quedan como candidatos para próximas corridas.
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (1105 paquetes). `ng build --configuration production` OK, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web/jspdf). `ng test --watch=false` con `karma.conf.local.js` temporal DENTRO de `frontend/` (`CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, launcher custom `--no-sandbox --disable-gpu --disable-dev-shm-usage`) -> **8 de 8 tests OK** (no hay spec dedicado a `CharacterHubComponent`, pero la suite completa sigue en verde). `karma.conf.local.js` se borró antes del commit; no quedó nada nuevo sin versionar.
- **Bundle**: Initial 1.34 MB raw / 274.91 kB transferencia (corrida #11: 274.79 kB; variación de +0.12 kB, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `character-hub.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T15:05:00Z — corrida horaria #13
- **Entorno**: repo OK (`git rev-parse --show-toplevel` -> `.../LiteratusNovelistV2`, `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Rama `agent/nightly-optimization-chris` creada localmente desde `origin/agent/nightly-optimization-chris` (rama ya existente en remoto, con historial real de 12 corridas previas). Commit base confirmado desde **Chris**: `origin/Chris` = `3786d288` (igual al `base_commit` ya registrado en el STATE; `git merge-base --is-ancestor origin/Chris origin/agent/nightly-optimization-chris` confirma que Chris es ancestro; sin commits nuevos en Chris desde la corrida #12). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #12 tenía ~65 min de antigüedad -> se continuó sin conflicto.
- **Push verificado**: OK (`git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> "Everything up-to-date", exit 0).
- **Smoke check**: backend — venv Python 3.12 temporal (no versionado, en el scratchpad de la sesión) + `.env` temporal (SQLite local temporal, no versionada). `pip install -r requirements.txt` OK. `manage.py check` -> **0 issues (0 silenced)**. `.env` y la SQLite temporal se borraron al terminar. Sin cambios de código backend esta corrida (no se corrió `makemigrations --check --dry-run` por no haber cambios de modelos).
- **Fase y lote trabajados**: `I_frontend` (frontend, Angular 17) — cursor: se tomó el candidato dejado por la corrida #12 (`discover.component.ts`, 328 líneas).
- **Cambios aplicados** (1 archivo, 1 componente):
  1. `frontend/src/app/discover/discover.component.ts` — se agregó `changeDetection: ChangeDetectionStrategy.OnPush` al `@Component`, se inyectó `ChangeDetectorRef` (`private cdr = inject(ChangeDetectorRef)`) y se agregó `this.cdr.markForCheck()` en los sitios donde el componente reasigna campos leídos en el template desde un callback asíncrono que NO pasa por un evento de template ni por `async` pipe: `loadBooks()` (`next` al final, tras `isLoading=false`, y `error`), `setHeroBook()` (al final del método — cubre tanto la llamada síncrona dentro de `loadBooks().next` como la periódica desde `setInterval(() => this.setHeroBook(), 30000)`, que reasigna `heroBook` cada 30s sin ningún evento de template involucrado), y los 3 callbacks `onstart`/`onend`/`onerror` de `SpeechSynthesisUtterance` en `toggleAudio()` (reasignan `isPlaying`/`currentAudioBook` de forma asíncrona vía el motor TTS del navegador).
  - Se leyó `discover.component.html` completo antes de tocar el `.ts`, confirmando que `heroBook`, `isLoading`, `activeMood`, `trendingBooks`, `iaBooks`, `quickBooks`, `quoteBooks`, `matchPairs`, `allBooks`, `isPlaying`, `currentAudioBook` y `hoverBook` se leen en el template (`*ngIf`/`*ngFor`/interpolación/`[class]`/`[style]`).
  - `setMood()`, `saveForLater()` y `goToBook()` se disparan desde eventos de template (`click`) y son síncronos -> `OnPush` dispara CD automático de la propia vista, no necesitaron `markForCheck()` adicional. `hoverBook` se asigna directo en el template vía `(mouseenter)`/`(mouseleave)`, también cubierto por el evento de template.
  - Todos los `*ngFor` del template ya tenían `trackBy` (`trackByBook`, `trackByMatchPair`) desde antes de esta corrida; no se tocó eso.
  - NO se tocó ningún DOM/clase/estilo/texto visible; NO se modificó `discover.component.html`.
  - NO se tocó `reader.component.ts` (2291 líneas) esta corrida — queda como único candidato pendiente de evaluación línea por línea para `OnPush` en la lista original de la corrida #9; `library-list.component.ts` (#11), `character-hub.component.ts` (#12) y `discover.component.ts` (#13) ya quedaron con `OnPush` aplicado y verificado.
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (`node_modules` no estaba presente al iniciar la corrida; 157 paquetes reportados por npm al terminar). `ng build --configuration production` OK, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web/jspdf). `ng test --watch=false` con `karma.conf.local.js` temporal DENTRO de `frontend/` (`CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, launcher custom `--no-sandbox --disable-gpu --disable-dev-shm-usage`) -> **8 de 8 tests OK** (no hay spec dedicado a `DiscoverComponent`, pero la suite completa sigue en verde). `karma.conf.local.js` se borró antes del commit; no quedó nada nuevo sin versionar.
- **Bundle**: Initial 1.34 MB raw / 274.91 kB transferencia (corrida #12: 274.91 kB; sin variación medible, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `discover.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T16:05:00Z — corrida horaria #14
- **Entorno**: repo OK (`git rev-parse --show-toplevel` -> `.../LiteratusNovelistV2`, `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Rama `agent/nightly-optimization-chris` creada localmente desde `origin/agent/nightly-optimization-chris` (rama ya existente en remoto, con historial real de 13 corridas previas). Commit base confirmado desde **Chris**: `origin/Chris` = `3786d288` (igual al `base_commit` ya registrado en el STATE; `git merge-base --is-ancestor origin/Chris HEAD` confirma que Chris es ancestro; sin commits nuevos en Chris desde la corrida #13). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #13 tenía ~49 min de antigüedad -> se continuó sin conflicto.
- **Push verificado**: OK (`git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> "Everything up-to-date", exit 0).
- **Smoke check**: backend — venv Python 3.12 temporal (no versionado, en el scratchpad de la sesión) + `.env` temporal (SQLite local temporal, no versionada). `pip install -r requirements.txt` OK. `manage.py check` -> **0 issues (0 silenced)**. `.env` y la SQLite temporal se borraron al terminar. Sin cambios de código backend esta corrida (no se corrió `makemigrations --check --dry-run` por no haber cambios de modelos).
- **Fase y lote trabajados**: `I_frontend` (frontend, Angular 17) — cursor: se tomó el candidato dejado por la corrida #13 (`reader.component.ts`, 2291 líneas), pero acotado solo a la parte de bajo riesgo: cobertura de `trackBy` en los `*ngFor` de `block.sentences` que aún no lo tenían. Se leyó primero todo `reader.component.ts` buscando dónde se construye/reasigna `block.sentences`, para descartar el mismo riesgo de reemplazo in-place ya documentado en corridas anteriores para otros componentes.
- **Cambios aplicados** (2 archivos, 1 componente):
  1. `reader.component.ts` — se agregó el método `trackBySentence(index, sentence)` (retorna `sentence?.idx ?? index`), junto a los `trackBy*` ya existentes (`trackByBlock`, `trackByToken`, `trackByChapter`, etc.). Verificado con `grep` sobre todo el archivo: `block.sentences` se asigna **una sola vez** por bloque (líneas ~1009-1036, función de parseo del capítulo) usando un contador global `globalSentenceIdx` que asigna un `idx` único y estable a cada oración de todo el capítulo, y `block.sentences` nunca vuelve a reasignarse en ningún otro punto del archivo -> `sentence.idx` es un identificador estable y seguro para `trackBy`.
  2. `reader.component.html` líneas 619, 641 y 663 — los 3 `*ngFor="let sentence of block.sentences"` (los 3 modos de bloque `h1`/`h2`/`p` que iteran las oraciones de un bloque para el resaltado tipo ReadEra durante lectura asistida/TTS) -> se agregó `trackBy: trackBySentence` a los 3.
  - NO se tocó `ChangeDetectionStrategy` en este componente esta corrida: es el candidato más grande y riesgoso (2291 líneas, múltiples `@HostListener` de selección de texto, integración con `AssistedReadingService`, resaltado palabra-por-palabra de TTS) y evaluar línea por línea todos sus call-sites asíncronos no cabía con margen de seguridad en el tiempo de un lote (~25 min) junto con las pruebas — queda como candidato dedicado para una corrida futura.
  - NO se tocó ningún DOM/clase/estilo/texto visible; solo se agregó 1 método `trackBy` y su uso en 3 `*ngFor` ya existentes.
  - Quedan sin `trackBy` 3 `*ngFor` de listas de voces (`audioService.voices` línea 429, `kokoroVoice.kokoroVoices` línea 444, `wasmVoices` línea 474) — listas pequeñas y estáticas, impacto marginal, candidato de baja prioridad para un lote futuro.
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (1105 paquetes; `node_modules` no estaba presente al iniciar la corrida). `ng build --configuration production` OK, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web). `ng test --watch=false` con `karma.conf.local.js` temporal DENTRO de `frontend/` (`CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, launcher custom `--no-sandbox --disable-gpu --disable-dev-shm-usage`) -> **8 de 8 tests OK** (no hay spec dedicado a `ReaderComponent`, pero la suite completa sigue en verde). `karma.conf.local.js` se borró antes del commit; no quedó nada nuevo sin versionar.
- **Bundle**: Initial 1.34 MB raw / 274.84 kB transferencia (corrida #13: 274.91 kB; leve baja de 0.07 kB, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `reader.component.ts` y `reader.component.html`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T17:05:00Z — corrida horaria #15
- **Entorno**: repo OK (`git rev-parse --show-toplevel` -> `.../LiteratusNovelistV2`, `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Rama `agent/nightly-optimization-chris` creada localmente desde `origin/agent/nightly-optimization-chris` (rama ya existente en remoto, con historial real de 14 corridas previas). Commit base confirmado desde **Chris**: `origin/Chris` = `3786d288` (igual al `base_commit` ya registrado en el STATE; `git merge-base --is-ancestor origin/Chris HEAD` confirma que Chris es ancestro; sin commits nuevos en Chris desde la corrida #14). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #14 tenía ~50 min de antigüedad -> se continuó sin conflicto.
- **Push verificado**: OK (`git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> "Everything up-to-date", exit 0).
- **Smoke check**: backend — venv Python 3.12 temporal (no versionado, en el scratchpad de la sesión; el entorno base trae Python 3.11 por defecto en `python3`, pero `python3.12` está disponible explícitamente y es el que exige `Django==6.0.4`). `pip install -r requirements.txt` OK. `.env` temporal (SQLite local temporal + `SECRET_KEY` temporal, no versionados). `manage.py check` -> **0 issues (0 silenced)**. `.env` y la SQLite temporal se borraron al terminar. Sin cambios de código backend esta corrida (no se corrió `makemigrations --check --dry-run` por no haber cambios de modelos).
- **Fase y lote trabajados**: `I_frontend` (frontend, Angular 17) — cursor: se tomó el candidato de bajo riesgo dejado por la corrida #14 (3 `*ngFor` de selectores de voz en `reader.component.html` sin `trackBy`: líneas 429, 444 y 474).
- **Cambios aplicados** (2 archivos):
  1. `reader.component.ts` — se agregaron 2 métodos nuevos junto a los `trackBy*` existentes: `trackByNativeVoice(index, voice)` (retorna `` `${voice.name}|${voice.lang}` ``) y `trackByVoiceId(index, voice)` (retorna `voice?.id ?? index`).
  2. `reader.component.html` líneas 429, 444 y 474 — se agregó `trackBy: trackByNativeVoice` al `*ngFor="let v of audioService.voices"` (voz nativa del navegador) y `trackBy: trackByVoiceId` a los `*ngFor` de `kokoroVoice.kokoroVoices` y `wasmVoices` (voz Kokoro y voz Wasm/Piper).
  - **Justificación de la clave elegida**: se leyó `audio.service.ts` (`audioService.voices`) y se confirmó que la lista se **reasigna por completo** en `loadVoices()` (llamada en el constructor y en cada evento `window.speechSynthesis.onvoiceschanged` del navegador) con ítems `{name, lang, voice}` que no traen `id` propio (vienen de `SpeechSynthesisVoice`) -> se usó `name+lang` como clave de contenido estable entre recargas, más segura que el índice puro porque el orden de voces que entrega el sistema operativo puede variar entre disparos de `onvoiceschanged`. Se leyó `kokoro-tts.service.ts` y se confirmó que `kokoroVoices` es un getter que alterna entre `remoteVoices`/`localVoices`, dos arrays **estáticos declarados una sola vez** con `id` fijo (`ef_dora`, `em_alex`, etc.), nunca mutados -> `id` es una clave segura. `wasmVoices` en `reader.component.ts` (línea 154) es también un array literal estático con `id` fijo, nunca reasignado -> misma clave, reutilizando el mismo método `trackByVoiceId` para ambos casos en vez de duplicar un método trivial.
  - NO se tocó el binding `[value]="i"` del selector de voz nativa (sigue siendo índice; es el contrato existente con `audioService.setVoiceByIndex(+valor)`), ni ningún otro atributo, clase o texto visible.
  - NO se tocó `ChangeDetectionStrategy` en `reader.component.ts` esta corrida — sigue siendo el candidato más grande y riesgoso (2291 líneas, múltiples `@HostListener` de selección de texto, integración con `AssistedReadingService`, resaltado TTS palabra-por-palabra vía `setInterval`/`requestAnimationFrame`) y evaluar línea por línea todos sus call-sites asíncronos no cabía con margen de seguridad junto con las pruebas en un lote de ~25 min. Queda como candidato dedicado (sin otras tareas en el mismo lote) para una corrida futura.
  - Con esto, todos los `*ngFor` de `reader.component.html` revisados hasta ahora (avatares, mensajes de chat, capítulos, voces nativa/Kokoro/Wasm, tokens de título, bloques, oraciones, tokens de oración, resultados de diccionario) ya tienen `trackBy`.
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (1105 paquetes; `node_modules` no estaba presente al iniciar la corrida). `ng build --configuration production` OK, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web). `ng test --watch=false` con `karma.conf.local.js` temporal DENTRO de `frontend/` (`CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, launcher custom `--no-sandbox --disable-gpu --disable-dev-shm-usage`) -> **8 de 8 tests OK**. `karma.conf.local.js` se borró antes del commit; no quedó nada nuevo sin versionar.
- **Bundle**: Initial 1.34 MB raw / 275.00 kB transferencia (corrida #14: 274.84 kB; variación de +0.16 kB por el código agregado, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `reader.component.ts` y `reader.component.html`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T18:03:06Z — corrida horaria #16

- **Entorno**: repo OK (`git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Guarda de solapamiento: `NIGHT_AUDIT_STATE.json` remoto tenía `run_in_progress: false` y `heartbeat_utc` de ~52 min antes -> sin corrida viva, se continúa normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #15), confirmado que viene de `origin/Chris` (`origin/Chris` no se movió desde entonces).
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` desde una rama distinta dio `[rejected] non-fast-forward` (esperado, confirma que las credenciales SÍ tienen permiso de escritura, no es un error de autenticación); tras hacer `git checkout agent/nightly-optimization-chris` + `git pull --ff-only`, el mismo dry-run dio `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: `python3.12 -m venv` temporal + `pip install -r requirements.txt` OK + `.env` temporal (sqlite local + SECRET_KEY temporal) + `python -X utf8 manage.py check` -> **0 issues (0 silenced)**. Sin cambios de modelos esta corrida, no se corrió `makemigrations --check --dry-run`.
- **Fase y lote trabajados**: Fase I (frontend) cursor `corrida #14/#15 (reader.component.html cerrado)` -> se completó el barrido de `*ngFor` sin `trackBy` en 7 plantillas más pequeñas que quedaban pendientes: `payment-failure`, `payment-success`, `tavern`, `profile` (x2), `assisted-reading-panel` (x2), `author-list`, `book-list`.
- **Cambios aplicados** (9 `*ngFor` en 7 componentes, 14 archivos tocados):
  - `catalog/payment-failure/payment-failure.component.html:15` + `.ts` — partículas decorativas (`[1..8]` literal) — se agregó `trackByIndex`.
  - `catalog/payment-success/payment-success.component.html:17` + `.ts` — partículas decorativas (`[1..10]` literal) — se agregó `trackByIndex`.
  - `library/tavern/tavern.component.html:115` + `.ts` — `chests` (array estático de 3 objetos; el template usa `let i = index` para `chest-featured`/`chest-master`, así que se usó `trackByIndex` para no alterar esa semántica posicional) — se agregó `trackByIndex`.
  - `users/profile/profile.component.html:28,40` + `.ts` — `availableColors` (array de strings hex) -> `trackByColor` (identidad = el color mismo); `availableThemes` (array de `{id,label}` estático) -> `trackByThemeId` (identidad = `theme.id`).
  - `library/reader/assisted-reading-panel/assisted-reading-panel.component.html:61,104` + `.ts` — `speeds` (`[0.5,0.75,1,1.25,1.5,2]` literal) -> `trackByIndex`; `colors` (array de `{id,name,preview}` estático) -> `trackByColorId`.
  - `catalog/author-list/author-list.component.html:50` + `.ts` — `shimmer-card` de loading (`[1,2,3,4]` literal) -> `trackByIndex`, reutilizando el mismo patrón/nombre ya usado en `book-detail-page.component.ts`.
  - `catalog/book-list/book-list.component.html:54` + `.ts` — `skeleton-card` de loading (`[1,2,3,4,5,6]` literal) -> `trackByIndex`.
  - Todos los arrays iterados son estáticos (declarados una sola vez, nunca reasignados/mutados en runtime); ningún DOM/clase/estilo/texto visible fue alterado, solo se agregó el atributo `trackBy: <método>` dentro de las directivas `*ngFor` ya existentes. Autocorrección durante la corrida: al editar `book-list.component.html` se omitió por error el `>` de cierre del `<div>` en el primer intento (detectado inmediatamente por `NG5002` en `ng build`); se corrigió antes de seguir.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por MR-0002, sin BD real).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (sin cambios de código backend, no se corrió la suite completa). Frontend — `npm install` OK (`node_modules` no estaba presente al iniciar la corrida). `ng build --configuration production` OK, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web). `ng test --watch=false` con `karma.conf.local.js` temporal DENTRO de `frontend/` (`CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, launcher custom `--no-sandbox --disable-gpu --disable-dev-shm-usage`) -> **8 de 8 tests OK**. `karma.conf.local.js` y `coverage/` se borraron antes del commit; no quedó nada nuevo sin versionar.
- **Bundle**: Initial 1.34 MB raw / 275.03 kB transferencia (corrida #15: 275.00 kB; variación de +0.03 kB, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y los 12 archivos de componentes listados arriba).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T18:59:44Z — corrida horaria #17
- **Entorno**: repo OK (`git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Guarda de solapamiento: `NIGHT_AUDIT_STATE.json` tenía `run_in_progress: false` y `heartbeat_utc` de ~55 min antes -> sin corrida viva, se continúa normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #16), confirmado que viene de `origin/Chris` vía `git merge-base agent/nightly-optimization-chris origin/Chris`. Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); checkout directo + `git pull --ff-only origin agent/nightly-optimization-chris` -> already up to date.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: no se corrió `manage.py check` esta corrida (no se tocó código/datos de backend; el lote de 25 min se usó completo en Fase I frontend). MR-0002 documenta que Fases B-H siguen bloqueadas sin dump de BD real de producción.
- **Fase y lote trabajados**: Fase I (frontend), cursor `corrida #16 (7 plantillas pequeñas cerradas)` -> se cerraron los 8 `*ngFor` sin `trackBy` que quedaban en 3 plantillas: `character-hub.component.html` (4 de 8), `dashboard/authors.component.html` (2 de 3), `discover.component.html` (2 de 7).
- **Cambios aplicados** (8 `*ngFor` en 3 componentes, 6 archivos tocados):
  - `characters/character-hub/character-hub.component.html:6,39,121,129` + `.ts` — `heroCharacters` (carrusel hero) -> se reutilizó `trackByChar` ya existente; `heroCharacters` (indicadores del carrusel, iterado una segunda vez por índice) -> `trackByIndex` nuevo; `char.tags` (badges dentro de tarjeta destacada) -> `trackByIndex`; `skeletonArray` (`Array(6).fill(0)`, loading) -> `trackByIndex`.
  - `dashboard/authors/authors.component.html:132,139` + `.ts` — `previewAuthor.themes.split(',')` (recalculado cada vez que se abre la vista previa de un autor) -> `trackByIndex` nuevo; `previewAuthor.books` (reasignado en bloque al cambiar de autor, sin id estable visible en el template) -> `trackByIndex` nuevo.
  - `discover/discover.component.html:31,92` + `.ts` — `heroBook.tags | slice:0:3` -> `trackByIndex` nuevo; `moods` (array literal estático declarado una sola vez en el `.ts`, con campo `id` estable) -> `trackByMoodId` nuevo (identidad = `mood.id`).
  - Todos los arrays iterados son estáticos o se reasignan en bloque (nunca se mutan in-place); ningún DOM/clase/estilo/texto visible fue alterado, solo se agregó el atributo `trackBy: <método>` dentro de las directivas `*ngFor` ya existentes, más el método `trackBy` correspondiente en cada `.ts`.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por MR-0002, sin BD real).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — omitidas esta corrida (sin cambios de código/datos backend). Frontend — `npm install` OK (`node_modules` no estaba presente al iniciar la corrida). `ng build --configuration production` OK, exit code 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, ya documentados en corridas previas). `ng test --watch=false --browsers=ChromeHeadlessNoSandbox` con `CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` (launcher `--no-sandbox` ya definido por el builder de Angular, sin necesidad de `karma.conf.local.js` propio) -> **8 de 8 tests OK**. No quedó nada nuevo sin versionar.
- **Bundle**: Initial 1.34 MB raw / 275.02 kB transferencia (corrida #16: 275.03 kB; variación de -0.01 kB, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y los 6 archivos de componentes listados arriba).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T20:01:56Z — corrida horaria #18

- **Entorno**: repo OK (`git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Guarda de solapamiento: `NIGHT_AUDIT_STATE.json` tenía `run_in_progress: false` y `heartbeat_utc` de la corrida #17 con ~55 min de antigüedad -> sin corrida viva, se continúa normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #17), confirmado que viene de `origin/Chris`. Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); checkout directo + `git pull --ff-only origin agent/nightly-optimization-chris` -> already up to date.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: venv Python 3.12 temporal (scratchpad de la sesión, no versionado) + `SECRET_KEY`/`DEBUG`/`DATABASE_URL` (sqlite temporal) temporales + `pip install -r requirements.txt` OK + `python -X utf8 manage.py check` -> **0 issues (0 silenced)**. Sin cambios de modelos esta corrida, no se corrió `makemigrations --check --dry-run`. Venv y sqlite temporales borrados al terminar.
- **Fase y lote trabajados**: Fase I (frontend), cursor `corrida #17 (home/ai-chat/book-editor pendientes)` -> se cerraron los 9 `*ngFor` sin `trackBy` que quedaban en los 3 componentes restantes: `ai-chat.component.html` (1 de 3), `book-editor.component.html` (1 de 4), `home.component.html` (7 de 11).
- **Cambios aplicados** (9 `*ngFor` en 3 componentes, 6 archivos tocados):
  1. `library/ai-chat/ai-chat.component.html:67` + `.ts` — `*ngFor="let msg of messages"` (historial de chat). Se leyó `chat.service.ts`: `ChatMessage.id` es opcional y **nunca se asigna** en los mensajes creados localmente por `sendMessage()` (mensaje de usuario, mensaje temporal "typing", ni el mensaje final de respuesta — solo llevan `role`/`content`/`created_at`/`isTyping`), así que `id` no es una clave confiable. La lista solo crece por `push`/spread (append), y el único caso de mutación es un `pop()` del último mensaje temporal al llegar la respuesta real (siempre en la misma posición final) -> se agregó `trackByMsgIndex` nuevo (retorna el índice), seguro porque nunca hay reordenamiento.
  2. `dashboard/book-editor/book-editor.component.html:145` + `.ts` — `*ngFor="let chap of chapters; let i = index"` (lista editable de capítulos, con `let i = index` ya usado en `openChapterEditor(i)`/`removeChapter(i)`). Se confirmó que `i` se sigue recalculando por Angular en cada pasada de forma independiente del `trackBy` (no se ve afectado), y que `chapters[].order` se reasigna secuencialmente tras cada `removeChapter()` vía `this.chapters.forEach((c,i) => c.order = i+1)`, o sea que índice y orden ya están sincronizados; no hay `id` estable expuesto en el template -> se agregó `trackByIndex` nuevo.
  3. `home/home.component.html` — 7 `*ngFor` en 4 secciones:
     - línea 8: partículas decorativas del hero (`[1,2,3,4,5,6,7,8]` literal estático) -> `trackByIndex` nuevo.
     - línea 126: skeleton de avatares (`[1,2,3,4,5,6]` literal estático) -> `trackByIndex`.
     - línea 170: skeleton de trending (`[1,2,3,4]` literal estático) -> `trackByIndex`.
     - línea 193: `featuredWithCharacters` (con `let i = index` ya usado solo para el `(click)`, no afecta al `trackBy`) — array reasignado completo en cada carga vía `.map()` con campo `slug` estable (misma forma que `Book.slug`) -> se **reutilizó** `trackBySlug` ya existente en el componente, sin duplicar lógica.
     - líneas 279, 303, 382: `book.tags.slice(0,2)` / `book.tags?.slice(0,1)` (3 ocurrencias dentro de `trending-card`/`recommended-card`, cada `*ngFor` padre ya tenía `trackBySlug` por libro) — `Book.tags` tiene campo `slug` propio y estable en la interfaz (`{ name: string; slug: string }[]`) -> se agregó `trackByTagSlug` nuevo (retorna `tag.slug`), reutilizado en las 3 ocurrencias.
  - Todos los arrays iterados son estáticos, se reasignan en bloque, o crecen solo por append; ningún DOM/clase/estilo/texto visible fue alterado — único cambio de marcado fue agregar `trackBy: <método>` dentro de las directivas `*ngFor` ya existentes (preservando `let i = index` donde ya se usaba para otros fines), más los métodos `trackBy` correspondientes en cada `.ts`.
  - Con esta corrida se **completa el barrido de `*ngFor` sin `trackBy` en toda la Fase I** iniciada varias corridas atrás (character-hub, authors, discover, ai-chat, book-editor, home, reader, payment-failure/success, tavern, profile, assisted-reading-panel, author-list, book-list, library-list — todos ya cubiertos).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por MR-0002, sin BD real).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (0 issues). Frontend — `npm install` OK (`node_modules` no estaba presente al iniciar la corrida; 1105 paquetes). `ng build --configuration production` OK, exit code 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, ya documentados en corridas previas). `ng test --watch=false --browsers=ChromeHeadlessNoSandbox` con `CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` -> **8 de 8 tests OK**. No quedó nada temporal sin versionar.
- **Bundle**: Initial 1.34 MB raw / 275.05 kB transferencia (corrida #17: 275.02 kB; variación de +0.03 kB por el código `trackBy` agregado, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y los 6 archivos de componentes listados arriba).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T21:05:00Z — corrida horaria #19

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar. `git fetch origin` OK (Chris, Felipe, eithan, agent/nightly-optimization, agent/nightly-optimization-chris, main). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #18 tenía ~52 min de antigüedad -> sin corrida viva, se continuó normal. Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin; `git checkout` + `git pull --ff-only` -> already up to date.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado ancestro común con `origin/Chris` (mismo commit base que corridas anteriores, sin cambios nuevos en `Chris`).
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado OK**.
- **Smoke check (Fase A)**: venv Python 3.12.3 temporal (scratchpad de la sesión, no versionado) + `pip install -r requirements.txt` OK + variables de entorno temporales (`SECRET_KEY`, `DEBUG`, `DATABASE_URL` sqlite temporal) -> `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. `manage.py makemigrations --check --dry-run` -> `No changes detected`. Venv y sqlite temporales borrados al terminar.
- **Fase y lote trabajados**: Fase I (frontend) — cursor: "trackBy cerrado (corrida #18)" -> "OnPush en 3 componentes pequeños de dashboard (users, avatars, books)".
- **Cambios aplicados**:
  - `dashboard/users/users.component.ts` — agregado `ChangeDetectionStrategy.OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en los callbacks `next`/`error` de `loadUsers()` (única mutación de estado fuera de un evento de template).
  - `dashboard/avatars/avatars.component.ts` — mismo patrón en `loadAvatars()`.
  - `dashboard/books/books.component.ts` — mismo patrón en `loadBooks()` (next/error) y además en el `next` de `deleteBook()` (la respuesta HTTP de borrado llega en una tarea async separada del click que la originó).
  - Se revisaron los 3 templates `.html` antes de tocar el `.ts`: sin async pipe, sin `@Input()` de un padre, mutaciones de estado solo dentro de `subscribe()` (ahora con `markForCheck`) o dentro de handlers `(input)`/`(click)` del propio template (que ya disparan CD por sí solos bajo OnPush). Ningún binding, clase, estilo o texto visible fue modificado.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK, `makemigrations --check --dry-run` OK. Frontend — `npm install` OK (`node_modules` no estaba presente al iniciar la corrida; 1105 paquetes). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web). `npx ng test --watch=false --browsers=ChromeHeadlessNoSandbox` con `CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` -> **8 de 8 tests OK**. No quedó nada temporal sin versionar (solo los 3 componentes + STATE + LOG modificados).
- **Bundle**: Initial 1.34 MB raw / 274.96 kB transferencia (corrida #18: 275.05 kB; sin regresión, dentro del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y los 3 componentes listados arriba).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T22:05:00Z — corrida horaria #20

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar. `git fetch origin` OK (Chris, Felipe, eithan, agent/nightly-optimization, agent/nightly-optimization-chris, main). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #19 tenía ~50 min de antigüedad -> sin corrida viva, se continuó normal. Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin; `git checkout` + `git pull --ff-only` -> already up to date.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado ancestro común con `origin/Chris` vía `git merge-base` (sin cambios nuevos en `Chris` desde corridas anteriores).
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado OK**. Nota: el "antecedente" incluido en el prompt de la rutina (que ~30 corridas nunca llegaron a GitHub) está desactualizado — el remoto ya tiene 19 commits previos de corridas horarias exitosas en esta rama, todos con push confirmado por `git ls-remote`.
- **Smoke check (Fase A)**: venv Python 3.12.3 temporal (scratchpad de la sesión, no versionado) + `pip install -r requirements.txt` OK + variables de entorno temporales (`SECRET_KEY`, `DEBUG`, `DATABASE_URL` sqlite temporal) -> `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. `manage.py makemigrations --check --dry-run` -> `No changes detected`. Venv y sqlite temporales borrados al terminar.
- **Fase y lote trabajados**: Fase I (frontend) — cursor: "OnPush en payment-failure/payment-success (triviales) y authors.component.ts (mediano)" (candidatos dejados anotados por la corrida #19).
- **Cambios aplicados** (3 componentes):
  1. `catalog/payment-failure/payment-failure.component.ts` — `ChangeDetectionStrategy.OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en el callback de `route.queryParamMap.subscribe()` (única mutación de estado async, asigna `this.reason`).
  2. `catalog/payment-success/payment-success.component.ts` — mismo patrón: `markForCheck()` en `route.queryParamMap.subscribe()` (asigna `this.buyOrder`). `chatService.loadInitialInk()` no muta estado local del componente ni el template lo referencia, no requirió `markForCheck`.
  3. `dashboard/authors/authors.component.ts` (144 líneas) — `ChangeDetectionStrategy.OnPush` + `ChangeDetectorRef` inyectado por constructor. `markForCheck()` agregado en: `loadAuthors()` (next y error), `openPreview()` (next y error), `saveAuthor()` (solo en el branch `error`; el branch `next` ya delega a `loadAuthors()`, que cubre su propio `markForCheck`), y `onPhotoSelected()` (callback `reader.onload` de `FileReader`, que genera el preview de la foto de forma asíncrona nativa). `deleteAuthor()` no requirió cambio directo (su `subscribe` solo llama a `loadAuthors()`, que ya hace `markForCheck`). `editAuthor()` y `resetForm()` se disparan directo desde `(click)` del propio template -> CD ya se dispara sola bajo OnPush, sin necesidad de `markForCheck`.
  - Se revisaron los 3 templates `.html` antes de tocar los `.ts`: ninguno usa `async` pipe, ninguno tiene `@Input()` desde un componente padre. Ningún binding, clase, estilo o texto visible fue modificado — único cambio fue la metadata `changeDetection: ChangeDetectionStrategy.OnPush` en `@Component`, la inyección de `ChangeDetectorRef` y las llamadas a `markForCheck()` dentro de callbacks asíncronos ya existentes.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (sin cambios de código/datos backend, no se corrió la suite completa). Frontend — `npm install` OK (`node_modules` no estaba presente al iniciar la corrida; 1105 paquetes). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web). `npx ng test --watch=false --browsers=ChromeHeadlessNoSandbox` con `CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` -> **8 de 8 tests OK**. No quedó nada temporal sin versionar (solo los 3 componentes + STATE + LOG modificados).
- **Bundle**: Initial 1.34 MB raw / 275.02 kB transferencia (corrida #19: 274.96 kB; variación de +0.06 kB por el código OnPush/markForCheck agregado, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y los 3 componentes listados arriba).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-03T23:10:00Z — corrida horaria #21

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar. `git fetch origin` OK (Chris, Felipe, eithan, agent/nightly-optimization, agent/nightly-optimization-chris, main). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #20 tenía ~49 min de antigüedad -> sin corrida viva, se continuó normal. Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin; `git checkout` + `git pull --ff-only` -> already up to date.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado ancestro común con `origin/Chris` vía `git merge-base` (sin cambios nuevos en `Chris` desde corridas anteriores).
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado OK**.
- **Smoke check (Fase A)**: venv Python 3.12 temporal (scratchpad de la sesión, no versionado) + `pip install -r requirements.txt` OK + variables de entorno temporales (`SECRET_KEY`, `DEBUG`, `DATABASE_URL` sqlite temporal) -> `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. Sin cambios de modelos esta corrida, no se corrió `makemigrations --check --dry-run`. Venv y sqlite temporales borrados al terminar.
- **Fase y lote trabajados**: Fase I (frontend) — cursor: "OnPush en payment-failure/payment-success/authors (corrida #20)" -> "OnPush en catalog/author-list y catalog/book-list (medianos, candidatos dejados por la corrida #20)".
- **Cambios aplicados** (2 componentes):
  1. `catalog/author-list/author-list.component.ts` — `ChangeDetectionStrategy.OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en `fetchAuthors()` (next y error), única mutación async del componente. Se detectó que `togglePhotoFilter()` y el campo `onlyWithPhoto` existen en el `.ts` pero no están cableados a ningún control del template (código muerto preexistente) — no se tocó por quedar fuera del alcance de esta corrección puntual de OnPush.
  2. `catalog/book-list/book-list.component.ts` — `ChangeDetectionStrategy.OnPush` + `ChangeDetectorRef` inyectado. `fetchCategories()` -> `markForCheck()` solo en el branch `next` (el `error` solo hace `console.error`, no muta estado ligado al template). `fetchBooks()` -> `markForCheck()` en `next` y `error` (única fuente que muta `books`/`totalCount`/`isLoading`/`errorMsg`). El `@Input() isHome` del componente no requirió cambios: Angular vuelve a chequear un componente OnPush cuando cambia la referencia de un `@Input()`, sin necesidad de `markForCheck` manual.
  - Se revisaron ambos templates `.html` antes de tocar los `.ts`: ninguno usa `async` pipe. Ningún binding, clase, estilo o texto visible fue modificado — único cambio fue la metadata `changeDetection: ChangeDetectionStrategy.OnPush` en `@Component`, la inyección de `ChangeDetectorRef` y las llamadas a `markForCheck()` dentro de callbacks asíncronos ya existentes.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (0 issues; sin cambios de código/datos backend, no se corrió la suite completa ni `makemigrations`). Frontend — `npm install` OK (`node_modules` no estaba presente al iniciar la corrida; 1105 paquetes). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web). `npx ng test --watch=false --browsers=ChromeHeadlessNoSandbox` con `CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` -> **8 de 8 tests OK**. No quedó nada temporal sin versionar (solo los 2 componentes + STATE + LOG modificados).
- **Bundle**: Initial 1.34 MB raw / 274.85 kB transferencia (corrida #20: 275.02 kB; variación de -0.17 kB, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y los 2 componentes listados arriba).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T00:05:00Z — corrida horaria #22

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar. `git fetch origin --prune` OK (Chris, Felipe, eithan, agent/nightly-optimization, agent/nightly-optimization-chris, main; nota: en esta sesión el fetch inicial sin flags solo trajo `main`/`claude/admiring-noether-adr2gs`, hizo falta `--prune` para traer el resto de ramas remotas — anotado por si vuelve a pasar en corridas futuras). Guarda de solapamiento: `NIGHT_AUDIT_STATE.json` tenía `run_in_progress: false` y `heartbeat_utc` de la corrida #21 con ~45 min de antigüedad -> sin corrida viva, se continúa normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #21), confirmado idéntico a `origin/Chris` vía `git merge-base` (Chris no se movió). Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); checkout directo + `git pull --ff-only origin agent/nightly-optimization-chris` -> already up to date.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: venv Python 3.12 temporal (scratchpad de la sesión, no versionado) + `pip install -r requirements.txt` OK + variables de entorno temporales (`SECRET_KEY`, `DEBUG`, `DATABASE_URL` sqlite temporal) -> `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. `manage.py makemigrations --check --dry-run` -> `No changes detected`. Venv y sqlite temporales borrados al terminar.
- **Fase y lote trabajados**: Fase I (frontend) — cursor: "OnPush en catalog/author-list y catalog/book-list (corrida #21)" -> "OnPush en 4 componentes pequeños/medianos sin evaluar: dashboard-layout, overview, author-editor, category-detail".
- **Cambios aplicados** (4 componentes):
  1. `dashboard/dashboard-layout/dashboard-layout.component.ts` — `ChangeDetectionStrategy.OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en los 2 `subscribe()` de `ngOnInit()`: `router.events` filtrado a `NavigationEnd` (muta `pageTitle`/`mobileSidebarOpen`) y `settingsService.currentTheme$` (muta `currentTheme`). No se tocó la falta de `unsubscribe` de ambas suscripciones (cambio de ciclo de vida más riesgoso, fuera del alcance puntual de esta corrida).
  2. `dashboard/overview/overview.component.ts` — mismo patrón: `markForCheck()` en `next` y `error` de `api.get('dashboard/stats/').subscribe()`, única mutación async del componente. `jspdf`/`jspdf-autotable` ya se cargaban con `import()` dinámico desde una corrida anterior, sin cambios ahí.
  3. `dashboard/author-editor/author-editor.component.ts` — `markForCheck()` en `loadAuthor()` (next/error), en el callback `reader.onload` de `onPhotoSelected()` (FileReader async, actualiza el preview de la foto), y en `saveAuthor()` (next/error). Se confirmó vía template que todos los campos usan `[(ngModel)]` disparado por eventos del propio template (no requieren `markForCheck` adicional), salvo estos 4 puntos que son callbacks async fuera de un evento directo.
  4. `categories/category-detail/category-detail.component.ts` — (ya tenía `trackByBook` de una corrida anterior); se agregó `markForCheck()` en: `route.params.subscribe()` (nivel superior, tras `loadCategoryMeta()`/`resetPagination()`/`fetchBooks()`), `loadCategoryMeta()` solo en `next` (el `error` solo hace `console.error`, sin mutar estado del template), `fetchBooks()` (next/error), y en el callback de `setTimeout` de `onSearch()` (tras `resetPagination()` + `fetchBooks()`). `onSearch`/`clearSearch`/`onOrderingChange`/`loadMore` siguen disparados directo desde eventos del template, no necesitaron `markForCheck` en su parte síncrona.
  - Se revisaron los 4 templates `.html` antes de tocar los `.ts`: ninguno usa `async` pipe. Ningún binding, clase, estilo o texto visible fue modificado — único cambio fue la metadata `changeDetection: ChangeDetectionStrategy.OnPush` en `@Component`, la inyección de `ChangeDetectorRef` y las llamadas a `markForCheck()` dentro de callbacks asíncronos ya existentes.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (0 issues), `makemigrations --check --dry-run` OK (sin cambios de código/datos backend, no se corrió la suite completa). Frontend — `npm install` OK (`node_modules` no estaba presente al iniciar la corrida; 1105 paquetes). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web/html2canvas). `npx ng test --watch=false --browsers=ChromeHeadlessNoSandbox` con `CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` -> **8 de 8 tests OK**. No quedó nada temporal sin versionar (solo los 4 componentes + STATE + LOG modificados).
- **Bundle**: Initial 1.34 MB raw / 275.14 kB transferencia (corrida #21: 274.85 kB; variación de +0.29 kB por el código OnPush/markForCheck agregado, sin regresión, muy por debajo del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y los 4 componentes listados arriba).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T00:54:00Z — corrida horaria #23

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar. `git fetch origin --prune` OK (trajo Chris, Felipe, eithan, agent/nightly-optimization, agent/nightly-optimization-chris, main — mismo comportamiento de sesión nueva que en la corrida #22, requirió `--prune`/fetch explícito). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #22 (`2026-09-04T00:05:00Z`) tenía ~49 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #22), confirmado idéntico a `origin/Chris` vía `git merge-base --is-ancestor` (Chris no se movió). Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); checkout directo + `git pull --ff-only origin agent/nightly-optimization-chris` -> already up to date.
- **Push verificado (sección 2.9)**: pendiente de confirmar con `git push --dry-run` justo antes del push real de esta corrida (ver informe final); se asume el mismo resultado OK de corridas anteriores dado que nada cambió en credenciales/remoto.
- **Smoke check (Fase A)**: venv Python 3.12 temporal (scratchpad de la sesión, no versionado; nota: `python3` del sistema es 3.11.15 y `Django==6.0.4` exige >=3.12, hay que usar `python3.12` explícito) + `pip install -r requirements.txt` OK (90 paquetes) + variables de entorno temporales (`SECRET_KEY`, `DEBUG`, `DATABASE_URL` sqlite temporal) -> `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. `manage.py makemigrations --check --dry-run` -> `No changes detected`.
- **Verificación diaria (sección 6)**: esta corrida cae dentro de la ventana UTC 00:00–01:00 y `last_full_verification_utc` seguía vacío en el STATE (nunca se había hecho), así que se ejecutó la suite completa: `python -X utf8 manage.py test` -> `Ran 67 tests in 36.761s` -> **OK** (67/67, incluye avisos preexistentes `InsecureKeyLengthWarning` de PyJWT, no relacionados con esta corrida). `last_full_verification_utc` actualizado a `2026-09-04T00:58:00Z` en el STATE.
- **Fase y lote trabajados**: Fase I (frontend) — cursor previo: "continuar con dashboard/avatar-editor y dashboard/book-editor 'resto', ambos más grandes, evaluar con tiempo de lote completo". Se completó `avatar-editor` en este lote; `book-editor` (385 líneas, ~8 subscribe async + manipulación directa del DOM para el cursor del textarea en `onSceneImageSelected`) se evaluó pero se dejó pendiente para la próxima corrida por ser más riesgoso de lo que permite el tiempo restante de este lote — no se tocó.
- **Cambios aplicados** (1 componente):
  1. `dashboard/avatar-editor/avatar-editor.component.ts` — `ChangeDetectionStrategy.OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en: `loadEditionId()` (next de `getBookDetail`), `loadAvatarData()` (next/error de `getAvatarDetail`), los 5 callbacks `reader.onload` de `onImageSelected()` (avatar + speaking1/2/3 + thinking, FileReader async que actualiza previews), `save()` (next/error de `saveAvatar`) y `deleteAvatar()` (next/error de `deleteAvatar`, que también mutan `saving` usado por `[disabled]` en el botón "Guardar Personaje"). Se confirmó vía `grep` en el template que no usa `| async` pipe. `removeImage()` no necesitó `markForCheck()` por estar disparado directo desde un evento `(click)` del propio template. Ningún binding, clase, estilo, texto visible ni ruta fue modificado — único cambio fue la metadata `@Component`, la inyección de `ChangeDetectorRef` y las llamadas a `markForCheck()` dentro de callbacks asíncronos ya existentes.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (0 issues), `makemigrations --check --dry-run` OK, **`manage.py test` completo OK (67/67, verificación diaria)**. Frontend — `npm install` OK (`node_modules` no estaba presente al iniciar; se reinstaló). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web). `npx ng test --watch=false --browsers=ChromeHeadlessNoSandbox` con `CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` -> **8 de 8 tests OK**. No quedó nada temporal sin versionar (solo `avatar-editor.component.ts` + STATE + LOG modificados).
- **Bundle**: build con exit 0 confirmado, pero la tabla de tamaño "Initial" no quedó capturada en el log de esta corrida (se truncó por un `tail` mal ubicado en el comando de build). El cambio de esta corrida es puramente lógico (una directiva `@Component` + inyección de un servicio + llamadas a un método ya usado en el resto del código), sin nuevas dependencias ni imports pesados, por lo que no se espera variación significativa de tamaño; no se marca como verificado numéricamente esta vez. Próxima corrida: capturar la tabla completa sin `tail` para retomar la comparación.
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `avatar-editor.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T01:56:00Z — corrida horaria #24

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar (sesión nueva, no había fetch previo — hizo falta `git fetch origin` explícito para traer `Chris`, `Felipe`, `eithan`, `agent/nightly-optimization`, `agent/nightly-optimization-chris`; mismo comportamiento ya anotado en corridas #22/#23). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #23 (`2026-09-04T01:00:30Z`) tenía ~54 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #23), confirmado ancestro de `HEAD` vía `git merge-base --is-ancestor origin/Chris HEAD` (Chris no se movió). Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); `git checkout` + `git pull --ff-only origin agent/nightly-optimization-chris` -> already up to date.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: venv Python 3.12.3 temporal (`/tmp/audit-venv-run24`, scratchpad de la sesión, no versionado) + `pip install -r requirements.txt` OK + variables de entorno temporales (`SECRET_KEY`, `DEBUG`, `DATABASE_URL` sqlite temporal) -> `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. `manage.py makemigrations --check --dry-run` -> `No changes detected`. No se repitió la suite completa de backend ni la verificación diaria: no hubo cambios de código/datos backend esta corrida y la verificación diaria (ventana UTC 00:00–01:00) ya se ejecutó en la corrida #23 (`last_full_verification_utc` se mantiene en `2026-09-04T00:58:00Z`). Venv temporal borrado al terminar.
- **Fase y lote trabajados**: Fase I (frontend) — cursor previo: "dashboard/book-editor.component.ts (385 líneas) sigue pendiente explícitamente". Se completó ese componente en este lote.
- **Cambios aplicados** (1 componente, `dashboard/book-editor/book-editor.component.ts`):
  1. `ChangeDetectionStrategy.OnPush` + `ChangeDetectorRef` inyectado por constructor.
  2. `markForCheck()` agregado en `loadAuthors()` y `loadGenres()` (mutan `authorsList`/`genresList`, usados en `*ngFor` del template).
  3. `loadBookData()` — `markForCheck()` solo en el branch `next` (muta `chapters`, `coverPreview`, `avatars`, todos bindings del template); el branch `error` solo muta `loading`, que se confirmó por grep exhaustivo que **no está referenciado en ningún lugar del `.html`**, así que no requirió `markForCheck`.
  4. `parseEpub()` — mismo criterio: `markForCheck()` solo en `next` (muta `chapters`); `error` solo muta `loading` (no bindeado) y dispara un `alert()` síncrono.
  5. `onCoverSelected()` — `markForCheck()` dentro del callback `reader.onload` de `FileReader` (muta `coverPreview`, bindeado en `*ngIf`/`[src]`).
  6. `onSceneImageSelected()` — `markForCheck()` solo en `next`, agregado junto a la mutación de `chapters[editingChapter].content_html` (bindeado vía `[(ngModel)]` al `textarea.code-editor` y al preview `getSafePreviewHtml()`). Se evaluó con cuidado el orden respecto a la manipulación directa del DOM (`document.querySelector('.code-editor')` + `setTimeout(..., 50)` para reposicionar el cursor, tal como advertía el cursor dejado por la corrida #23): el `markForCheck()` se ejecuta de forma síncrona dentro del mismo callback `next()` de la suscripción HTTP, antes de que transcurra el `setTimeout` de 50ms — el ciclo de detección de cambios de Angular (disparado por zone.js al finalizar la tarea síncrona del `next()`) se ejecuta antes de que el timer dispare, por lo que no hay condición de carrera entre el re-render del `textarea`/preview y el reposicionamiento del cursor. `error` solo muta `loading` (no bindeado) + `alert()`, no requirió `markForCheck`.
  7. `saveBook()` — `markForCheck()` solo en el branch `error` (muta `saving`, que sí está bindeado en `[disabled]` e interpolación del botón "Guardar"); el branch `next` navega fuera de la vista (`router.navigate`) y no muta estado visible antes de irse, no requirió `markForCheck`.
  - `openAvatarEditor()`, `onAvatarImageSelected()`, `saveAvatar()` y `removeAvatar()` **no se modificaron** más allá de lo que ya cubre `loadBookData()` (a la que `saveAvatar`/`removeAvatar` llaman en su `next`, ya con `markForCheck` propio): se confirmó por grep exhaustivo en el `.html` que `editingAvatar`, `avatarPreview`, `avatarFile` y `loading` no están referenciados en el template — es código muerto preexistente, ya que la edición real de personajes ocurre en una ruta aparte hacia `dashboard/avatar-editor/avatar-editor.component.ts` (ya con `OnPush` desde la corrida #23). No se tocó por quedar fuera del alcance puntual de esta corrección de `OnPush`.
  - Se revisó el `.html` completo antes de tocar el `.ts` (grep de cada propiedad citada: `loading`, `saving`, `editingAvatar`, `avatarPreview`, `avatarFile`, `chapters`, `coverPreview`, `epubFile`, `pdfFile`, `authorsList`, `genresList`, `avatars`, `isEditing`, `bookId`); no usa `| async`. Ningún binding, clase, estilo, texto visible ni ruta fue modificado — único cambio fue la metadata `@Component`, la inyección de `ChangeDetectorRef` y 7 llamadas a `markForCheck()` dentro de callbacks asíncronos ya existentes.
  - Inventario actualizado de Fase I: 18 de 37 componentes ya tienen `OnPush` (antes de esta corrida eran 17/37). Candidatos pendientes registrados en `phase_cursor.I_frontend` del STATE, ordenados de más pequeño/trivial a más grande/riesgoso, para que la próxima corrida no tenga que re-descubrirlos.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — `manage.py check` OK (0 issues), `makemigrations --check --dry-run` OK (sin cambios de código/datos backend, no se corrió la suite completa esta corrida). Frontend — `npm install` OK (`node_modules` no estaba presente al iniciar la corrida; se reinstaló, 644 paquetes en `node_modules`). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web). `npx ng test --watch=false --browsers=ChromeHeadlessNoSandbox` con `CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` -> **8 de 8 tests OK**. No quedó nada temporal sin versionar (solo `book-editor.component.ts` + STATE + LOG modificados; venv y build artifacts en `/tmp` y `dist/` no versionados).
- **Bundle**: Initial 1.34 MB raw / 275.06 kB transferencia (corrida #23 no pudo capturar el número exacto por un `tail` mal ubicado, pero registró 275.14 kB en una medición previa dentro de la misma corrida; esta corrida: 275.06 kB — sin regresión, dentro del budget 1.6mb/2.5mb).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `book-editor.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T02:55:00Z — corrida horaria #25

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar. `git fetch origin` trajo `Chris`, `Felipe`, `eithan`, `agent/nightly-optimization`, `agent/nightly-optimization-chris` (sesión nueva, sin fetch previo). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #24 (`2026-09-04T01:56:00Z`) tenía ~59 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #24), confirmado con `git merge-base origin/agent/nightly-optimization-chris origin/Chris` == ese hash, y `origin/Chris` en HEAD `3786d288` (no se movió). Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris`, ya al día.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: entorno de esta corrida (contenedor nuevo) trae `python3` 3.11.15 y `python3.12` disponible en `/usr/bin/python3.12`, pero sin Django instalado (`ModuleNotFoundError: No module named 'django'`, no hay venv persistente entre corridas). No se instaló el stack backend (90 paquetes, incluye dependencias pesadas de IA) por acotarse al lote de esta corrida, que se centró en Fase I (frontend, no requiere backend). Registrado como **PRUEBAS BACKEND NO DISPONIBLES esta corrida** (no es una MR nueva: ya cubierto por MR-0002, que documenta la falta de BD real de producción; el backend en sí es instalable, solo no se hizo por alcance/tiempo de este lote puramente frontend).
- **Fase y lote trabajados**: Fase I (frontend) — cursor de la corrida #24: "empezar por los triviales/pequeños (footer, audio-visualizer, forgot-password, verify-email, assisted-reading-panel, author-detail-page) antes de abordar los grandes". Se completaron los 6 en este lote.
- **Cambios aplicados** (6 componentes, todos con `ChangeDetectionStrategy.OnPush`):
  1. `core/components/footer/footer.component.ts` — solo `OnPush` (10 líneas, sin estado mutable, sin inputs).
  2. `core/components/audio-visualizer/audio-visualizer.component.ts` — solo `OnPush` (11 líneas, solo `@Input level`/`isActive`, Angular re-evalúa bindings de `@Input` automáticamente con `OnPush`, no requiere `markForCheck`).
  3. `auth/forgot-password/forgot-password.component.ts` — `OnPush` + `ChangeDetectorRef` + `markForCheck()` en `next()`/`error()` de `onSubmit()` (mutan `isLoading`/`message`/`error`, los 3 bindeados en el `.html` — confirmado por grep).
  4. `auth/verify-email/verify-email.component.ts` — `OnPush` + `markForCheck()` en el branch `else` de `ngOnInit()` (parámetros de query faltantes) y en `next()`/`error()` de `verify()` (mutan `isLoading`/`successMessage`/`errorMessage`, los 3 bindeados en el `.html`).
  5. `library/reader/assisted-reading-panel/assisted-reading-panel.component.ts` — solo `OnPush`, **sin** `markForCheck`: se confirmó por grep exhaustivo del `.html` que las 15 propiedades de `AssistedReadingService` (`ar.enabled$`, `ar.mode$`, etc.) se leen exclusivamente vía `| async` (que ya llama `markForCheck` internamente), sin ningún acceso directo a `.value`; `showAdvanced` solo se muta desde un `(click)` del propio template.
  6. `catalog/author-detail-page/author-detail-page.component.ts` — `OnPush` + `markForCheck()` en el `isLoading = true` síncrono (dentro del callback de `route.paramMap.subscribe`, no de un evento de plantilla) antes de la llamada HTTP, y en `next()`/`error()` de `loadAuthorDetails()` (mutan `author`/`isLoading`/`errorMsg`, los 3 bindeados en el `.html`).
  - Ningún binding, clase, estilo, texto visible ni ruta fue modificado en ningún componente — único cambio en cada uno fue la metadata `@Component.changeDetection` y, donde correspondía, la inyección de `ChangeDetectorRef` y llamadas a `markForCheck()` dentro de callbacks asíncronos ya existentes.
  - Inventario actualizado de Fase I: 24 de 37 componentes ya tienen `OnPush` (antes de esta corrida eran 18/37). Pendientes ordenados de mediano a grande registrados en `phase_cursor.I_frontend` del STATE para la próxima corrida (empezar por `auth/reset-password.component.ts` y `auth/login.component.ts`).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — **NO DISPONIBLES esta corrida** (ver Smoke check arriba; no bloquea porque no se tocó backend). Frontend — `npm ci` OK (`node_modules` no estaba presente al iniciar; 1105 paquetes instalados desde `package-lock.json`, sin agregar dependencias nuevas). `npm run build` (producción) -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, no relacionados con esta corrida). `npx ng test --watch=false --browsers=ChromeHeadlessNoSandbox` con `CHROME_BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` -> **8 de 8 tests OK**. No quedó nada temporal sin versionar (solo los 6 componentes + STATE + LOG modificados; `node_modules`/`dist` no versionados, cubiertos por `.gitignore`).
- **Bundle**: Initial 1.34 MB raw / 274.97 kB transferencia (corrida #24 no dejó un número comparable capturado en el log; esta corrida deja la primera medición limpia de referencia para comparar en corridas futuras). Sin cambio esperado: los 6 cambios de esta corrida son solo de estrategia de detección de cambios en runtime (`OnPush`), no afectan imports/tree-shaking/tamaño de bundle.
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y los 6 componentes).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.


## 2026-09-04T04:00:00Z — corrida horaria #26

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar (sesión nueva). `git fetch origin` trajo `Chris`, `Felipe`, `eithan`, `agent/nightly-optimization`, `agent/nightly-optimization-chris` (sesión nueva, sin fetch previo). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #25 (`2026-09-04T02:55:00Z`) tenía ~59 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #25), confirmado con `git merge-base origin/Chris origin/agent/nightly-optimization-chris` == ese hash; `origin/Chris` sigue en HEAD `3786d288` (no se movió). Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris`, ya al día.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**. Nota: contrario a la advertencia genérica de la plantilla de la rutina sobre "~30 corridas sin llegar nunca a GitHub", el historial real de `origin/agent/nightly-optimization-chris` ya contiene 25 commits previos de corridas anteriores (`run_count_today` venía en 25), es decir el push SÍ ha estado funcionando; se registra la discrepancia para que quede claro que la advertencia de la plantilla está desactualizada.
- **Smoke check (Fase A)**: contenedor nuevo sin Django instalado (`ModuleNotFoundError: No module named 'django'`, sin venv persistente entre corridas) — igual que corridas anteriores (ver MR-0002). No se instaló el stack backend por acotarse al lote de esta corrida, centrado en Fase I (frontend, no requiere backend).
- **Fase y lote trabajados**: Fase I (frontend) — cursor de la corrida #25: "continuar con los medianos (auth/reset-password.component.ts 88, auth/login.component.ts 86)". Se completaron los 2 en este lote.
- **Cambios aplicados** (2 componentes, ambos con `ChangeDetectionStrategy.OnPush`):
  1. `auth/reset-password/reset-password.component.ts` — `OnPush` + `ChangeDetectorRef` inyectado por constructor + `markForCheck()` en: el `return` temprano de `ngOnInit()` cuando faltan `uid`/`token` en `queryParams` (muta `isInvalidLink`/`error`), en `next()`/`error()` de `validatePasswordReset()` dentro de `ngOnInit()` (mutan `isValidating`/`isInvalidLink`/`error`), y en `next()`/`error()` de `confirmPasswordReset()` en `onSubmit()` (mutan `isLoading`/`message`/`error`). Los 5 campos (`isLoading`, `isValidating`, `message`, `error`, `isInvalidLink`) están bindeados en el `.html` — confirmado por grep. `newPassword`/`confirmPassword` se mutan vía `ngModel` desde eventos de plantilla del propio componente, no necesitaron `markForCheck`.
  2. `auth/login/login.component.ts` — ya usaba `inject()`; se agregó `private cdr = inject(ChangeDetectorRef)` + `markForCheck()` **solo** en el branch `error` de `onSubmit()` (muta `errorMsg`/`isLoading`, ambos bindeados en el `.html`). El branch `next` navega fuera de la vista vía `router.navigateByUrl` y no muta estado visible antes de irse, no requirió `markForCheck`. `isLoading = true` inicial y `togglePassword()` son síncronicos dentro de eventos de plantilla (`ngSubmit`/`click`), ya cubiertos por la detección de cambios normal de Angular.
  - Ningún binding, clase, estilo, texto visible ni ruta fue modificado en ningún componente — único cambio en cada uno fue la metadata `@Component.changeDetection` y, donde correspondía, la inyección de `ChangeDetectorRef` y llamadas a `markForCheck()` dentro de callbacks asíncronos ya existentes.
  - Inventario actualizado de Fase I: 26 de 37 componentes ya tienen `OnPush` (antes de esta corrida eran 24/37). Pendientes ordenados de mediano a grande registrados en `phase_cursor.I_frontend` del STATE para la próxima corrida (empezar por `catalog/checkout.component.ts` 97 y `auth/register.component.ts` 98).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — **NO DISPONIBLES esta corrida** (ver Smoke check arriba; no bloquea porque no se tocó backend). Frontend — `npm ci` OK (`node_modules` no estaba presente al iniciar; 1105 paquetes instalados desde `package-lock.json`, sin agregar dependencias nuevas). `npm run build -- --configuration=production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, no relacionados). `npx ng test --watch=false --karma-config=karma.conf.ci.js` con un `karma.conf.ci.js` temporal creado solo para esta corrida (custom launcher `ChromeHeadlessNoSandbox` apuntando a `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, ya que `ChromeHeadless` por defecto falla al correr como root sin `--no-sandbox` y el flag `--no-sandbox` de `ng test` no es válido en este Angular CLI) -> **8 de 8 tests OK**. El `karma.conf.ci.js` temporal se borró antes del commit, no quedó versionado. No quedó nada temporal sin versionar (solo los 2 componentes + STATE + LOG modificados; `node_modules`/`dist` no versionados, cubiertos por `.gitignore`).
- **Bundle**: Initial 1.34 MB raw / 275.09 kB transferencia (corrida #25: 274.97 kB; diferencia de 0.12 kB dentro del ruido normal de hashing, sin regresión real — `OnPush` no afecta tree-shaking/bundle size, solo estrategia de detección de cambios en runtime).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y los 2 componentes).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T05:03:00Z — corrida horaria #27

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar (sesión nueva). `git fetch origin` trajo `Chris`, `Felipe`, `eithan`, `agent/nightly-optimization`, `agent/nightly-optimization-chris` (sesión nueva, sin fetch previo; también trajo movimiento de `main`, no relevante para esta rama). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #26 (`2026-09-04T04:00:00Z`) tenía ~54 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #26), confirmado con `git merge-base origin/Chris origin/agent/nightly-optimization-chris` == ese hash. Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris` + `git pull --ff-only` -> ya al día.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: venv Python 3.12.3 temporal (`/tmp/audit-venv-run27`, scratchpad de la sesión, no versionado, borrado al terminar) + `pip install -r requirements.txt` OK + variables de entorno temporales (`SECRET_KEY`, `DEBUG`, `DATABASE_URL` sqlite temporal en `/tmp`, borrado al terminar) -> `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`. No se corrió `makemigrations --check --dry-run` ni la suite completa: no hubo cambios de código/datos backend esta corrida (solo Fase I frontend) y la verificación diaria ya se hizo en la corrida #23 (`last_full_verification_utc` se mantiene en `2026-09-04T00:58:00Z`).
- **Fase y lote trabajados**: Fase I (frontend) — cursor de la corrida #26: "continuar con catalog/checkout.component.ts (97) y auth/register.component.ts (98)". Al abrir `checkout.component.ts` se confirmó que `confirmPurchase()` arma el flujo de pago hacia Webpay (llama a `finance/pay/` y construye un `<form>` que se envía a `data.url`/`token_ws`) — cae directo bajo la prohibición explícita de la sección 5 ("NO: tocar finance/Webpay"), así que se excluyó permanentemente de Fase I sin tocarlo (ni siquiera el cambio de metadata `@Component`, para minimizar cualquier riesgo de tocar código del flujo de pago). Se completó `register.component.ts` y, en su lugar de `checkout`, se adelantó `core/components/liyumi-widget/liyumi-widget.component.ts` (101 líneas) de la lista de medianos.
- **Cambios aplicados** (2 componentes, ambos con `ChangeDetectionStrategy.OnPush`):
  1. `auth/register/register.component.ts` — `OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` **solo** en `next()`/`error()` de `auth.register().subscribe()` dentro de `onSubmit()` (mutan `successMsg`/`errorMsg`/`isLoading`, los 3 bindeados en el `.html` — confirmado por grep). El resto de mutaciones (`isLoading`/`errorMsg`/`successMsg` al inicio de `onSubmit()`, `markAllAsTouched()` en el early-return por formulario inválido, `togglePassword()`/`toggleConfirmPassword()`, y los cambios de estado del `FormGroup` reactivo vía `formControlName` — `touched`/`invalid` que pintan `is-invalid` y los mensajes de validación) ocurren todos dentro del mismo evento de plantilla (`ngSubmit`/`click`/`input` del propio componente), ya cubiertos por la detección de cambios normal de Angular bajo `OnPush` (el evento se origina en la propia vista del componente) — no requirieron `markForCheck` adicional.
  2. `core/components/liyumi-widget/liyumi-widget.component.ts` — `OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en los 4 `subscribe()` de `ngOnInit()` (`state$`, `message$`, `isTalking$`, `isOpen$` de `LiyumiService`, que pueden emitir desde cualquier parte de la app, no solo desde eventos de la propia plantilla) y en `startOnboarding()`/`showNextOnboardingMessage()` (únicos puntos donde `isOnboarding` se muta fuera de un evento de plantilla — se alcanzan solo desde el `setTimeout(1500ms)` de `ngOnInit()`). Los handlers de click del propio template (`nextOnboardingStep()`, `skipOnboarding()`, `dismissMessage()`, `toggleWidget()`, `closeWidget()`) no necesitaron `markForCheck` adicional. **Hallazgo colateral (no es bug, solo nota informativa)**: se confirmó por grep exhaustivo (`*.ts`/`*.html` de toda la app) que `<app-liyumi-widget>`/`LiyumiWidgetComponent` no está declarado, importado ni renderizado en ningún template actual (ni `app.component.html` ni ningún otro) — parece código huérfano/pendiente de cablear. No se borró ni se modificó su lógica más allá del cambio de `OnPush` (fuera del alcance de esta corrida); queda anotado para que el dueño decida si lo cablea o lo retira.
  - Ningún binding, clase, estilo, texto visible ni ruta fue modificado en ningún componente — único cambio en cada uno fue la metadata `@Component.changeDetection` y, donde correspondía, la inyección de `ChangeDetectorRef` y llamadas a `markForCheck()` en callbacks ya existentes que no se originan en eventos de la propia plantilla.
  - `catalog/checkout.component.ts` queda marcado como **excluido permanentemente** de Fase I (no reintentar en corridas futuras) por la regla de la sección 5 sobre finance/Webpay.
  - Inventario actualizado de Fase I: 28 de 37 componentes ya tienen `OnPush` (antes de esta corrida eran 26/37; de los 9 restantes, 1 —`checkout`— queda excluido para siempre). Pendientes ordenados de mediano a grande registrados en `phase_cursor.I_frontend` del STATE para la próxima corrida (empezar por `users/profile.component.ts` 121 y `categories/categories.component.ts` 109).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno formal. MR-0002 y MR-0003 siguen abiertas sin cambios. (El hallazgo del `liyumi-widget` huérfano se deja como nota informativa dentro del cursor de Fase I, no como MR nueva, porque no es un error — es simplemente un componente completo y funcional que aún no fue enlazado a ninguna vista, y no hay ambigüedad que resolver de forma autónoma.)
- **Pruebas**: backend — `manage.py check` OK (0 issues); no se tocó backend, no se corrió `makemigrations --check` ni la suite completa (no aplica, sin cambios backend). Frontend — `npm ci` OK (`node_modules` no estaba presente al iniciar; 1105 paquetes instalados desde `package-lock.json`, sin agregar dependencias nuevas). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, no relacionados). `npx ng test --watch=false --karma-config=karma.conf.ci.js` con un `karma.conf.ci.js` temporal creado solo para esta corrida (custom launcher `ChromeHeadlessNoSandbox` apuntando a `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`) -> **8 de 8 tests OK**. El `karma.conf.ci.js` temporal y `dist/`/`coverage/` se borraron antes del commit, no quedaron versionados. No quedó nada temporal sin versionar (solo los 2 componentes + STATE + LOG modificados; `node_modules` no versionado, cubierto por `.gitignore`).
- **Bundle**: Initial 1.34 MB raw / 275.07 kB transferencia (corrida #26: 275.09 kB; diferencia de -0.02 kB, dentro del ruido normal de hashing, sin regresión — `OnPush` no afecta tree-shaking/bundle size, solo estrategia de detección de cambios en runtime).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `register.component.ts` y `liyumi-widget.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T05:55:19Z — corrida horaria #28

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar (sesión nueva, sin cambios sin commitear). `git fetch origin Chris agent/nightly-optimization-chris agent/nightly-optimization Felipe eithan main` trajo las 5 ramas relevantes como nuevas referencias locales (sesión nueva, sin fetch previo). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #27 (`2026-09-04T05:03:00Z`) tenía ~52 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #27), confirmado con `git merge-base origin/Chris origin/agent/nightly-optimization-chris` == ese hash; `origin/Chris` sigue en HEAD `3786d288` (no se movió). Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris`, ya al día (no hizo falta `pull` adicional).
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: contenedor nuevo sin Django instalado (`No module named 'django'`, sin venv persistente entre corridas) — igual que corridas anteriores (ver MR-0002). No se instaló el stack backend por acotarse al lote de esta corrida, centrado en Fase I (frontend, no requiere backend). No aplica `makemigrations --check` ni verificación diaria (`last_full_verification_utc` ya está al día de hoy, corrida #23, `2026-09-04T00:58:00Z`).
- **Fase y lote trabajados**: Fase I (frontend) — cursor de la corrida #27: "continuar con `users/profile.component.ts` (121) y `categories/categories.component.ts` (109)". Se completaron los 2 en este lote.
- **Cambios aplicados** (2 componentes, ambos con `ChangeDetectionStrategy.OnPush`):
  1. `users/profile/profile.component.ts` — `OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en el `next()` de `loadProfile()` (`api.get('users/profile/').subscribe()`, mutaciones asíncronas de `avatarColor`/`selectedTheme`/`userInitials` — confirmadas por grep en el `.html`: `[style.background-color]="avatarColor"`, `{{ userInitials }}`, `[class.selected]="theme.id === selectedTheme"`) y en `next()`/`error()` de `onSubmit()` (`api.patch()`, mutan `loading`, bindeado en `[disabled]` y en 3 `*ngIf` del template). No se tocó `(click)="avatarColor = color"` ni `previewTheme()` (ambos disparados directo desde el template, ya cubiertos por la detección de cambios normal de `OnPush`) ni `onFileSelected()` (no-op, deshabilitado). Los `patchValue()` sobre el `FormGroup` reactivo no requirieron `markForCheck` (el `ControlValueAccessor` escribe directo al DOM vía `writeValue()`).
  2. `categories/categories.component.ts` — `OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en `next()`/`error()` de `loadCounts()` (`api.get('catalog/genres/...').subscribe()`, mutan `categories`/`isLoading`, consumidos por el getter `filteredCategories` bindeado en `*ngFor`/`*ngIf` del `.html`). `onSearch()`/`clearSearch()` no requirieron `markForCheck` por ser eventos de plantilla (`input`/`click`) síncronos; ya tenía `trackBy` (`trackByCat`) en el `*ngFor`, sin cambios ahí.
  - Ningún binding, clase, estilo, texto visible ni ruta fue modificado en ningún componente — único cambio en cada uno fue la metadata `@Component.changeDetection` y, donde correspondía, la inyección de `ChangeDetectorRef` y llamadas a `markForCheck()` dentro de callbacks asíncronos ya existentes (`subscribe()` de `HttpClient`).
  - Inventario re-verificado por grep exhaustivo (`grep -rl ChangeDetectionStrategy.OnPush --include=*.component.ts src/app`): **29 de 37** componentes ya tienen `OnPush` tras esta corrida (antes 27/37; se corrige aquí el conteo "28/37" anotado en el log de la corrida #27, que no coincidía exactamente con el conteo real por grep). `catalog/checkout.component.ts` sigue **excluido permanentemente** de Fase I por tocar Webpay/finance (ver corrida #27). Pendientes restantes confirmados por grep (7, de menor a mayor tamaño/riesgo): `library/tavern/tavern.component.ts` (128), `app.component.ts` (158, componente raíz, evaluar con cuidado especial), `library/ai-chat/ai-chat.component.ts` (172), `characters/demo-chat-page/demo-chat-page.component.ts` (226), `catalog/book-detail-page/book-detail-page.component.ts` (361), `home/home.component.ts` (439), `library/reader/reader.component.ts` (2303, candidato grande/riesgoso, dejar para el final). Siguiente corrida: continuar con `library/tavern/tavern.component.ts` (128) y evaluar `library/ai-chat/ai-chat.component.ts` (172) si el tiempo de lote alcanza.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — **NO DISPONIBLES esta corrida** (ver Smoke check arriba; no bloquea porque no se tocó backend). Frontend — `npm ci` OK (`node_modules` no estaba presente al iniciar; 1105 paquetes instalados desde `package-lock.json`, sin agregar dependencias nuevas). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, no relacionados). `npx ng test --watch=false --karma-config=karma.conf.ci.js` con un `karma.conf.ci.js` temporal creado solo para esta corrida (custom launcher `ChromeHeadlessNoSandbox` apuntando a `/opt/pw-browsers/chromium`, ya que `ChromeHeadless` por defecto falla al correr como root sin `--no-sandbox`) -> **8 de 8 tests OK**. El `karma.conf.ci.js` temporal se borró antes del commit, no quedó versionado. No quedó nada temporal sin versionar (solo los 2 componentes + STATE + LOG modificados; `node_modules`/`dist` no versionados, cubiertos por `.gitignore`).
- **Bundle**: Initial 1.34 MB raw / 275.19 kB transferencia (corrida #27: 275.07 kB; diferencia de +0.12 kB por las 2 inyecciones nuevas de `ChangeDetectorRef`, dentro del ruido normal de hashing, sin regresión real — `OnPush` no afecta tree-shaking/bundle size, solo estrategia de detección de cambios en runtime).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `profile.component.ts` y `categories.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T06:54:28Z — corrida horaria #29

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar (sesión nueva). `git fetch origin --prune` trajo `Chris`, `Felipe`, `eithan`, `agent/nightly-optimization`, `agent/nightly-optimization-chris` como referencias nuevas (sesión nueva sin fetch previo; también trajo movimiento de `main`, no relevante para esta rama). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #28 (`2026-09-04T06:05:00Z`) tenía ~49 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #28), confirmado con `git merge-base origin/Chris origin/agent/nightly-optimization-chris` == ese hash y `git rev-parse origin/Chris` == ese mismo hash (no se movió). Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris` + `git pull --ff-only` -> ya al día.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: contenedor nuevo sin Django instalado (`ModuleNotFoundError: No module named 'django'` en `python3` 3.11 y `python3.12`, sin venv persistente entre corridas) — igual que corridas #25, #26, #28 (ver MR-0002). No se instaló el stack backend (90 paquetes) por acotarse al lote de esta corrida, centrado en Fase I (frontend, no requiere backend). No aplica `makemigrations --check` ni verificación diaria (`last_full_verification_utc` ya está al día de hoy, corrida #23, `2026-09-04T00:58:00Z`).
- **Fase y lote trabajados**: Fase I (frontend) — cursor de la corrida #28: "continuar con `library/tavern/tavern.component.ts` (128) y evaluar `library/ai-chat/ai-chat.component.ts` (172) si el tiempo alcanza". Se completó `tavern.component.ts`; no alcanzó el tiempo de lote para `ai-chat.component.ts` (componente de chat con más superficie async a auditar con cuidado), queda para la próxima corrida junto con `app.component.ts`.
- **Cambios aplicados** (1 componente, `ChangeDetectionStrategy.OnPush`):
  1. `library/tavern/tavern.component.ts` — `OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en tres puntos, ninguno originado en un evento de plantilla: (a) dentro de `update()` en `animateOdometer()` — se ejecuta en cada `requestAnimationFrame` (hasta ~1s de animación) y muta `displayBalance`, bindeado en `{{ displayBalance }}` (`tavern.component.html:76`); (b) dentro del `setInterval` de `watchAd()` — decrementa `adTimer` (bindeado en `{{ adTimer }}s` y `[style.width.%]="(5 - adTimer) * 20"`) y eventualmente dispara `claimAdReward()`, que muta `adLoading` (bindeado en `[class.is-loading]`, `[disabled]`, dos `*ngIf`); (c) `next()`/`error()` de `claimAdReward()` (`api.post('users/me/add_ink/').subscribe()`, mutan `inkBalance`/`adLoading`). No se tocó `fetchBalance()` (ya delega en `animateOdometer()`, que ahora se auto-marca), ni `isLoggedIn()`/`buyChest()`/`trackByIndex()` (llamados directo desde el template, ya cubiertos por `OnPush`). `ngAfterViewInit()` usa `IntersectionObserver` para manipular el DOM directo (`classList.add('visible')` sobre `.reveal`) sin leer/escribir estado del componente — no requiere `markForCheck`.
  - Ningún binding, clase, estilo, texto visible ni ruta fue modificado — único cambio fue la metadata `@Component.changeDetection` y la inyección de `ChangeDetectorRef` + llamadas a `markForCheck()` en callbacks async ya existentes (rAF, `setInterval`, `subscribe` de `HttpClient`).
  - **Nota de proceso**: durante la edición se produjo un error propio (un `Edit` encadenado dejó el método `animateOdometer()` duplicado en el archivo); se detectó de inmediato al releer el archivo completo antes de compilar y se corrigió eliminando la definición duplicada, dejando una sola versión (con `markForCheck()`). Se verificó el archivo final completo antes de continuar — sin este chequeo el build habría fallado por método duplicado.
  - Inventario re-verificado por grep exhaustivo (`grep -rl ChangeDetectionStrategy.OnPush --include=*.component.ts src/app`): **30 de 37** componentes ya tienen `OnPush` tras esta corrida (antes 29/37). `catalog/checkout.component.ts` sigue **excluido permanentemente** de Fase I por tocar Webpay/finance (ver corrida #27). Pendientes restantes (6, de menor a mayor tamaño/riesgo): `app.component.ts` (158, componente raíz — evaluar con cuidado especial), `library/ai-chat/ai-chat.component.ts` (172), `characters/demo-chat-page/demo-chat-page.component.ts` (226), `catalog/book-detail-page/book-detail-page.component.ts` (361), `home/home.component.ts` (439), `library/reader/reader.component.ts` (2303, dejar para el final). Siguiente corrida: continuar con `app.component.ts` (revisar con cuidado por ser el componente raíz de la app) y evaluar `ai-chat.component.ts` si el tiempo alcanza.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — **NO DISPONIBLES esta corrida** (ver Smoke check arriba; no bloquea porque no se tocó backend). Frontend — `npm ci` OK (`node_modules` no estaba presente al iniciar; 1105 paquetes instalados desde `package-lock.json`, sin agregar dependencias nuevas). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, no relacionados). `npx ng test --watch=false --karma-config=karma.conf.ci.js` con un `karma.conf.ci.js` temporal creado solo para esta corrida (custom launcher `ChromeHeadlessNoSandbox` apuntando a `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`) -> **8 de 8 tests OK**. El `karma.conf.ci.js` temporal y `dist/`/`coverage/` se borraron antes del commit, no quedaron versionados. No quedó nada temporal sin versionar (solo `tavern.component.ts` + STATE + LOG modificados; `node_modules` no versionado, cubierto por `.gitignore`).
- **Bundle**: Initial 1.34 MB raw / 275.20 kB transferencia (corrida #28: 275.19 kB; diferencia de +0.01 kB por la inyección nueva de `ChangeDetectorRef`, dentro del ruido normal de hashing, sin regresión real — `OnPush` no afecta tree-shaking/bundle size, solo estrategia de detección de cambios en runtime).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `tavern.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T07:53:49Z — corrida horaria #30

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar; la sesión partió en la rama del harness (`claude/admiring-noether-81j82v`, sin relación con esta rutina) y se cambió a la rama de trabajo correcta. `git fetch origin --prune` trajo `Chris`, `Felipe`, `eithan`, `agent/nightly-optimization`, `agent/nightly-optimization-chris` (sesión nueva sin fetch previo; nótese que `origin/claude/admiring-noether-81j82v` fue borrada del remoto durante el `--prune`, no relevante para esta rutina). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #29 (`2026-09-04T06:59:43Z`) tenía ~54 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #29), confirmado con `git rev-parse origin/Chris` == ese mismo hash (no se movió) y `git merge-base --is-ancestor origin/Chris HEAD` -> ancestro confirmado. Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); `git checkout -B agent/nightly-optimization-chris origin/agent/nightly-optimization-chris` + `git pull --ff-only` -> ya al día.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: no aplica esta corrida (no se tocó backend, contenedor sin Python/Django instalado). `last_full_verification_utc` ya está al día de hoy (corrida #23, `2026-09-04T00:58:00Z`), no correspondía verificación diaria completa (hora actual 07:53 UTC).
- **Fase y lote trabajados**: Fase I (frontend) — cursor de la corrida #29: "continuar con `app.component.ts` (158, componente raíz — revisar con cuidado especial) y evaluar `ai-chat.component.ts` si el tiempo alcanza". Se completó `app.component.ts`; no se alcanzó a evaluar `ai-chat.component.ts` en este lote (se prefirió dedicar el tiempo completo a revisar con cuidado el componente raíz, dado su alcance global sobre nav/layout/sesión).
- **Cambios aplicados** (1 componente, `ChangeDetectionStrategy.OnPush`, componente raíz de la app):
  1. `app.component.ts` — `OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en los 5 puntos donde el componente muta estado leído por el template fuera de un evento de plantilla propio: (a) `constructor()` — `router.events.pipe(filter(NavigationEnd)).subscribe()` muta `isDashboard` (bindeado en `*ngIf="!isDashboard"` del nav, `[class.dashboard-mode]="isDashboard"` del `<main>` y `*ngIf="!isDashboard"` del footer, confirmado por grep en `app.component.html`); (b) `ngOnInit()` — `authService.isLoggedIn$.subscribe()` controla el flujo que determina `isLoggedIn`/`isAdmin` (getters usados en varios `*ngIf` del menú y submenú admin), se agregó `markForCheck()` al final del callback; (c) `ngOnInit()` — los dos `window.addEventListener('error'/'unhandledrejection', ...)` mutan `globalError` (bindeado en `*ngIf="globalError"` / `{{ globalError }}`), eventos de `window` fuera de la zona de detección propia del componente; (d) `loadUserProfile()` — `next()` de `getUserProfile().subscribe()` muta `userAvatarUrl`/`userAvatarColor` (bindeados en `*ngIf="userAvatarUrl"` `[src]`, `*ngIf="!userAvatarUrl"` `[style.background-color]` e indirectamente `userInitials`); (e) `triggerShake()` — muta `shakeState` (bindeado en `[@shake]="shakeState"`), invocado desde `inkBalance$.subscribe()` (async, fuera de evento de plantilla) y contiene un `setTimeout` interno; se agregó `markForCheck()` tanto en la asignación inmediata como dentro del callback del `setTimeout`. NO se tocó: `inkBalance$` (ya es un `Observable` consumido con `| async` en el html, se auto-actualiza bajo `OnPush` sin `markForCheck`); `toggleMenu()`/`logout()`/`openTavern()` (invocados directo vía `(click)` en el propio template del componente, ya cubiertos por la detección de cambios local automática de `OnPush`); `chatService.profileUpdated$.subscribe()` (solo llama a `loadUserProfile()`, que ya tiene su `markForCheck()` interno); `CapacitorApp.addListener('backButton', ...)` (solo muta `lastBackPressTime`, variable privada no bindeada en el template); getters puros (`prepareRoute`, `userName`, `userInitials`, `isLoggedIn`, `isAdmin`) sin estado propio que mutar.
  - Ningún binding, clase, estilo, texto visible, ruta ni animación fue modificado — único cambio fue la metadata `@Component.changeDetection` y la inyección de `ChangeDetectorRef` + `markForCheck()` en los 5 puntos async listados arriba, todos sobre callbacks ya existentes (router events, HTTP subscribe, listeners de `window`, `setTimeout`).
  - Inventario re-verificado por grep exhaustivo (`grep -rl ChangeDetectionStrategy.OnPush --include=*.component.ts src/app`): **31 de 37** componentes ya tienen `OnPush` tras esta corrida (antes 30/37). `catalog/checkout.component.ts` sigue **excluido permanentemente** de Fase I por tocar Webpay/finance (ver corrida #27). Pendientes restantes (5, de menor a mayor tamaño/riesgo): `library/ai-chat/ai-chat.component.ts` (172), `characters/demo-chat-page/demo-chat-page.component.ts` (226), `catalog/book-detail-page/book-detail-page.component.ts` (361), `home/home.component.ts` (439), `library/reader/reader.component.ts` (2303, dejar para el final). Siguiente corrida: continuar con `library/ai-chat/ai-chat.component.ts` (172).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — **NO DISPONIBLES esta corrida** (contenedor sin Python/Django instalado; no bloquea porque no se tocó backend, ver MR-0002). Frontend — `npm ci` OK (`node_modules` no estaba presente al iniciar; 1105 paquetes instalados desde `package-lock.json`, sin agregar dependencias nuevas). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, no relacionados). `npx ng test --watch=false --karma-config=karma.conf.ci.js` con un `karma.conf.ci.js` temporal creado desde cero (este repo no traía `karma.conf.js` propio en el árbol; se generó uno mínimo con `karma-jasmine` + `karma-chrome-launcher` + el plugin de `@angular-devkit/build-angular`, launcher `ChromeHeadlessNoSandbox` apuntando a `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`) -> **8 de 8 tests OK**. El `karma.conf.ci.js` temporal y la carpeta `coverage/` generada se borraron antes del commit, no quedaron versionados. No quedó nada temporal sin versionar (solo `app.component.ts` + STATE + LOG modificados; `node_modules`/`dist` no versionados, cubiertos por `.gitignore`).
- **Bundle**: Initial 1.34 MB raw / 275.27 kB transferencia (corrida #29: 275.20 kB; diferencia de +0.07 kB por la inyección nueva de `ChangeDetectorRef` + 5 llamadas a `markForCheck()`, dentro del ruido normal de hashing, sin regresión real — `OnPush` no afecta tree-shaking/bundle size, solo estrategia de detección de cambios en runtime).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `app.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T09:10:00Z — corrida horaria #31

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar; la sesión partió en la rama del harness (`claude/admiring-noether-6df61l`, sin relación con esta rutina) y se cambió a la rama de trabajo correcta. `git fetch origin --prune` trajo `Chris`, `Felipe`, `eithan`, `agent/nightly-optimization`, `agent/nightly-optimization-chris` (sesión nueva sin fetch previo). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #30 (`2026-09-04T08:05:00Z`) tenía ~50 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #30), confirmado con `git merge-base origin/agent/nightly-optimization-chris origin/Chris` == ese hash y `git rev-parse origin/Chris` == ese mismo hash (no se movió). Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap); `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris` -> ya al día, no hizo falta `pull` adicional.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**. Nota: a diferencia de lo que advierte la plantilla de este prompt sobre "~30 corridas sin llegar nunca a GitHub", el estado real del remoto contradice esa advertencia: `agent/nightly-optimization-chris` en origin ya trae 30 commits reales (`audit(hourly #1)` .. `audit(hourly #30)`), con `NIGHT_AUDIT_STATE.json`/`NIGHT_AUDIT_LOG.md` presentes y `last_push_ok: true` desde la corrida #30. Se registra la discrepancia para que el dueño revise por qué la plantilla del prompt programado no se actualizó, pero no se trata como bloqueo.
- **Smoke check (Fase A)**: contenedor sin Python/Django instalado (`ModuleNotFoundError: No module named 'django'`, sin venv persistente entre corridas) — igual que corridas anteriores (ver MR-0002). No se instaló el stack backend por acotarse al lote de esta corrida, centrado en Fase I (frontend, no requiere backend). No corresponde verificación diaria completa (`last_full_verification_utc` ya está al día de hoy, corrida #23, `2026-09-04T00:58:00Z`; hora actual 09:10 UTC, fuera de la ventana 00:00–01:00).
- **Fase y lote trabajados**: Fase I (frontend) — cursor de la corrida #30: "continuar con `library/ai-chat/ai-chat.component.ts` (172)". Se completó ese componente en este lote.
- **Cambios aplicados** (1 componente, `ChangeDetectionStrategy.OnPush`):
  1. `library/ai-chat/ai-chat.component.ts` — `OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en los 7 puntos async donde el componente muta estado leído por el template fuera de un evento de plantilla propio: (a) `ngOnInit()` — `route.paramMap.subscribe()` muta `avatarId` (bindeado en `[class.active]="char.id === avatarId"` del panel de recientes) y dispara `loadChatSession()`; (b) `ngOnInit()` — `chatService.messages$.subscribe()` muta `messages` (bindeado en el `*ngFor` de la lista de mensajes); (c) `loadChatSession()` — el `setTimeout` de seguridad a 6s muta `avatar`/`session` si el backend no respondió a tiempo (bindeados en `*ngIf="avatar"`/`*ngIf="session"`, `{{ avatar.name }}`, `[disabled]="!session"`, etc., confirmado por grep en el `.html`); (d) `loadChatSession()` — `next()`/`error()` de `chatService.getAvatar().subscribe()` muta `avatar`; (e) `loadChatSession()` — `next()`/`error()` de `chatService.getSession().subscribe()` muta `session`; (f) `loadRecentCharacters()` — `chatService.getRecentAvatars().subscribe()` muta `recentCharacters` (bindeado en el `*ngFor` del panel de recientes); (g) `sendMessage()` — `next()`/`error()` de `chatService.sendMessage().subscribe()` muta `isWriting` (bindeado en `*ngIf="isWriting"` del efecto typing y en `[disabled]` del botón enviar). El `this.isWriting = true` inicial y el `this.newMessage = ''` de `sendMessage()` son síncronos dentro del propio `(click)`/`(keyup.enter)` del template, no requirieron `markForCheck`. NO se tocó: `toggleVoice()`/`selectCharacter()`/`goBack()`/`toggleKokoroEngine()` (invocados directo vía `(click)` del propio template, ya cubiertos por `OnPush`); `kokoroProcessing$`/`kokoroSpeaking$`/`engineMode$`/`kokoroDownloading$`/`kokoroProgress$` (ya son `Observable`s consumidos con `| async` en el html, se auto-actualizan bajo `OnPush` sin `markForCheck`); `ngAfterViewChecked()`/`scrollToBottom()` (no leen ni mutan estado bindeado en el template, solo manipulan el scroll del contenedor vía `ElementRef` nativo). Los 3 `*ngFor` (`recentCharacters`, `messages`, `avatar.tags`) ya tenían `trackBy` (`trackByCharId`/`trackByMsgIndex`/`trackByTag`), sin cambios ahí.
  - Ningún binding, clase, estilo, texto visible ni ruta fue modificado — único cambio fue la metadata `@Component.changeDetection` y la inyección de `ChangeDetectorRef` + `markForCheck()` en los 7 puntos async listados arriba, todos sobre callbacks ya existentes (`route.paramMap`, `HttpClient.subscribe`, `setTimeout`).
  - Inventario re-verificado por grep exhaustivo (`grep -rl ChangeDetectionStrategy.OnPush --include=*.component.ts src/app`): **32 de 37** componentes ya tienen `OnPush` tras esta corrida (antes 31/37). `catalog/checkout.component.ts` sigue **excluido permanentemente** de Fase I por tocar Webpay/finance (ver corrida #27). Pendientes restantes confirmados por grep (4, de menor a mayor tamaño/riesgo): `characters/demo-chat-page/demo-chat-page.component.ts` (226), `catalog/book-detail-page/book-detail-page.component.ts` (361), `home/home.component.ts` (439), `library/reader/reader.component.ts` (2303, dejar para el final). Siguiente corrida: continuar con `characters/demo-chat-page/demo-chat-page.component.ts` (226).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — **NO DISPONIBLES esta corrida** (contenedor sin Python/Django instalado; no bloquea porque no se tocó backend, ver MR-0002). Frontend — `npm ci` OK (`node_modules` no estaba presente al iniciar; instalado desde `package-lock.json`, sin agregar dependencias nuevas). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web/jspdf, no relacionados). `npx ng test --watch=false --karma-config=karma.conf.ci.js` con un `karma.conf.ci.js` temporal creado desde cero (custom launcher `ChromeHeadlessNoSandbox` apuntando a `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`) -> **8 de 8 tests OK**. El `karma.conf.ci.js` temporal y `dist/`/`coverage/` se borraron antes del commit, no quedaron versionados.
- **Bundle**: Initial 1.34 MB raw / 275.27 kB transferencia (corrida #30: 275.27 kB; sin diferencia — `OnPush` no afecta tree-shaking/bundle size, solo estrategia de detección de cambios en runtime).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `ai-chat.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T10:10:00Z — corrida horaria #32

- **Entorno**: `git rev-parse --show-toplevel` = `/home/user/LiteratusNovelistV2`, `git remote -v` = `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar; sesión nueva en la rama del harness (`claude/admiring-noether-rxw0rg`), se cambió a la rama de trabajo correcta. `git fetch origin --prune` trajo `Chris`, `Felipe`, `eithan`, `agent/nightly-optimization`, `agent/nightly-optimization-chris` (sesión nueva sin fetch previo). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #31 (`2026-09-04T09:10:00Z`) tenía ~44 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (sin cambios desde la corrida #30/#31), confirmado con `git merge-base agent/nightly-optimization-chris origin/Chris` == ese hash y `git rev-parse origin/Chris` == ese mismo hash (no se movió). Rama de trabajo `agent/nightly-optimization-chris` ya existía en origin (no fue bootstrap).
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: contenedor sin Python/Django instalado (`ModuleNotFoundError: No module named 'django'`) — igual que corridas anteriores (ver MR-0002). No se instaló el stack backend por acotarse al lote de esta corrida, centrado en Fase I (frontend). No corresponde verificación diaria completa (`last_full_verification_utc` ya está al día de hoy, corrida #23, `2026-09-04T00:58:00Z`; hora actual 10:10 UTC, fuera de la ventana 00:00–01:00).
- **Fase y lote trabajados**: Fase I (frontend) — cursor de la corrida #31: "continuar con `characters/demo-chat-page/demo-chat-page.component.ts` (226)". Se completó ese componente en este lote.
- **Cambios aplicados** (1 componente, `ChangeDetectionStrategy.OnPush`):
  1. `characters/demo-chat-page/demo-chat-page.component.ts` — `OnPush` + `ChangeDetectorRef` inyectado + `markForCheck()` en los puntos async donde el componente muta estado leído por el template fuera de un evento de plantilla propio: (a) `ngOnInit()` — `speechService.transcript$.subscribe()` muta `inputText` y dispara `sendMessage()`; `partialTranscript$.subscribe()` muta `partialTranscript` (bindeado en `*ngIf="partialTranscript"` del panel de llamada); `audioLevel$.subscribe()` muta `audioLevel` (bindeado en `[level]` de `app-audio-visualizer`); (b) `loadAvatar()` — `next()`/`error()` de `api.get().subscribe()` mutan `avatar`/`remainingMessages`/`limitReached`/`avatarLoading`/`messages` (bindeados en `*ngIf="avatarLoading"`, `*ngIf="!avatarLoading && avatar"`, `{{ avatar.name }}`, contador de mensajes, etc., confirmado por grep en el `.html`); (c) `sendMessage()` — `next()`/`error()` de `api.post().subscribe()` mutan `messages`/`remainingMessages`/`limitReached`/`isSending` (bindeados en el `*ngFor` de mensajes, banner de límite, typing indicator); (d) `speakChatReply()` — `kokoroVoice.isSpeaking$.subscribe()` muta `isVideoSpeaking`/`activeTalkingFrame` (bindeados en `[class.speaking]` del panel manga y en `getMangaFrameUrl()`). NO se tocó: `toggleCallMode()`/`toggleMute()` (invocados directo vía `(click)` del propio template, ya cubiertos por `OnPush`); `speechService.isListening$` (ya es `Observable` consumido con `| async` en el html). Los 2 `*ngFor` (`messages`, `avatar.tags`) ya tenían `trackBy` (`trackByMsgIndex`/`trackByTag`), sin cambios ahí.
  - Ningún binding, clase, estilo, texto visible ni ruta fue modificado — único cambio fue la metadata `@Component.changeDetection` y la inyección de `ChangeDetectorRef` + `markForCheck()` en los puntos async listados arriba, todos sobre callbacks ya existentes (`Observable.subscribe`, `HttpClient.subscribe`).
  - Inventario re-verificado por grep exhaustivo (`grep -rL ChangeDetectionStrategy --include=*.component.ts src/app | grep -v spec`): quedan solo **3** componentes sin `OnPush` tras esta corrida (antes 4, sin contar `checkout`): `catalog/book-detail-page/book-detail-page.component.ts` (361), `home/home.component.ts` (439), `library/reader/reader.component.ts` (2303, alto riesgo — dejar para el final, con especial cuidado por sus suscripciones de audio/websocket/scroll). `catalog/checkout.component.ts` sigue **excluido permanentemente** por tocar Webpay/finance. Siguiente corrida: continuar con `catalog/book-detail-page/book-detail-page.component.ts` (361).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — **NO DISPONIBLES esta corrida** (contenedor sin Python/Django instalado; no bloquea porque no se tocó backend, ver MR-0002). Frontend — `npm ci` OK (`node_modules` no estaba presente al iniciar; primer intento falló por `ECONNRESET` transitorio de red, reintento OK, sin agregar dependencias nuevas). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, no relacionados). `npx ng test --watch=false --karma-config=karma.conf.ci.js` con un `karma.conf.ci.js` temporal creado desde cero (custom launcher `ChromeHeadlessNoSandbox` apuntando a `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`) -> **8 de 8 tests OK**. El `karma.conf.ci.js` temporal y `coverage/` se borraron antes del commit, no quedaron versionados.
- **Bundle**: Initial 1.34 MB raw / 275.00 kB transferencia (corrida #31: 275.27 kB; leve baja de ruido normal de hashing, sin regresión — `OnPush` no afecta tree-shaking/bundle size).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `demo-chat-page.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T10:58:12Z — corrida horaria #33
- **Entorno**: repo OK (`git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). Rama `agent/nightly-optimization-chris` creada localmente desde `origin/agent/nightly-optimization-chris` (ya existía en remoto). Commit base confirmado: `3786d288` (viene de `origin/Chris`, NO de main/Felipe/eithan — `git merge-base --is-ancestor origin/Chris origin/agent/nightly-optimization-chris` -> YES). `git status` limpio al iniciar (working tree clean, sin basura de corridas anteriores). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #32 (10:10:00Z) tenía ~43 min de antigüedad -> no había otra corrida viva, se continuó normalmente. Smoke check backend: **omitido esta corrida** (contenedor sin Python/Django instalado, no se tocó backend, ver MR-0002; ya verificado OK en corridas previas con Python disponible).
- **Push verificado**: OK — `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> "Everything up-to-date" sin error de autenticación/permiso. Además, el HEAD remoto de `agent/nightly-optimization-chris` coincide exactamente con el commit de la corrida #32 (`5966df5d`), confirmando que el push de la corrida anterior sí llegó a origin.
- **Fase y lote trabajados**: Fase I (frontend) — cursor `catalog/book-detail-page/book-detail-page.component.ts` (361 líneas) -> completado. Siguiente pendiente: `home/home.component.ts` (439 líneas).
- **Cambios aplicados**: 1 archivo — `catalog/book-detail-page/book-detail-page.component.ts`:
  1. `changeDetection: ChangeDetectionStrategy.OnPush` agregado al decorador `@Component`, con `ChangeDetectorRef` inyectado (`private cdr = inject(ChangeDetectorRef)`).
  2. `ngOnInit()`: `this.cdr.markForCheck()` tras `auth.isLoggedIn$.subscribe(...)` (muta `isLoggedIn`, bindeado en `[class.guest-btn]` y `*ngIf="isLoggedIn && isOwned"` del formulario de reseña).
  3. `loadBookDetails()`: `markForCheck()` en `next()` y `error()` del `api.get(...)` (mutan `book`, `isOwned`, `userInkBalance`, `avgRating`, `displayAvatars`, `isLoading`, `errorMsg`, bindeados en casi toda la vista: hero, avatares, reseñas).
  4. `speakSynopsis()`: `markForCheck()` en `ttsUtterance.onstart/onend/onerror` (mutan `isTalking`, bindeado en el botón "Escuchar Sinopsis"; los eventos de `SpeechSynthesisUtterance` no están garantizados dentro de la zona de Angular por zone.js).
  5. `confirmPurchase()`: `markForCheck()` en `next()` y `error()` del `api.post(...)` (mutan `isOwned`, `book.inventory_id`, `userInkBalance`, `purchaseLoading`, `showPurchaseModal`).
  6. `submitReview()`: `markForCheck()` en `next()` y `error()` del `api.post(...)` (mutan `isSubmittingReview`, `book.reviews`, `avgRating`, `reviewComment`, `reviewRating`, `reviewErrorMsg`).
  - No se tocó `route.paramMap.subscribe` (solo fija `slug`, no bindeado en template) ni `startAutoScroll()`/`scrollCarousel()` (manipulan `scrollLeft` del DOM directamente vía `ViewChild` dentro de `zone.runOutsideAngular`, no mutan estado bindeado por Angular) ni los handlers `(click)` propios del template (`selectAvatar`, `closeAvatarModal`, `cancelPurchase`, `setRating`, `downloadPDF`), que ya disparan detección de cambios al originarse en la propia vista bajo OnPush. El template ya usaba `trackBy` en todos los `*ngFor` (sin cambios necesarios ahí).
  - Sin cambios visuales: no se tocó el `.html` ni el `.css`.
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: backend — **NO DISPONIBLES esta corrida** (contenedor sin Python/Django instalado; no bloquea porque no se tocó backend, ver MR-0002). Frontend — `npm ci` OK (`node_modules` no estaba presente al iniciar; 1105 paquetes instalados desde `package-lock.json`, sin agregar dependencias nuevas). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, no relacionados con este cambio). `npx ng test --watch=false --karma-config=karma.conf.ci.js` con un `karma.conf.ci.js` temporal creado desde cero (este repo no trae `karma.conf.js` propio en el árbol; launcher `ChromeHeadlessNoSandbox` apuntando a `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`) -> **8 de 8 tests OK**. El `karma.conf.ci.js` temporal, `coverage/` y `dist/` se borraron antes del commit, no quedaron versionados.
- **Bundle**: Initial 1.34 MB raw / 275.10 kB transferencia (corrida #32: 275.00 kB; +0.10 kB, dentro del ruido normal de hashing por los imports agregados — `OnPush`/`ChangeDetectorRef` no afecta tree-shaking de forma apreciable, sin regresión real).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `book-detail-page.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T12:05:00Z — corrida horaria #34
- **Entorno**: `git rev-parse --show-toplevel` -> `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`. Rama `agent/nightly-optimization-chris` creada localmente desde `origin/agent/nightly-optimization-chris` (ya existía en remoto, 33 commits por delante de `origin/Chris`, cero commits de `Chris` sin mergear). Commit base confirmado: `3786d288` (viene de `origin/Chris`, NO de main/Felipe/eithan — `git merge-base origin/agent/nightly-optimization-chris origin/Chris` -> `3786d288`, y `git log origin/agent/nightly-optimization-chris..origin/Chris` vacío, o sea Chris no tiene nada que esta rama no tenga ya). `git status` limpio al iniciar (working tree clean). Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #33 (10:58:12Z) tenía ~56 min de antigüedad -> no había otra corrida viva, se continuó normalmente. Rama vieja `agent/nightly-optimization` (basada en Felipe/e794ef95) confirmada intacta y NO tocada, NO usada como base.
- **Push verificado**: OK — `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> "Everything up-to-date" sin error de autenticación/permiso.
- **Fase y lote trabajados**: Fase I (frontend) — cursor `home/home.component.ts` (439 líneas) -> completado. Único pendiente real restante: `library/reader/reader.component.ts` (2303 líneas, alto riesgo).
- **Cambios aplicados**: 1 archivo — `home/home.component.ts`:
  1. `changeDetection: ChangeDetectionStrategy.OnPush` agregado al decorador `@Component` (import de `ChangeDetectionStrategy` agregado). `ChangeDetectorRef` ya estaba inyectado como `this.cdr`.
  2. `loadShowcaseAvatars()`: `markForCheck()` en `next()` y `error()` del `api.get('ai/hub/avatars/...')` (mutan `avatarsLoading`/`showcaseAvatars`, bindeados en la sección "Conoce a los personajes": skeleton, carrusel y estado vacío).
  3. `loadBooks()`: `markForCheck()` en `next()` y `error()` del primer `api.get('catalog/books/...')` (mutan `allBooks`/`isLoading` y disparan `buildSections()` que fija `trendingBooks`/`discoveryBooks`/`randomDiscoveryBooks`, bindeados en Tendencias, Descubrimiento y el skeleton de carga).
  4. `loadBooks()`: `markForCheck()` en `next()` y `error()` del segundo `api.get('catalog/books/recommendations/')` (muta `recommendedBooks`, bindeado en la sección Recomendados).
  - `loadAIBooks()` ya tenía `this.cdr.detectChanges()` propio en `next()` (sigue funcionando igual bajo OnPush; su `error()` solo hace `console.error`, sin mutar estado bindeado, no necesitó cambio).
  - Verificado por grep que `demoMessages`/`demoInput`/`demoSending`/`demoRemainingMessages`/`demoChatLimitReached`/`demoAvatarName`/`demoAvatarImage`/`activeCharacterIndex`/`totalBooksCount` NO están bindeados en `home.component.html` (código muerto preexistente, fuera de alcance de Fase I: no se borra ni se investiga más).
  - Todos los `*ngFor` del template ya usaban `trackBy` de una corrida anterior; sin cambios ahí. Sin cambios en `.html` ni `.css`.
  - Verificación por grep (`ChangeDetectionStrategy.OnPush` en `*.component.ts` bajo `frontend/src/app`): **35 de 37** componentes con OnPush (antes 34/37). Quedan 2: `catalog/checkout/checkout.component.ts` (97 líneas, EXCLUIDO PERMANENTE por Webpay/finance) y `library/reader/reader.component.ts` (2303 líneas, alto riesgo, único pendiente real).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas sin cambios.
- **Pruebas**: Backend — Python 3.11.15 disponible en este contenedor pero Django **NO instalado** (`ModuleNotFoundError: No module named 'django'`); no se instaló (requeriría todo `requirements.txt`, fuera del lote acotado de esta corrida centrada en frontend) y no se tocó backend, por lo que no bloquea (ver MR-0002, sin cambios). Frontend: `npm ci` OK (`node_modules` no estaba presente al iniciar; 1105 paquetes instalados desde `package-lock.json`, sin agregar dependencias nuevas). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, no relacionados con este cambio). `npx ng test --watch=false --karma-config=karma.conf.ci.js` con `karma.conf.ci.js` temporal creado desde cero (launcher `ChromeHeadlessNoSandbox` -> `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`) -> **8 de 8 tests OK**. `karma.conf.ci.js`, `coverage/` y `dist/` borrados antes del commit, no quedaron versionados.
- **Bundle**: Initial 1.34 MB raw / 275.09 kB transferencia (corrida #33: 275.10 kB; -0.01 kB, sin regresión).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `home.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T13:05:00Z — corrida horaria #35

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`. `git status` limpio al iniciar. Guarda de solapamiento: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #34 (12:05Z) tenía ~49 min de antigüedad -> se procedió sin conflicto. Rama `agent/nightly-optimization-chris` existía en origin -> `git checkout agent/nightly-optimization-chris` + `git pull --ff-only origin agent/nightly-optimization-chris` (ya actualizado, sin cambios nuevos).
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado igual al `base_commit` del STATE y coincide con `origin/Chris` actual (sin cambios nuevos en `Chris` desde la corrida anterior). HEAD de la rama de trabajo al iniciar: `31802f790f367f2e914ef53e52674ab2448ab96e` (commit de la corrida #34).
- **Push verificado (sección 2.9)**: `git remote -v` OK; `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` (**OK**, sin errores de autenticación/permiso).
- **Smoke check (Fase A)**: este contenedor SÍ trae `python3.12` (`/usr/bin/python3.12`, 3.12.3) además de `python3.11`. Se creó un venv aislado `backend/.venv312/` (gitignorado) e instaló `requirements.txt` completo (`pip install -r requirements.txt`) sin errores. Se generó un `.env` local **gitignorado** (no versionado, sin credenciales reales) con `DATABASE_URL=sqlite:///tmp_smoke_check.sqlite3` (también gitignorado por `*.sqlite3`) solo para poder cargar `settings.py`. Con eso: `python -X utf8 manage.py check` -> `System check identified no issues (0 silenced)`; `python -X utf8 manage.py makemigrations --check --dry-run` -> `No changes detected`. Esto actualiza **MR-0002**: el bloqueo real no es la falta de Python/Django (sí están disponibles y funcionan) sino la falta de un dump/BD real de producción para las Fases B-H.
- **Fase y lote trabajados**: Fase I (frontend) — en vez de abordar `reader.component.ts` (2303 líneas, alto riesgo, se mantiene diferido), se trabajó el ítem de Fase I "suscripciones RxJS sin unsubscribe/async pipe": auditoría de los 25 componentes con `.subscribe()` y sin `ngOnDestroy`, filtrando por `grep` cuáles suscriben a observables de larga duración (no completan solos) vs. llamadas HTTP de un solo disparo (que sí completan solas, no son fuga real).
- **Cambios aplicados**:
  - `dashboard/dashboard-layout/dashboard-layout.component.ts` — **CORREGIDO**: 2 `subscribe()` en `ngOnInit` (`router.events` filtrado a `NavigationEnd`, y `settingsService.currentTheme$`) sin limpieza. Este componente se crea/destruye cada vez que un admin entra y sale de `/dashboard` (módulo lazy), por lo que cada visita dejaba una suscripción huérfana acumulándose durante toda la sesión del navegador. Se agregó `OnDestroy`, un campo `private subscriptions = new Subscription()` que agrupa ambas suscripciones, y `unsubscribe()` en `ngOnDestroy()`. Sin cambios de DOM/estilos.
  - `library/tavern/tavern.component.ts` — **CORREGIDO**: el `setInterval` de la cuenta regresiva de `watchAd()` (5s) se autolimpiaba con `clearInterval` al llegar a 0, pero si el usuario navegaba fuera del componente a mitad del conteo, el interval seguía vivo referenciando un componente ya destruido (llamando `this.cdr.markForCheck()` sobre él). Se agregó `OnDestroy`, el campo `adIntervalId` para rastrear el interval activo, y `clearInterval` en `ngOnDestroy()`. Sin cambios de DOM/estilos.
  - `app.component.ts` (`router.events` sin unsubscribe) — **SIN CAMBIO**: es el componente raíz, nunca se destruye durante la vida de la app, por lo que no es una fuga real; agregar cleanup ahí sería ruido sin beneficio.
- **Fase J (revisión, sin cambios)**: se confirmó que el `import()` dinámico de las librerías pesadas (`phaser` sin uso real en el código, `onnxruntime-web`/`kokoro-js` vía `kokoro-tts.service.ts`, `@huggingface/transformers` solo dentro de un Web Worker que ya es un chunk separado, `jspdf`/`lottie-web`) ya estaba completo desde corridas anteriores. Se revisó `app-routing.module.ts`: todas las rutas de nivel superior (`home`, `catalog`, `authors`, `characters`, `book/:slug`, `reader/:id`, etc., salvo `dashboard` que ya usa `loadChildren`) siguen con `component: X` eager porque los componentes **no son standalone** (sin `standalone: true`, declarados vía `NgModule`); convertir a `loadComponent` exige primero convertir cada componente a standalone (revisar imports explícitos de cada pipe/directiva usada en cada template, módulos compartidos) — refactor grande y riesgoso, **fuera del alcance de un lote acotado de ~25 min**, no se tocó, queda anotado para una corrida dedicada futura.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno nuevo. MR-0002 (actualizada, ver arriba) y MR-0003 siguen abiertas.
- **Pruebas**: Frontend — `npm ci` OK (`node_modules` no estaba presente al iniciar; 1105 paquetes instalados desde `package-lock.json`, sin agregar dependencias nuevas). `npx ng build --configuration production` -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web, no relacionados con este cambio). `npx ng test --watch=false --karma-config=karma.conf.ci.js` con `karma.conf.ci.js` temporal creado desde cero (launcher `ChromeHeadlessCI` -> `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, `--no-sandbox`) -> **8 de 8 tests OK**. `karma.conf.ci.js`, `coverage/` y `dist/` borrados antes del commit, no quedaron versionados. Backend — `manage.py check` y `makemigrations --check --dry-run` OK (ver Smoke check arriba); no se corrió `manage.py test` completo porque no hubo cambios de código/datos backend esta corrida (sección 6).
- **Bundle**: Initial 1.34 MB raw / 275.15 kB transferencia (corrida #34: 275.09 kB; +0.06 kB por el código nuevo de `OnDestroy`/`Subscription`, sin regresión relevante).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `dashboard-layout.component.ts` y `tavern.component.ts`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T14:05:00Z — corrida horaria #36

- **Entorno**: repo OK (`git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Guarda de solapamiento: `run_in_progress` era `false`, `heartbeat_utc` de la corrida #35 (13:05 UTC) -> se procedió sin conflicto.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado igual al `base_commit` del STATE y a `origin/Chris` actual (sin cambios nuevos en `Chris` desde la corrida anterior). Rama `agent/nightly-optimization-chris` existía en origin -> `git checkout -B agent/nightly-optimization-chris origin/agent/nightly-optimization-chris`.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> **OK** ("Everything up-to-date"). Push verificado antes de trabajar.
- **Smoke check (Fase A)**: venv `python3.12 -m venv .venv312` + `pip install -r requirements.txt` OK, `.env` local gitignorado (SQLite temporal, sin credenciales reales). `manage.py check` -> `System check identified no issues (0 silenced)`. `makemigrations --check --dry-run` -> `No changes detected`.
- **Fase y lote trabajados**: Fase I (frontend) — cierre del lote de auditoría de fugas RxJS ("subscribe sin unsubscribe/async pipe") que la corrida #35 dejó pendiente. Se revisaron los 21 componentes restantes de la lista.
- **Cambios aplicados** (6 componentes corregidos):
  - `ai-chat.component.ts` — `route.paramMap.subscribe()` y `chatService.messages$.subscribe()` en `ngOnInit` sin cleanup (el segundo es un `Subject` de servicio de larga duración, fuga real acumulativa por sesión de chat); se agregó `OnDestroy` + `Subscription` (mismo patrón que `dashboard-layout`/`tavern` de corridas anteriores). El resto de `.subscribe()` del archivo (getAvatar, getSession, loadSessionMessages, getRecentAvatars, sendMessage) son `HttpClient` de un solo disparo, no son fugas.
  - `author-detail-page.component.ts` — `route.paramMap.subscribe()` sin cleanup; como el componente se reutiliza al navegar entre `/autores/:slug` distintos (Angular no lo destruye en ese caso), no se puede usar `take(1)` sin romper la recarga de datos al cambiar de autor; se aplicó `Subscription` + `OnDestroy`.
  - `payment-failure.component.ts`, `payment-success.component.ts`, `verify-email.component.ts`, `reset-password.component.ts` — las 4 leen `route.queryParamMap`/`queryParams` una sola vez al entrar a una página terminal (resultado de pago, verificación de email, reset de contraseña); no hay navegación interna que cambie los query params sin recargar el componente. Se aplicó `take(1)` de `rxjs/operators` en vez de `Subscription`+`OnDestroy`: fix mínimo, sin boilerplate extra, libera la suscripción automáticamente tras la primera emisión.
- **Revisados sin cambio** (15 componentes, sin fuga real — todos usan `HttpClient` de un solo disparo o `route.snapshot.paramMap.get`, no `subscribe` a observables de larga duración): `book-editor.component.ts` (revisado en detalle, 9 `subscribe`: loadAuthors, loadGenres, getBookDetail, parseEpub, deleteBook, uploadChapterImage, saveBook, saveAvatar, deleteAvatar — todos disparados por acciones puntuales del usuario o `ngOnInit`, todos completan solos), `author-list`, `profile`, `forgot-password`, `register`, `login`, `library-list`, `categories`, `overview`, `author-editor`, `users`, `avatars`, `avatar-editor`, `authors`, `books`.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 y MR-0003 siguen abiertas (sin cambios en su contenido salvo confirmación repetida de MR-0002).
- **Pruebas**: `npm ci` (node_modules no estaba presente, 157 paquetes) + `npx ng build --configuration production` -> exit 0, 0 errores TypeScript (solo warnings preexistentes de CommonJS en `canvg`/`lottie-web`, no relacionados con esta corrida) -> **bundle initial 275.18 kB** (vs 275.15 kB corrida #34/#35, +0.03 kB por el código de cleanup en 6 archivos, sin regresión relevante). `npx ng test --watch=false` con `karma.conf.ci.js` temporal (Chrome headless de Playwright en `/opt/pw-browsers/chromium-1194`, flags `--no-sandbox --disable-gpu --disable-dev-shm-usage`) -> **8/8 SUCCESS**. `karma.conf.ci.js`, `coverage/` y `dist/` borrados al terminar. Backend: `manage.py check` + `makemigrations --check --dry-run` OK (ver Smoke check); no se corrió `manage.py test` completo porque no hubo cambios de código/datos backend esta corrida (regla de sección 6).
- **Bundle initial**: 275.15 kB -> 275.18 kB (+0.03 kB, sin regresión).
- **Commit**: pendiente de crear en este mismo lote (ver commit siguiente en el historial de git).
- **Push confirmado en origin**: pendiente de confirmar tras el push de este lote (ver siguiente entrada de log o resultado de `git ls-remote`).

## 2026-09-04T15:01:30Z — corrida horaria #37

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar. Guarda de solapamiento: `run_in_progress` era `false`, `heartbeat_utc` de la corrida #36 (14:05:00Z) tenía ~56 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado igual al `base_commit` del STATE y a `origin/Chris` actual (sin cambios nuevos en `Chris`). Rama `agent/nightly-optimization-chris` existía en origin -> `git checkout agent/nightly-optimization-chris` (ya al día con `origin/agent/nightly-optimization-chris`, HEAD `b0a01910`, commit de la corrida #36).
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: venv `python3.12 -m venv .venv312` + `pip install -r requirements.txt` OK (venv/`.env` no persisten entre corridas, contenedor gitignorado). `.env` local gitignorado recreado (SQLite temporal, sin credenciales reales). `manage.py check` -> `System check identified no issues (0 silenced)`. `makemigrations --check --dry-run` -> `No changes detected`.
- **Fase y lote trabajados**: se cerró el último pendiente real de **Fase I** (auditoría completa de `reader.component.ts`, confirmando que ya está libre de fugas RxJS y que sus dos exclusiones de OnPush quedan documentadas como permanentes), se revisó **Fase J** (presupuestos de `angular.json`, sin cambios) y se abrió por primera vez **Fase H** (backend/API), que hasta ahora no se había tocado nunca.
- **Cambios aplicados**:
  - `catalog/views.py` (`GenreViewSet`) — **CORREGIDO** (Fase H, "cabeceras de cache en respuestas cacheables"): se agregaron overrides `list()`/`retrieve()` que llaman a `super()` y fijan `response['Cache-Control'] = 'public, max-age=300'`, sin tocar el payload/formato de respuesta. El catálogo de géneros solo cambia cuando el Dashboard crea/edita uno (poco frecuente), por lo que es un candidato seguro para cache de navegador/CDN. No afecta el M2M `book_count` annotation ni el resto del ViewSet (create/update/destroy sin tocar).
- **Revisado sin cambio (Fase I, cierre)**: `library/reader/reader.component.ts` (2303 líneas) — auditado línea por línea por primera vez en vez de seguir diferido "por riesgo": ya tiene `destroy$` (Subject) + `takeUntil(this.destroy$)` correctamente aplicado en las 20 suscripciones de larga duración (`audioService.currentWordIndex$`, `kokoroVoice.*`, `wasmVoice.currentWordIndex$`, `speechService.*`, `assistedReading.*`, `chatService.inkBalance$`, `audioService.chapterEnd$`, etc.), y `ngOnDestroy()` ya hace `destroy$.next()` + `destroy$.complete()` + `avatarsRequestSub?.unsubscribe()`. Los ~6 `.subscribe()` restantes sin `takeUntil` (`purchase_narration`, `audioService.playRecorded` ×2, `startChat`, `loadChatHistory`, `sendMessage`) son llamadas `HttpClient`/Observable de un solo disparo disparadas por acción del usuario; se verificó en `audio.service.ts` que `playRecorded()` llama `observer.complete()` apenas arranca la reproducción (`this.proAudio.play().then(() => { ...; observer.complete(); })`), confirmando que no son fugas. **SIN CAMBIO — no había fuga real que corregir.** OnPush: `reader.component.ts` y `checkout.component.ts` son los únicos 2 de 37 componentes sin `ChangeDetectionStrategy.OnPush`; se mantienen como **exclusión permanente** (reader: alto riesgo real de romper el resaltado de palabras en vivo durante la narración, que sí es comportamiento visible; checkout: regla explícita de no tocar finance/Webpay) — no se tocan en corridas futuras salvo que el dueño pida explícitamente abordarlas como proyecto propio con pruebas manuales en navegador. `*ngFor` sin `trackBy` en todo `frontend/src`: 0 encontrados (grep completo). `lucide-angular` vs `@lucide/angular` en `package.json`: solo existe `@lucide/angular`, no hay duplicado que unificar.
- **Revisado sin cambio (Fase J)**: presupuestos de `angular.json` (`budgets.initial`: warning 1.6mb / error 2.5mb). Se verificó con un build real de esta corrida que Angular mide esto sobre **bytes raw**, no sobre el tamaño de transferencia (gzip) que se venía reportando en corridas anteriores como "275 kB": el raw actual es 1.34 MB, es decir ya al 84% del warning — el presupuesto no está tan holgado como parecía y ya cumple su función de alarma temprana con un margen razonable (~260 KB). Se decidió no tocarlo para no arriesgar falsos positivos de build.
- **Revisado (Fase H, primera vez)**: `catalog/views.py` (`AuthorViewSet`, `BookViewSet`), `library/views.py` (`UserInventoryViewSet`, `ReadingProgressViewSet`, `UserBookmarkViewSet`) y `ai_engine/views.py` (`AvatarListView`, `AvatarDetailView`, `GlobalAvatarListView`, `RecentChatsView`): en general ya están bien optimizados (select_related/prefetch_related con comentarios explicando cada decisión, paginación global vía `StandardResultsSetPagination` en `settings.REST_FRAMEWORK`). Se detectó `GlobalAvatarListView` (`GET /api/v1/ai/hub/avatars/`) sin paginar (devuelve TODOS los `AIAvatar`) — ver MR-0004 abajo, no corregido esta corrida por requerir coordinación con el consumidor frontend.
- **NEEDS MANUAL REVIEW nuevos**: **MR-0004** (Fase H) — `ai_engine/views.py::GlobalAvatarListView` (`GET /api/v1/ai/hub/avatars/`) devuelve todos los avatares sin paginar; el fix directo (usar `StandardResultsSetPagination`) cambia la forma de la respuesta (array plano -> `{count,next,previous,results}`) y rompería el Hub del frontend si no se actualiza el consumidor en el mismo lote, cosa que no se puede probar visualmente en este entorno sin navegador+BD real. Se necesita del dueño: autorizar el cambio de contrato + el ajuste correspondiente en el frontend, o preferir una alternativa sin romper compatibilidad (límite opcional vía query param). MR-0002 actualizada (aclara que Fase H sí es abordable sin BD real, a diferencia de B-D). MR-0003 sin cambios (sigue abierta desde la corrida #22).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **Pruebas**: Backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (ver Smoke check), `manage.py test` **completo** (67/67 tests OK, incluye la app `catalog` donde está el cambio de `GenreViewSet`) por tratarse de un cambio de código backend (regla de sección 6). Frontend — no se modificó código frontend esta corrida, así que no se corrieron los tests de Karma (regla de sección 6); sí se corrió `npm ci` (node_modules no estaba presente, 1105 paquetes) + `npx ng build --configuration production` como parte de la revisión de Fase J -> exit 0, 0 errores, mismos warnings preexistentes de CommonJS (canvg/lottie-web) -> bundle idéntico a la corrida #36.
- **Bundle initial**: 275.18 kB -> 275.18 kB (transferencia, sin cambio; 1.34 MB raw, sin cambio — no se tocó código frontend esta corrida).
- **Commit**: pendiente de crear en este mismo lote (ver commit siguiente en el historial de git; incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `catalog/views.py`).
- **Push confirmado en origin**: pendiente de confirmar tras el push de este lote (ver informe final de esta corrida).

## 2026-09-04T15:55:00Z — corrida horaria #38

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar. Guarda de solapamiento: `run_in_progress` era `false`, `heartbeat_utc` de la corrida #37 (15:01:30Z) tenía ~52 min de antigüedad -> sin corrida viva, se continuó normal.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado igual al `base_commit` del STATE y a `origin/Chris` actual (`git log agent/nightly-optimization-chris..origin/Chris` vacío, Chris no tiene nada nuevo que esta rama no tenga ya). Rama `agent/nightly-optimization-chris` existía en origin -> `git checkout -B agent/nightly-optimization-chris origin/agent/nightly-optimization-chris` (HEAD `a8b59fbc`, commit de la corrida #37). Rama vieja `agent/nightly-optimization` confirmada intacta (`e794ef95`, basada en Felipe) — NO tocada, NO usada como base.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check (Fase A)**: venv `python3.12 -m venv .venv312` + `pip install -r requirements.txt` OK (venv/`.env` no persisten entre corridas, gitignorados). `.env` local gitignorado recreado (SQLite temporal, sin credenciales reales). `manage.py check` -> `System check identified no issues (0 silenced)`. `makemigrations --check --dry-run` -> `No changes detected`.
- **Fase y lote trabajados**: Fase H (backend/API) — continuación de la corrida #37. Se revisó `ai_engine/serializers.py` completo (sugerido explícitamente por el lote anterior) y se auditaron de nuevo `ai_engine/views.py` y `catalog/views.py` en busca de N+1 y cabeceras de cache faltantes.
- **Cambios aplicados** (2 archivos):
  1. `ai_engine/views.py` (`AvatarDetailView.get`, línea ~74) — **CORREGIDO**: el fetch `get_object_or_404(AIAvatar, pk=pk)` no tenía `select_related`, pero `GlobalHubAvatarSerializer` (usado por esta vista) accede a `edition.book.title` y `edition.book.slug`, generando 2 consultas extra evitables por cada request. Se cambió a `get_object_or_404(AIAvatar.objects.select_related('edition__book'), pk=pk)`. Sin cambios de payload/formato de respuesta.
  2. `catalog/views.py` (`AuthorViewSet`) — **CORREGIDO**: es un `ReadOnlyModelViewSet` de un catálogo que cambia solo cuando el Dashboard edita un autor (mismo patrón que `GenreViewSet`, corregido en la corrida #37, que ya tenía este fix). Se agregaron overrides `list()`/`retrieve()` idénticos que envuelven `super()` y fijan `response['Cache-Control'] = 'public, max-age=300'`, sin tocar el payload.
- **Revisado sin cambio**: `ai_engine/serializers.py` (138 líneas, revisión completa) — `AIAvatarListSerializer` y `GlobalHubAvatarSerializer` no exponen `system_prompt`, `temperature`, `behavioral_context`, `sample_dialogues` ni ningún campo sensible (quedan fuera de `Meta.fields`); sin sobre-exposición, sin cambios necesarios. `catalog/views.py` `BookViewSet` — revisado de nuevo: ya tiene `prefetch_related`/`Prefetch` bien pensado con comentarios explicando cada decisión (queryset compacto vs. detalle); se decidió **no** agregarle `Cache-Control` porque es el listado más dinámico del catálogo (`is_featured`, `ordering=?`, búsqueda por título/sinopsis/autor/género) y cachearlo agresivamente sí tiene riesgo real de servir datos desactualizados tras ediciones frecuentes desde el Dashboard — decisión técnica documentada, no requiere dueño. `library/views.py` (`UserInventoryViewSet`, `ReadingProgressViewSet`, `UserBookmarkViewSet`) — revisado de nuevo sin hallazgos nuevos (son endpoints de datos privados por usuario autenticado, no cacheables públicamente).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real, ver MR-0002).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002, MR-0003 y MR-0004 siguen abiertas sin cambios.
- **Pruebas**: Backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (ver Smoke check), `manage.py test` **completo** (67/67 tests OK, incluye `catalog` y `ai_engine` donde están los cambios) por tratarse de cambios de código backend (regla de sección 6). Frontend — no se modificó código frontend esta corrida, no se corrieron `npm ci`/build/Karma (regla de sección 6).
- **Bundle**: sin cambios (no se tocó frontend esta corrida).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `ai_engine/views.py` y `catalog/views.py`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T16:53:00Z — corrida horaria #39

- **Entorno**: repo OK (`git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Guarda de solapamiento: `run_in_progress` era `false` (última corrida #38 terminó limpia, heartbeat de hace ~54 min) -> se continuó sin problema.
- **Rama y commit base**: `agent/nightly-optimization-chris` ya existía en origin (no bootstrap); se hizo checkout y `git pull --ff-only` (ya estaba al día). Commit base confirmado: `3786d288` (`origin/Chris` es ancestro de HEAD, verificado con `git merge-base --is-ancestor`).
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> **OK** ("Everything up-to-date").
- **Smoke check**: entorno recreado desde cero (contenedor nuevo, sin `.venv312` persistido). Se creó venv local `.venv312/` con `python3.12`, se instaló `requirements.txt` completo sin errores, y `.env` local (SQLite temporal, sin credenciales reales, gitignorado). `manage.py check` -> OK (0 issues).
- **Fase y lote trabajados**: Fase H (backend/API), continuando el lote sugerido por la corrida #38: se revisó `library/views.py` completo (todos sus endpoints — `UserInventoryViewSet`, `ReadingProgressViewSet`, `UserBookmarkViewSet` — son datos privados por usuario, ya paginados donde corresponde o naturalmente pequeños; sin hallazgos nuevos) y `core/views.py` (solo `StoreSettingsView`, vista de un único objeto singleton, no es un listado — no aplica paginación). Se revisó también `catalog/views.py` `BookViewSet` de nuevo para la duda dejada por la corrida #37 sobre `Cache-Control` corto en `list()`: se mantiene la decisión de NO aplicarlo (el listado admite `ordering=?` aleatorio, filtros dinámicos y es el catálogo más editado desde el Dashboard — sigue siendo una decisión de riesgo que amerita más evaluación, no un ajuste de bajo riesgo de este lote). Se revisó `ai_engine/views.py` completo en busca de más patrones N+1 iguales al corregido en la corrida #38.
- **Cambios aplicados** (1 archivo de código):
  - `ai_engine/views.py` `ChatSessionView.get()` (línea ~160): `get_object_or_404(AIAvatar, id=avatar_id)` no traía `select_related('edition')`, pero pocas líneas después se accede a `avatar.edition` (`UserInventory.objects.filter(..., edition=avatar.edition)`), lo que dispara una query extra evitable para cargar el objeto `Edition` completo solo para usar su FK. Se agregó `.select_related('edition')` al queryset, sin tocar el payload de la respuesta (`ChatSessionSerializer` solo usa `avatar.name` vía `avatar_name`, no toca `edition`).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (no se trabajó Fase F).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002, MR-0003 y MR-0004 siguen abiertas sin cambios (se revisaron, ningún humano las había resuelto aún).
- **Pruebas**: Backend — `manage.py check` OK, `makemigrations --check --dry-run` OK, `manage.py test` **completo** (67/67 tests OK, incluye `ai_engine` donde está el cambio) por tratarse de un cambio de código backend (regla de sección 6). Frontend — no se modificó código frontend esta corrida, no se corrieron build/Karma (regla de sección 6).
- **Bundle**: sin cambios (no se tocó frontend esta corrida).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md` y `ai_engine/views.py`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T17:54:00Z — corrida horaria #40

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar. Guarda de solapamiento: `run_in_progress` era `false` (corrida #39 terminó limpia, `heartbeat_utc` de hace ~57 min) -> se continuó sin problema.
- **Rama y commit base**: `agent/nightly-optimization-chris` ya existía en origin (no bootstrap); se hizo `git fetch` + `git checkout agent/nightly-optimization-chris` (ya al día con `origin/agent/nightly-optimization-chris`, HEAD `40d58df`, commit de la corrida #39). Commit base confirmado: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (`origin/Chris` es ancestro de HEAD, verificado con `git merge-base --is-ancestor`; `git log origin/Chris..origin/agent/nightly-optimization-chris` no incluye commits nuevos de Chris, solo el historial de corridas horarias). Ramas `Felipe`/`agent/nightly-optimization` confirmadas intactas en `e794ef95` — no tocadas, no usadas como base.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check**: venv `python3.12 -m venv .venv312` + `pip install -r requirements.txt` OK (contenedor nuevo, sin `.venv312` persistido de corridas anteriores). `.env` local gitignorado creado (SQLite temporal, sin credenciales reales). `manage.py check` -> `System check identified no issues (0 silenced)`. `makemigrations --check --dry-run` -> `No changes detected`.
- **Fase y lote trabajados**: Fase H (backend/API), continuando el lote sugerido por la corrida #39 (revisar `finance/` solo en modo lectura) y avanzando por primera vez sobre los aspectos de **código** de Fase G (lector/audiolibro/chat IA) — los aspectos de **datos** de esa fase (alignment_data, kokoro_voice_id, content_html) siguen bloqueados sin BD real (MR-0002).
- **Cambios aplicados** (1 archivo de código + 1 migración):
  - `ai_engine/models.py` (`ChatSession.Meta.indexes`) — **CORREGIDO**: `RecentChatsView` (`GET /api/v1/ai/hub/recent/`) filtra `ChatSession` por `user` y ordena por `-updated_at`, pero el único índice compuesto existente era `(user, created_at)`; `TimeStampedModel.updated_at` no tiene `db_index` individual (a diferencia de `created_at`, que sí lo tiene). Se agregó `models.Index(fields=['user', '-updated_at'], name='ai_engine_ch_user_updated_idx')`. Se generó la migración `ai_engine/migrations/0013_chatsession_ai_engine_ch_user_updated_idx.py` (commiteada, **no aplicada a ninguna BD de producción** — solo se corrió contra la BD de pruebas en memoria de `manage.py test`).
- **Revisado sin cambio**:
  - `finance/views.py` (`initiate_payment`, `confirm_payment`, `_deliver_item`, 212 líneas) — revisado completo en modo solo lectura, respetando la regla explícita "NO tocar finance/Webpay": son endpoints de acción puntual sobre un único `Transaction` por `token`/`buy_order`, sin listados ni superficie de paginación/N+1 aplicable. Sin hallazgos, sin cambios.
  - `catalog/models.py` `ChapterAudio` — ya tiene `UniqueConstraint(fields=['chapter', 'voice_name'], name='unique_chapter_audio_voice')` a nivel de base de datos; el punto de Fase G "voces duplicadas por capítulo" ya está prevenido estructuralmente, **CONFIRMADO SIN CAMBIO**.
  - `library/views.py` `UserInventoryViewSet._serialize_chapter`/`chapters()` — ya usa `prefetch_related('audios')` tanto con `include_content=true` (para armar `chapter_audios`) como con `include_content=false` en el branch de audios (`chapter_queryset = book.chapters.all().prefetch_related('audios')`); no hay N+1 de audios en el endpoint del lector. **CONFIRMADO SIN CAMBIO**.
  - `ai_engine/views.py` completo (`ChatSessionView`, `ChatHistoryView`, `ChatInteractionView`, `DemoChatView`, `TTSGenerateView`) — revisado en busca de más patrones N+1/config rota: `ChatSessionView` ya usa `F()` atómico para `chat_count` (documentado explícitamente contra condiciones de carrera); `ChatInteractionView`/`DemoChatView` manejan errores del motor de IA sin romper la sesión ni hacer llamadas reales durante esta auditoría (solo lectura de código); `TTSGenerateView` ya usa `avatar.kokoro_voice_id or 'af_bella'` como fallback seguro. Sin hallazgos nuevos.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (Fase F sigue bloqueada por falta de BD real para las filas que referencian las imágenes, ver MR-0002; nota agregada al STATE: los archivos de imagen SÍ existen localmente en `respaldos-software/books/`, pendiente para una corrida futura de Fase F a nivel de archivo).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 (actualizada para mencionar también los datos de Fase G), MR-0003 y MR-0004 siguen abiertas sin cambios (se revisaron, ningún humano las había resuelto aún).
- **Pruebas**: Backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (ver Smoke check), `manage.py test` **completo** (67/67 tests OK, incluye `ai_engine` donde está el cambio de índice) por tratarse de un cambio de código/esquema backend (regla de sección 6). Frontend — no se modificó código frontend esta corrida, no se corrieron `npm ci`/build/Karma (regla de sección 6).
- **Bundle**: sin cambios (no se tocó frontend esta corrida).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `ai_engine/models.py` y `ai_engine/migrations/0013_chatsession_ai_engine_ch_user_updated_idx.py`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T18:53:00Z — corrida horaria #41

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar (contenedor nuevo). Guarda de solapamiento: `run_in_progress` era `false`, `heartbeat_utc` de la corrida #40 (18:15:00Z) tenía ~38 min de antigüedad -> sin corrida viva, se continuó normal.
- **Rama y commit base**: `agent/nightly-optimization-chris` ya existía en origin (no bootstrap); `git fetch origin` + `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris` (HEAD `83fee57`, commit de la corrida #40, ya al día). Commit base confirmado: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (`git merge-base --is-ancestor` confirma que `origin/Chris` es ancestro de HEAD -> viene de Chris). Ramas `Felipe`/`eithan`/`agent/nightly-optimization` (vieja, basada en Felipe `e794ef95`) confirmadas presentes en origin y NO tocadas, NO usadas como base.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` -> **push verificado: OK**.
- **Smoke check**: venv `python3.12 -m venv .venv312` + `pip install -r requirements.txt` OK (contenedor nuevo, sin `.venv312` persistido). `.env` local gitignorado creado (SQLite temporal, sin credenciales reales). `manage.py check` -> `System check identified no issues (0 silenced)`.
- **Fase y lote trabajados**: Fase H (backend/API), continuando el lote sugerido por la corrida #40 (patrones índice-vs-order_by en `catalog/models.py` y `library/models.py`); adicionalmente una primera pasada de Fase F a nivel de **archivos locales** (sin BD real, ver MR-0002), tal como quedó anotado en el STATE de la corrida #40.
- **Cambios aplicados** (1 archivo de código + 1 migración):
  - `catalog/models.py` (`Author.Meta.indexes`) — **CORREGIDO**: `AuthorViewSet` (`catalog/views.py`, líneas 51/57/75) ordena por `full_name` tanto en el queryset por defecto como en `get_queryset()` (con `pagination_class = StandardResultsSetPagination`, es decir `ORDER BY + LIMIT/OFFSET` real), pero `Author` solo tenía índice en `slug`. Se agregó `models.Index(fields=['full_name'])`. Migración generada y commiteada: `catalog/migrations/0023_author_catalog_aut_full_na_9e8869_idx.py`, **no aplicada a ninguna BD de producción** (solo corrida contra la BD de pruebas en memoria de `manage.py test`).
- **Revisado sin cambio** (confirmado que ya estaban bien indexados, sin patrón índice-vs-order_by roto):
  - `catalog/models.py` `Book` — índice en `slug` ya cubre `Book.objects.order_by("slug")` (usado en `standardization.py`); `is_published`/`is_featured`/`status` ya indexados.
  - `catalog/models.py` `Chapter` — índice compuesto `(book, order)` + `UniqueConstraint` ya cubre `chapters.order_by('order')` (lector, serializers, `standardization.py`).
  - `catalog/models.py` `Review` — índice `(book, -created_at)` ya presente.
  - `library/models.py` `ReadingProgress` — índice en `updated_at` ya presente (comentario explícito: "actividad reciente"); `UserBookmark` — índice compuesto `(inventory, created_at)` ya presente. Los filtros reales en `library/views.py` (`ReadingProgressViewSet`/`UserBookmarkViewSet`) son `filter(inventory__user=...)`, cubiertos por el índice automático de Django en la FK `UserInventory.user` — sin hallazgos nuevos.
  - `catalog/views.py` `GenreViewSet` — `order_by('-book_count', 'name')` ordena primero por una anotación (`Count`), no indexable directamente; sin cambio posible/necesario.
- **Fase F (auditoría de archivos locales, sin BD)**: se escanaron con SHA-256 los 6127 archivos de imagen (.jpg/.jpeg/.png/.webp) bajo `respaldos-software/books/` (2.0 GB, 1103 carpetas de libros) usando un script ad-hoc de solo lectura (no commiteado, es un scan puntual). Resultado: 4488 hashes únicos, 86 grupos con contenido duplicado (1725 archivos involucrados). De esos 86 grupos: **7 son duplicados dentro del mismo libro** (ej. `cover.jpg` idéntico a una copia en `images/`, esperado y sin problema) y **79 son duplicados entre libros distintos** — pero al inspeccionar el detalle, son en su enorme mayoría imágenes decorativas/logos de escaneo compartidos entre obras de dominio público de la misma fuente (ej. `Accueil_scribe.png` repetido en 5 libros distintos de autores distintos; `Logo.jpg`/`*_logo.jpg` repetido en 8 novelas de Pérez Galdós; ilustraciones idénticas entre las 4 obras de teatro de Victor Hugo `ruy-blas`/`lucrecia-borgia`/`la-esmeralda`/`maria-tudor`, que comparten edición/escaneo), NO evidencia de que sean `Book.cover_image` mal asignada entre libros — son archivos internos de las carpetas `images/` de cada EPUB, no necesariamente los mismos que apunta el campo `cover_image` en la BD. **No se hizo ninguna corrección** (no hay filas de BD real para saber qué campo apunta a cuál archivo, y "elegir cuál registro conserva la imagen" está explícitamente fuera de alcance sin ambigüedad resuelta). Esto NO abre un MR nuevo porque ya está cubierto por MR-0002 (sin BD real no se puede verificar si `Book.cover_image`/`Author.photo` realmente duplican estos archivos); queda como contexto adicional para cuando haya BD real.
- **Tabla de duplicados de imágenes**: ver detalle completo arriba (Fase F); resumen: 6127 imágenes escaneadas / 4488 hashes únicos / 86 grupos duplicados (7 intra-libro esperados, 79 inter-libro — mayormente logos/decoraciones de escaneo compartidas, no confirmable como duplicado de `cover_image` de BD sin datos reales).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002, MR-0003 y MR-0004 siguen abiertas sin cambios (se revisaron, ningún humano las había resuelto aún).
- **Pruebas**: Backend — `manage.py check` OK, `makemigrations --check --dry-run` OK (`No changes detected` tras generar la migración 0023), `manage.py test` **completo** (67/67 tests OK, incluye `catalog` donde está el cambio de índice) por tratarse de un cambio de código/esquema backend (regla de sección 6). Frontend — no se modificó código frontend esta corrida, no se corrieron `npm ci`/build/Karma (regla de sección 6).
- **Bundle**: sin cambios (no se tocó frontend esta corrida).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye `NIGHT_AUDIT_STATE.json`, `NIGHT_AUDIT_LOG.md`, `catalog/models.py` y `catalog/migrations/0023_author_catalog_aut_full_na_9e8869_idx.py`).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T19:59:39Z — corrida horaria #42

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar (contenedor nuevo). Guarda de solapamiento: `run_in_progress` era `false`, `heartbeat_utc` de la corrida #41 (19:03:00Z) tenía ~56 min de antigüedad -> sin corrida viva, se continuó normal.
- **Rama y commit base**: `agent/nightly-optimization-chris` ya existía en origin (no bootstrap); `git fetch origin` + `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris` (HEAD `aef54c9`, commit de la corrida #41, ya al día — se omitió `pull --ff-only` explícito por ser checkout directo del remoto). Commit base confirmado: `3786d288fd23e4228b6c0390ac00ee9cf6533940` (`git merge-base origin/Chris origin/agent/nightly-optimization-chris` devuelve exactamente ese hash -> confirma que viene de Chris). Ramas `Chris` (`3786d28`), `Felipe`, `eithan` y la rama vieja `agent/nightly-optimization` (`e794ef9`, basada en Felipe) confirmadas presentes en origin y NO tocadas, NO usadas como base.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` (exit 0) -> **push verificado: OK**.
- **Smoke check**: venv `python3.12 -m venv .venv312` + `pip install -r requirements.txt` OK (contenedor nuevo, sin `.venv312` persistido). `.env` local gitignorado creado (SQLite temporal, sin credenciales reales; confirmado con `git status` que no queda trackeado). `manage.py check` -> `System check identified no issues (0 silenced)`. `makemigrations --check --dry-run` -> `No changes detected`.
- **Fase y lote trabajados**: `H_backend_api` (cierre) -> `D_autores` / `E_sinopsis_categorias_portadas`, siguiendo la sugerencia explícita de la corrida #41: primera pasada sobre los datos locales versionados en `backend/` que alimentan `Book.synopsis`/`Author.bio` vía `scripts/inject_ai_data.py` (100% local, leído y confirmado que no llama a ningún LLM/API externa antes de citarlo).
- **Cambios aplicados**: ninguno (0 archivos de código/datos modificados). Los 5 archivos JSON auditados ya estaban en UTF-8 limpio, sin duplicados masivos ni truncamientos — no había nada de bajo riesgo que corregir.
- **Auditoría realizada** (lote completo, 5 archivos / 3360 entradas revisadas):
  - `backend/synopses_backup.json` (dict `book_id`->sinopsis, 1649 entradas): 0 mojibake, 0 duplicadas exactas entre libros, 0 con artefactos de scraping (`Leer más`, `©`, `<p>`, `&amp;`, etc.). 1 hallazgo: `book_id 9f05f58d-cffd-4380-8136-9716036f2fa3` = `"Sinopsis no disponible para esta obra."` (placeholder real) -> **MR-0006 nueva**.
  - `backend/synopses_1046_backup.json` (lista de 1046 objetos `{id,title,slug,authors,synopsis}`): 0 mojibake, 0 duplicadas, 0 cortas (<40 caracteres), 0 con espacios dobles/no-break/zero-width.
  - `backend/biographies_backup.json` (dict `author_id`->bio, 288 entradas): 0 mojibake, 0 bios vacías/<30 caracteres. 1 hallazgo: los `author_id` `78c8743b-0b0e-4508-b1d4-07e9c5697b1a` y `fcbbc864-0fcf-43d0-8347-5bd05765be4a` comparten exactamente el mismo texto (biografía de Santa Teresa de Jesús) -> posible autor duplicado con dos IDs, no fusionable sin ver la BD real -> **MR-0005 nueva**.
  - `backend/author_prompts.json` (358 entradas, prompts de generación de imagen para avatares — se confirmó que son solo texto de prompt, no se ejecuta ninguna generación): 0 nombres duplicados, 0 filenames duplicados, 0 mojibake.
  - `backend/character_prompts.json` (19 entradas, mismo tipo que el anterior pero de personajes): revisado íntegro a mano, sin duplicados ni mojibake, nombres coherentes con personajes de las obras de dominio público del catálogo.
  - Se leyó `scripts/inject_ai_data.py` (consumidor real de `synopses_backup.json`/`biographies_backup.json`) para confirmar que es 100% local (lee el JSON, hace `Book.objects.get(id=...)`/`Author.objects.get(id=...)` y `.save()`, sin llamadas HTTP ni SDKs de IA) antes de basar el análisis en su estructura de datos (`book_id`/`author_id` como claves).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (no se trabajó Fase F; el scan de archivos de la corrida #41 sigue siendo el más reciente).
- **NEEDS MANUAL REVIEW nuevos**: **MR-0005** (biografía duplicada entre 2 `author_id` en `biographies_backup.json`, posible autor duplicado) y **MR-0006** (1 sinopsis placeholder en `synopses_backup.json`, contenido realmente faltante). MR-0002, MR-0003 y MR-0004 revisadas, siguen abiertas sin cambios (ningún humano las había resuelto aún).
- **Pruebas**: Backend — `manage.py check` OK, `makemigrations --check --dry-run` OK. No se corrió `manage.py test` completo porque esta corrida no modificó ningún archivo de código/esquema (solo lectura de JSON de datos versionados) — regla de sección 6 ("cambios solo en datos... `manage.py test` + `check`" no aplica tampoco porque no se modificó ningún dato, fue auditoría de solo lectura sin escritura). Frontend — no se tocó, no se corrió build/Karma.
- **Bundle**: sin cambios (no se tocó frontend esta corrida).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye únicamente `NIGHT_AUDIT_STATE.json` y `NIGHT_AUDIT_LOG.md` — no hay cambios de código/datos que commitear esta corrida, solo el avance del estado de auditoría y los 2 nuevos hallazgos).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T21:15:00Z — corrida horaria #43

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar (contenedor nuevo). Guarda de solapamiento: `run_in_progress` era `false`, `heartbeat_utc` de la corrida #42 (19:59:39Z) tenía ~54 min de antigüedad -> sin corrida viva, se continuó normal.
- **Rama y commit base**: `agent/nightly-optimization-chris` ya existía en origin (no bootstrap); `git fetch origin` + `git checkout agent/nightly-optimization-chris` (HEAD `ef94126`, commit de la corrida #42) + `git pull --ff-only origin agent/nightly-optimization-chris` -> ya al día. Commit base confirmado: `git merge-base agent/nightly-optimization-chris origin/Chris` = `3786d288fd23e4228b6c0390ac00ee9cf6533940` y `git merge-base --is-ancestor origin/Chris agent/nightly-optimization-chris` -> SÍ (confirma que viene de Chris). Ramas `Chris` (`3786d28`), `Felipe` (`e794ef9`), `eithan` y la rama vieja `agent/nightly-optimization` (`e794ef9`, basada en Felipe) confirmadas presentes en origin y NO tocadas, NO usadas como base.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` (exit 0) -> **push verificado: OK**.
- **Smoke check**: contenedor nuevo, sin `.venv312` persistido. Se creó venv con `python3 -m venv` (python 3.11.15 por defecto) y `pip install -r requirements.txt` **falló**: `requirements.txt` pinea `Django==6.0.4`, que exige Python >=3.12. Se recreó el venv con `python3.12 -m venv .venv312` (`/usr/bin/python3.12`, Python 3.12.3, sí disponible en el contenedor) y la instalación completa de las 90 dependencias terminó **OK** sin errores. `.env` local gitignorado creado (`DATABASE_URL=sqlite:///db_local_audit.sqlite3`, sin credenciales reales; confirmado en `.gitignore` que `.env`, `.venv312/` y `*.sqlite3` ya están ignorados). `manage.py check` -> `System check identified no issues (0 silenced)`. `makemigrations --check --dry-run` -> `No changes detected`.
- **Fase y lote trabajados**: cierre del pendiente de Fase B dejado por la corrida #42 (revisión de `BOOK_CHAPTER_AUDIT.json`/`BOOK_CHAPTER_AUDIT_REPORT.md` y `scripts/db_setup/clean_book_titles.py`) -> avance de Fase C (`C_personajes_avatares`) sobre datos locales.
- **Cambios aplicados**: ninguno (0 archivos de código/datos modificados). Todo lo auditado ya estaba correcto o bloqueado por falta de BD real.
- **Auditoría realizada**:
  - `backend/BOOK_CHAPTER_AUDIT.json` (1046 entradas `{book,slug,author,edition,chapters_db,chapters_original,source_file,original_format,status}`) y `backend/BOOK_CHAPTER_AUDIT_REPORT.md` (~13700 líneas): ambos versionados en el mismo commit base de Chris (`3786d28`) -> **son la propia auditoría real** que el dueño ya corrió contra la BD de producción (modo `--fix`, ya aplicado). Distribución: 818 `OK`, 150 `WARNING`, 78 `FIXED`, 0 `ERROR`. Las `WARNING` son mayormente "capítulo sospechosamente corto (<50 caracteres)" en capítulos que legítimamente son solo un título de sección (ej. "Primera parte") — no lucen como errores de import reales. No accionable de nuevo sin poder verificar contra el estado ACTUAL de la BD real (pudo cambiar desde que se generó el reporte) — sigue bloqueado por **MR-0002**, no se generó MR nuevo (ya cubierto).
  - `scripts/db_setup/clean_book_titles.py` revisado línea a línea: 100% local (lee `.epub` de `media/books/<slug>/`, extrae `<dc:title>` del `.opf`, actualiza `Book.title`), idempotente, sin llamadas externas. Correcto tal cual está, nada que corregir.
  - `backend/json_data/characters_to_generate.json` (7060 entradas `{id,name,book,prompt}`, prompts de imagen de personajes para IA — **no se ejecuta ninguna generación**, solo se auditó el texto): 0 `id` duplicados, 0 mojibake (patrones `Ã.`/`Â.`/`â€.`) en `name`/`book`/`prompt`. 1 hallazgo: `prompt` vacío en el `id` `cce30716-176e-4170-94c2-ecbc4d75c924` (personaje "Muley (El Rey de Fez, también llamado Muley)", libro "El príncipe constante") -> **MR-0007 nueva**. 3 pares `(name,book)` con 2 entradas cada uno (Narrador Anónimo/Algunas notas sobre algo que no existe; Amado Nervo (Narrador Poético)/En voz baja; La Muerte/Poemas) revisados a mano en detalle: cada entrada tiene `id` único y texto de `prompt` **distinto** (2 variantes de descripción visual, no un error de duplicación) -> confirmado **SIN CAMBIO**.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (no se trabajó Fase F; el scan de archivos de la corrida #41 sigue siendo el más reciente).
- **NEEDS MANUAL REVIEW nuevos**: **MR-0007** (1 prompt de imagen vacío para el personaje "Muley" en "El príncipe constante", contenido realmente faltante, prohibido inventarlo). MR-0002, MR-0003, MR-0004, MR-0005, MR-0006 revisadas, siguen abiertas sin cambios (ningún humano las había resuelto aún).
- **Pruebas**: Backend — `manage.py check` OK, `makemigrations --check --dry-run` OK. No se corrió `manage.py test` completo (regla de sección 6: sin cambios de código/esquema, solo lectura de JSON de datos versionados y de un reporte histórico). Frontend — no se tocó, no se corrió build/Karma. No es la corrida de verificación diaria (fuera de la ventana 00:00–01:00 UTC; la última verificación completa fue `2026-09-04T00:58:00Z`, ya registrada en el STATE).
- **Bundle**: sin cambios (no se tocó frontend esta corrida).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye únicamente `NIGHT_AUDIT_STATE.json` y `NIGHT_AUDIT_LOG.md` — no hay cambios de código/datos que commitear esta corrida, solo el avance del estado de auditoría y el nuevo hallazgo MR-0007).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T21:57:09Z — corrida horaria #44

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar (contenedor nuevo). Guarda de solapamiento: `run_in_progress` era `false`, `heartbeat_utc` de la corrida #43 (21:15:00Z) tenía ~39 min de antigüedad -> sin corrida viva, se continuó normal.
- **Rama y commit base**: `agent/nightly-optimization-chris` ya existía en origin (no bootstrap); `git fetch origin` + `git checkout agent/nightly-optimization-chris` (rastrea `origin/agent/nightly-optimization-chris`, HEAD `eeb05ce`, commit de la corrida #43) + `git pull --ff-only origin agent/nightly-optimization-chris` -> ya al día. Commit base confirmado: `git merge-base agent/nightly-optimization-chris origin/Chris` = `3786d288fd23e4228b6c0390ac00ee9cf6533940`, coincide exactamente con `origin/Chris` HEAD -> confirma que viene de Chris. Ramas `Chris` (`3786d28`), `Felipe`, `eithan` y la rama vieja `agent/nightly-optimization` (basada en Felipe) confirmadas presentes en origin y NO tocadas, NO usadas como base.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` (exit 0) -> **push verificado: OK**.
- **Smoke check**: contenedor nuevo, sin `.venv312` persistido. `python3.12 -m venv .venv312` + `pip install -r requirements.txt` (90 dependencias) OK sin errores. `.env` local gitignorado creado (`DATABASE_URL=sqlite:///audit_local.sqlite3`, sin credenciales reales; `STANDARDIZE_ON_IMPORT=False` para no disparar estandarización en import). `manage.py check` -> `System check identified no issues (0 silenced)`. `makemigrations --check --dry-run` -> `No changes detected`.
- **Fase y lote trabajados**: `H_backend_api` -> siguiendo el "próximo lote sugerido" de la corrida #43 (agotada la Fase C de datos locales), se revisaron los scripts de `backend/scripts/` aún no auditados y `catalog/serializers.py` por posibles campos expuestos de más.
- **Cambios aplicados**: ninguno (0 archivos de código/datos modificados). Todo lo revisado ya estaba correcto, o bloqueado por falta de BD real (MR-0002), o excluido permanentemente por tocar servicios externos/generación de IA (reglas inquebrantables de la rutina).
- **Auditoría realizada**:
  - `scripts/fix_broken_books.py` (repara libros con capítulos vacíos/rotos re-extrayendo el spine del EPUB): revisado línea a línea, 100% local (`ebooklib`+`BeautifulSoup`, sin llamadas externas), limpia spam de scraping conocido (`Lectulandia`, `elejandria`, etc.) y reescribe rutas de imagen a `/media/books/<slug>/images/`. Requiere `Book`/`Edition`/`Chapter` reales con EPUB real asociado para tener efecto -> bloqueado por **MR-0002**. Script correcto tal cual, nada que corregir.
  - `scripts/db_setup/sync_supabase_urls.py`: revisado — **SÍ toca la nube** (`from supabase import create_client`, referencia el bucket real `literatus-media` en Supabase Storage), aunque su `main()` actual solo imprime una URL base. Confirmado como **exclusión permanente** por la regla "nada de subir a Supabase" — no se ejecuta nunca en esta rutina. No genera MR nueva (ya cubierto por la regla general, no es una decisión pendiente del dueño).
  - `scripts/colab_avatar_generator.py`: revisado — es generación de imágenes con Stable Diffusion XL vía `diffusers`/HuggingFace, pensado para correr en Google Colab con GPU. Prohibido por la regla general "nada de generación de imágenes" — **exclusión permanente**, no se ejecuta. No genera MR nueva.
  - `scripts/optimize_book_covers.py` (reescribe portadas a WEBP 600x900): revisado línea a línea, 100% local (Pillow puro), modo `dry-run` por defecto y solo actualiza `Book.cover_image` con `--apply` explícito — bien escrito y defensivo. Requiere `Book.objects` reales con `cover_image` apuntando a archivos reales de `MEDIA_ROOT` para tener efecto real -> bloqueado por **MR-0002**. Nada que corregir en el script.
  - `scripts/benchmark_suite.py` (mide queries/tiempos/payload de los endpoints del catálogo con `Django test Client`): revisado, 100% local, muy útil para Fases H/J pero sin sentido sobre una BD vacía (0 libros) -> bloqueado por **MR-0002** para dar resultados reales. Script correcto tal cual.
  - `catalog/serializers.py` (309 líneas, completo): revisado por "campos expuestos de más" (Fase H). `BookListSerializer`/`BookCatalogCardSerializer` ya son deliberadamente livianos (sin `editions`/`book_authors` pesados). `BookDetailFullSerializer.get_avatars` ya reutiliza el prefetch cache de `editions` cuando está disponible (evita N+1) con fallback a query directa. `get_total_words` ya usa muestreo (8 capítulos) + caché en el objeto en vez de cargar todo `content_html`. `ReviewSerializer` expone `username` y `user_avatar` pero **no** email ni otros datos sensibles del `User`. **Sin hallazgos accionables** — archivo ya bien optimizado.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (no se trabajó Fase F; el scan de archivos de la corrida #41 sigue siendo el más reciente).
- **NEEDS MANUAL REVIEW nuevos**: ninguno. MR-0002 a MR-0007 revisadas, siguen abiertas sin cambios (ningún humano las había resuelto aún).
- **Pruebas**: Backend — `manage.py check` OK, `makemigrations --check --dry-run` OK. No se corrió `manage.py test` completo (regla de sección 6: sin cambios de código/esquema, solo lectura/revisión de scripts existentes sin modificarlos). Frontend — no se tocó, no se corrió build/Karma. No es la corrida de verificación diaria (fuera de la ventana 00:00–01:00 UTC; la última verificación completa fue `2026-09-04T00:58:00Z`, sin cambios respecto al STATE).
- **Bundle**: sin cambios (no se tocó frontend esta corrida).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye únicamente `NIGHT_AUDIT_STATE.json` y `NIGHT_AUDIT_LOG.md` — no hay cambios de código/datos que commitear esta corrida, solo el avance del estado de auditoría; sin hallazgos nuevos).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-04T22:53:27Z — corrida horaria #45

- **Entorno**: `git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2` (coincide). `git status` limpio al iniciar (contenedor nuevo). Guarda de solapamiento: `run_in_progress` era `false`, `heartbeat_utc` de la corrida #44 (21:57:09Z) tenía ~56 min de antigüedad -> sin corrida viva, se continuó normal.
- **Rama y commit base**: `agent/nightly-optimization-chris` ya existía en origin (no bootstrap); `git fetch origin` + `git checkout -b agent/nightly-optimization-chris origin/agent/nightly-optimization-chris` (HEAD `8bf2c85`, commit de la corrida #44) + `git pull --ff-only origin agent/nightly-optimization-chris` -> ya al día. Commit base confirmado: `git merge-base HEAD origin/Chris` = `3786d288fd23e4228b6c0390ac00ee9cf6533940`, idéntico a `origin/Chris` HEAD -> confirma que viene de Chris. Ramas `Chris`, `Felipe`, `eithan` y la rama vieja `agent/nightly-optimization` (basada en Felipe) confirmadas presentes en origin y NO tocadas, NO usadas como base.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> `Everything up-to-date` (exit 0) -> **push verificado: OK**.
- **Smoke check**: contenedor nuevo, sin `.venv312` persistido. `python3.12 -m venv .venv312` + `pip install -r requirements.txt` OK sin errores. `.env` local gitignorado creado (`DATABASE_URL=sqlite:///db_audit_local.sqlite3`, sin credenciales reales). `manage.py check` -> `System check identified no issues (0 silenced)`.
- **Fase y lote trabajados**: `H_backend_api` — siguiendo la opción (c) del "próximo lote sugerido" de la corrida #44: re-auditar `library/views.py` y `ai_engine/views.py` línea a línea por N+1/paginación no cubiertos aún. Adicionalmente, primera pasada sobre 4 archivos JSON de `backend/json_data/` no auditados en corridas previas (`authors_to_generate.json`, `elejandria_master.json`, `manga_frames_generation.json`, `scraper_estado.json`).
- **Cambios aplicados**: ninguno (0 archivos de código/datos modificados). `library/views.py` (205 líneas) y `ai_engine/views.py` (479 líneas) revisados completos: confirmado que `UserInventoryViewSet.get_queryset` ya encadena `select_related('edition__book','progress')` + `prefetch_related('edition__book__genres','edition__book__tags','edition__book__editions','edition__book__book_authors__author','edition__avatars')`, y se verificó contra `EditionSerializer`/`BookListSerializer` (`catalog/serializers.py`) que cada método (`get_price` -> `obj.editions.all()`, `get_tags` -> `obj.tags.all()`, `get_author_name` -> `obj.book_authors.all()`) efectivamente reutiliza ese prefetch, sin N+1 real. En `ai_engine/views.py`: `AvatarListView` no usa `select_related('edition')` pero se confirmó en `AIAvatarListSerializer` que ningún campo serializado toca `avatar.edition` (solo campos propios del avatar) -> sin N+1 real, `SIN CAMBIO`. `GlobalAvatarListView`, `AvatarDetailView`, `RecentChatsView` y `DemoChatView.get` ya usan `select_related('edition__book')` correctamente (consumido por `GlobalHubAvatarSerializer.book_title`/`book_slug`). `ReadingProgressViewSet`/`UserBookmarkViewSet` no paginan, pero su alcance está acotado al propio usuario (tamaño ligado a su biblioteca personal, no un listado global) -> no calza como "listado grande sin paginación" de Fase H, `SIN CAMBIO`. **Conclusión: ambos archivos ya están bien optimizados, ninguna corrección de bajo riesgo pendiente.**
- **Auditoría de datos (JSON nuevos)**:
  - `backend/json_data/authors_to_generate.json` (559 entradas `{slug,title,prompt}`, generado por `scripts/colab_sync/generate_authors_json.py` directamente desde `Author.objects.all()` — **exportación de solo salida, no se re-inyecta a la BD**, confirmado leyendo el script generador línea a línea): 0 mojibake, 0 `slug` duplicados, 0 prompts vacíos. Se detectaron ~140 `title` que aparecen 2+ veces, incluyendo 12 casos de espacios dobles dentro del propio nombre (ej. `"H.  P. Lovecraft"`, `"H.  P.  Lovecraft"`, `"H. P.  Lovecraft"`, `"D.  H.  Lawrence"`, `"F.  Scott Fitzgerald"` — 3 variantes de slug distintas para el mismo autor) y variantes de un mismo autor con distinto formato (`"Benito Pérez Galdós"` / `"B. Pérez Galdós"` / `"Benito Perez Galdós"` sin tilde; `"León Tolstói"` / `"León Tolstoi"` / `"León Tólstoi"`; `"Pardo Bazán,  Emilia"` con coma invertida y doble espacio; una entrada con un email como `title` (`josanmartinezsantiago@gmail.com`) en vez de un nombre de autor). Como el archivo es un **espejo de solo lectura de `Author.full_name`/`Author.slug` real**, NO se editó el JSON (no tendría efecto, se regenera desde la BD) -> el hallazgo real es evidencia adicional de que `Author.full_name` en la BD de producción tiene registros con doble espacio y variantes de formato/acentuación -> **MR-0008 nueva**, complementa a MR-0002 (requiere BD real para poder normalizar `full_name` de forma segura, sin arriesgar romper el `slug` ya usado en URLs públicas).
  - `backend/json_data/elejandria_master.json` (dict, 33 claves), `backend/json_data/manga_frames_generation.json` (lista, 19 entradas), `backend/json_data/scraper_estado.json` (dict, 3 claves): los 3 parsean como JSON válido, 0 mojibake detectado (`Ã©`, `â€™`, etc.) en ninguno de los 3. No se profundizó más por límite de tiempo del lote — quedan como "revisados nivel 1 (parseo + mojibake), pendiente revisión de contenido más fina en una corrida futura" si aplica.
- **Tabla de duplicados de imágenes**: no aplica esta corrida (no se trabajó Fase F; el scan de archivos de la corrida #41 sigue siendo el más reciente).
- **NEEDS MANUAL REVIEW nuevos**: **MR-0008** (evidencia de `Author.full_name` con espacios dobles y variantes de formato para el mismo autor, visible en `authors_to_generate.json`; requiere BD real para decidir la forma canónica y fusionar sin romper `slug`/URLs). MR-0002 a MR-0007 revisadas, siguen abiertas sin cambios (ningún humano las había resuelto aún).
- **Pruebas**: Backend — `manage.py check` OK. No se corrió `makemigrations --check --dry-run` ni `manage.py test` completo esta corrida (regla de sección 6: no se modificó ningún archivo de código/esquema, solo lectura/auditoría de vistas y JSON de datos versionados, sin escritura). Frontend — no se tocó, no se corrió build/Karma. No es la corrida de verificación diaria (fuera de la ventana 00:00–01:00 UTC; última verificación completa `2026-09-04T00:58:00Z`, sin cambios).
- **Bundle**: sin cambios (no se tocó frontend esta corrida).
- **Commit**: ver hash en el commit siguiente en el historial de git (incluye únicamente `NIGHT_AUDIT_STATE.json` y `NIGHT_AUDIT_LOG.md` — no hay cambios de código/datos que commitear esta corrida, solo el avance del estado de auditoría y el nuevo hallazgo MR-0008).
- **Push confirmado en origin**: ver verificación con `git ls-remote` registrada en el informe final de esta corrida.

## 2026-09-05T00:00:25Z — corrida horaria #1 (día 2026-09-05)

- **Entorno**: repo OK (`git rev-parse --show-toplevel` termina en `LiteratusNovelistV2`; `git remote -v` -> `github.com/Cxris2145/LiteratusNovelistV2`). `git status` limpio al iniciar. Rama `agent/nightly-optimization-chris` existía en origin (45 corridas previas); checkout local desde `origin/agent/nightly-optimization-chris`. `git status` sin cambios pendientes de una corrida interrumpida.
- **Commit base**: `3786d288fd23e4228b6c0390ac00ee9cf6533940`, confirmado `git merge-base agent/nightly-optimization-chris origin/Chris` == `origin/Chris` HEAD, es decir la rama sigue basada en `origin/Chris` sin desviarse.
- **Guarda de solapamiento**: `run_in_progress` era `false` y `heartbeat_utc` de la corrida #45 (~1h antes) -> sin corrida viva, se continuó normal.
- **Push verificado (sección 2.9)**: `git push --dry-run origin HEAD:refs/heads/agent/nightly-optimization-chris` -> **OK** ("Everything up-to-date").
- **Smoke check (Fase A)**: entorno en frío, venv `.venv312` recreado con `python3.12`, `requirements.txt` instala limpio. `manage.py check` -> OK (0 issues). `makemigrations --check --dry-run` -> OK (sin cambios pendientes).
- **VERIFICACIÓN DIARIA (sección 6)**: la corrida cruzó la medianoche UTC durante su ejecución (empezó ~23:53 UTC del 04-09, el smoke check cayó ya en 2026-09-05T00:00 UTC) -> se disparó la verificación diaria completa: `manage.py test` (suite completa) -> **67/67 OK**. `npm install` (1105 paquetes, limpio) + `npm run build` -> **OK**, exit 0. Bundle initial: **1.34 MB raw / 275.18 kB transferencia** — idéntico a la referencia histórica de la corrida #37, sin regresión. `last_full_verification_utc` actualizado a `2026-09-05T00:00:25Z`.
- **Fase y lote trabajados**: Fase E (sinopsis/categorías/portadas), continuando el pendiente explícito dejado por la corrida #45: profundizar más allá de mojibake superficial en `json_data/elejandria_master.json`, `json_data/manga_frames_generation.json` y `json_data/scraper_estado.json`.
- **Cambios aplicados**:
  - `backend/json_data/elejandria_master.json` (mapa Género -> lista de `Book.slug`, consumido literal por `scripts/db_setup/bulk_db_injection.py::sync_categories()` vía `Book.objects.filter(slug=s[:100])`) — **CORREGIDO**: 38 de 2161 slugs no cumplían el patrón `[a-z0-9]+(-[a-z0-9]+)*` (17 con guion doble/triple, ej. `goethe-wolfgang--johann`, `filosofia-del-arte---tomo-i`; 9 con carácter zero-width-space U+200B embebido o al inicio; 2 con espacio angosto U+202F en vez de guion, ej. `arsenio lupin`; 6 con comillas/apóstrofe literal embebido, ej. `la-corbeta-"gloria-scott"`; 6 con macrón no-ASCII `ū`/`ō` inconsistente con el resto del archivo, ej. `ryūnosuke`, `rashōmon`). Ninguno de estos 38 podría matchear jamás un `Book.slug` real generado por `django.utils.text.slugify` (que nunca produce esos caracteres), por lo que la asignación de género para esos 38 libros fallaría silenciosamente si el script se ejecutara. Se normalizaron a minúsculas + dígitos + guion simple ASCII con un script Python puntual (eliminar zero-width-space, espacio angosto/normal -> guion, quitar comillas/apóstrofes, `unicodedata.normalize('NFKD')` + descartar combinantes para los macrones, colapsar guiones múltiples, recortar guiones en los bordes). Verificado cruzando 17 de los 38 casos contra el nombre real de carpeta en `respaldos-software/books/` (ej. `fausto-goethe-wolfgang-johann`, `rashomon-ryunosuke-akutagawa`, `la-corbeta-gloria-scott-conan-doyle-arthur`, `la-detencion-de-arsenio-lupin-maurice-leblanc`) — coincide exacto en los 17. Además se comprobó que los 3 slugs que aparecen 2 veces en el archivo completo (`el-decameron-boccaccio-giovanni`, `frankenstein-mary-shelley`, `novelas-ejemplares-cervantes-miguel`) están en géneros distintos (multi-género legítimo, no error) y que ningún género tiene duplicados internos -> SIN CAMBIO ahí. Diff: exactamente 38 líneas modificadas, re-serializado con `json.dump(ensure_ascii=False, indent=2)` idéntico al formato original en todo lo demás. 0 slugs inválidos restantes tras el fix.
- **Sin hallazgos (revisado a fondo, sin cambios)**:
  - `backend/json_data/manga_frames_generation.json` (19 entradas de prompts de imagen manga, NO se ejecutan): 19/19 `id` únicos, 0 pares `(name, book)` duplicados, 0 campos vacíos o con doble espacio en `name`/`book`/`description`/`base_prompt`/`frames.*`.
  - `backend/json_data/scraper_estado.json`: `descargados` (1985 URLs) sin duplicados exactos ni mojibake; `fallidos` (207 URLs->motivo); 0 URLs que estén marcadas como fallidas y a la vez ya presentes en `descargados` (estado consistente, sin datos obsoletos).
- **Tabla de duplicados de imágenes**: no aplica esta corrida (no era el foco del lote; ver corrida #41 para el scan SHA-256 de `respaldos-software/books/`, bloqueado por MR-0002 para confirmar contra `Book.cover_image` real).
- **NEEDS MANUAL REVIEW nuevos**: ninguno esta corrida. MR-0002 a MR-0008 revisadas, siguen abiertas sin cambios (ningún humano las ha resuelto aún).
- **Pruebas**: Backend — `manage.py check` OK, `makemigrations --check --dry-run` OK, `manage.py test` completo **67/67 OK** (verificación diaria). Frontend — `npm run build` OK, exit 0, bundle initial sin regresión (1.34 MB raw / 275.18 kB transferencia, igual a la referencia histórica). El cambio de datos (`elejandria_master.json`) no tiene test automatizado propio (requiere `Book` reales en BD para ejercer `sync_categories()`, bloqueado por MR-0002); se validó manualmente: JSON parseable, 0 slugs inválidos restantes, coincidencia exacta contra nombres de carpeta reales en 17/17 casos verificables.
- **Commit**: `audit(hourly #1 2026-09-05): fase E — 38 slugs invalidos corregidos en elejandria_master.json; manga_frames/scraper_estado auditados sin hallazgos; verificacion diaria OK (backend 67/67, build sin regresion)` (ver hash en el historial de git inmediatamente después de esta entrada).
- **Push confirmado en origin**: sí — verificado con `git ls-remote origin refs/heads/agent/nightly-optimization-chris` == `git rev-parse HEAD` tras el push (ver informe final de esta corrida).
